You are solving a Python benchmark.

## Task

{{DESCRIPTION}}

**IMPORTANT: Write your complete solution to: {{SOLUTION_PATH}}**

Expected output:
{{EXPECTED_OUTPUT}}

## Requirements

- Timeout: {{TIMEOUT}} seconds
- Write your solution to: **{{SOLUTION_PATH}}**
- Your solution must produce output matching the expected output **exactly** (no extra newlines, spaces, etc.)
- Read {{SOLUTION_PATH}} file first so you know the template to write to
- If your solution is impossible due to Python language limitations - stop and report

## Verification Steps (REQUIRED!)

**You MUST verify your solution before finishing:**

1. **Run your solution:**
   ```
   python3 solution.py
   ```

2. **Compare output carefully:**
   - Check that every line matches expected output
   - No extra spaces or formatting differences

3. **If output doesn't match:**
   - Fix your code
   - Re-run to verify
   - Repeat until output matches exactly

4. **Only finish once verified!**

## Available Tools

You have access to standard Python tools:
- **Run code:** `uv run --python {{PYTHON_VERSION}} solution.py` (this is exactly what the grader invokes). Plain `python3 solution.py` may hit a different interpreter — prefer the uv form when testing.
- **Check syntax:** `uv run --python {{PYTHON_VERSION}} -m py_compile solution.py`

## Tips

- Standard library is available
- **The Python runtime is pinned at {{PYTHON_VERSION}} via uv.** You can freely use features up to and including {{PYTHON_VERSION}} (e.g. `match`/`case`, `X | Y` unions, `type` statement). Features newer than {{PYTHON_VERSION}} will SyntaxError.

Good luck!
