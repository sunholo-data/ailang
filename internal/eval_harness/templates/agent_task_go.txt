You are solving a Go benchmark.

## Task

{{DESCRIPTION}}

**IMPORTANT: Write your complete solution to: {{SOLUTION_PATH}}**

Expected output:
{{EXPECTED_OUTPUT}}

## Requirements

- Timeout: {{TIMEOUT}} seconds
- Write your solution to: **{{SOLUTION_PATH}}**
- Your solution must produce output matching the expected output **exactly** (no extra newlines, spaces, etc.)
- Read {{SOLUTION_PATH}} file first so you know the template to write to
- If your solution is impossible due to Go language limitations - stop and report

## Verification Steps (REQUIRED!)

**You MUST verify your solution before finishing:**

1. **Run your solution:**
   ```
   go run solution.go
   ```

2. **Compare output carefully:**
   - Check that every line matches expected output
   - No extra spaces or formatting differences

3. **If output doesn't match:**
   - Fix your code
   - Re-run to verify
   - Repeat until output matches exactly

4. **Only finish once verified!**

## Available Tools

You have access to standard Go tools:
- **Run code:** `go run solution.go` (this is exactly what the grader invokes)
- **Build check:** `go build solution.go`

## Tips

- Go standard library is available (fmt, os, bufio, sort, math, strconv, strings, encoding/json, etc.)
- Do NOT use external modules — only the Go standard library
- Your file must start with `package main` and define `func main()`
- Use `fmt.Println()` for output (adds a newline)
- Use `fmt.Print()` if you need output without a trailing newline

Good luck!
