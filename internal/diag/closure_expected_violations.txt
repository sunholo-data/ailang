# Known leaks from the language core into the platform, as of 2026-09-14
# (dev @ 7133edd32). Read by internal/diag/closure_test.go, which fails on any
# NEW entry and on any entry that is no longer reached — so this file can only
# shrink. Each line is a platform package (path under the module) or a
# third-party root exactly as spelled in tools/simplicity_metrics.sh.
#
# Why each is here, and the phase that removes it
# (design_docs/planned/m-v1-simplification-program.md):
#   internal/ai          effects/ai*.go              → Phase 1.3 registration seam
#   internal/secrets     effects/secret.go           → Phase 1.3 registration seam
#   internal/telemetry   pipeline, repl, effects cb  → Phase 1.3 no-op interface in core
#   internal/mcp_client  prompt                       → Phase 1.3
#   ollama SDK           builtins/ollama_embed.go    → Phase 1.3
#   otel / grpc / gcp    via telemetry               → gone when telemetry is a no-op here
#
# Cleared (M-V1-SIMPLIFY-S2 M2, 2026-09-15): go-sqlite3 and gorilla/websocket —
# effects now opens its persistent SharedCache and its ws:// transport through
# registration seams; the implementations live in internal/platform/{sharedmem,streamws}.
internal/ai
internal/secrets
internal/telemetry
internal/mcp_client
github.com/ollama/ollama
go.opentelemetry.io
google.golang.org/grpc
cloud.google.com
