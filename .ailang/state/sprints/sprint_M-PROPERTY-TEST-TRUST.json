{
  "sprint_id": "M-PROPERTY-TEST-TRUST",
  "status": "in_progress",
  "created": "2026-07-31T19:47:43Z",
  "branch": "sprint/m-property-seed-determinism",
  "worktree": "/tmp/wt-m-property-seed-determinism",
  "base_commit": "386cf6d15",
  "design_doc": "design_docs/planned/v0_31_0/m-property-seed-determinism.md",
  "sprint_plan": "design_docs/planned/v0_31_0/m-property-seed-determinism-sprint-plan.md",
  "github_issues": [547, 535],
  "planner_model": "claude-opus-5",
  "mission_iteration": 126,
  "execution_environment": "EXECUTE M1 ONLY. M2 and M3 are PARKED to the 2026-08-03 07:00 re-arm and MUST NOT be started in this iteration. AILANG-fix lane: Go only, package internal/testing + one new cmd/ailang test file. HARD BLOCKER: internal/testing/runner.go is 790 lines of an 800-line CI cap (make check-file-sizes, .github/workflows/ci.yml:97, `wc -l > 800` -> exit 1). M1's FIRST commit MUST be a pure byte-for-byte move of runEnsuresProperty (runner.go:312-432, 121 lines) into a NEW file internal/testing/contract_domain.go, verified green before any logic change; runner.go must land at 669 lines. Growing runner.go in place breaks CI. The design doc is UNTRACKED in the worktree (?? design_docs/planned/v0_31_0/m-property-seed-determinism.md); the tracked copy landed on dev in 01c36db8d and both have shasum c9f51414cf59dfe758d64a0bfab4da7aa8b61096 - do NOT `git add` it, only the sprint plan and this JSON. The codex sandbox CANNOT bind loopback sockets: any `bind: operation not permitted` in `go test ./cmd/ailang/...` is UNINFORMATIVE UNDER SANDBOX, neither pass nor product failure, and the controller re-runs AC12-M1 outside the sandbox. Codex cannot git commit in a linked worktree - the controller finalizes every commit. Use `refs #547` on development commits; the final M1 commit may use `Fixes #547` but MUST NOT use any closing keyword for #535, which stays OPEN after M1. DO NOT fix the named-test body stripper bug found during planning and DO NOT touch internal/testing/executor.go. NOTE: .ailang/ is gitignored but sibling sprint JSONs are TRACKED - commit with `git add -f .ailang/state/sprints/sprint_M-PROPERTY-TEST-TRUST.json`.",
  "metadata": {
    "scope": "M1 ONLY: close #547, the vacuous-FAILURE class in `ailang test`. runEnsuresProperty never evaluates the function's `requires` clause, so it reports `ensures violated` for inputs the contract EXCLUDES. Reproduced first-hand 3/3 on the doc's precond2.ail (x=-611, -384, -787 - every value excluded by `requires { x > 100 }`), while runRequiresProperty SKIPS the identical condition. Fix: a new all-of-kind accessor findAllLoweredContractPredicates over the already-cached DeclMeta.Contracts, conjunctive short-circuit filtering of each generated tuple through the EXISTING EvaluateRequiresHarnessFromCore (which binds params WITHOUT calling the function), 100 accepted cases / 1000 generation attempts, and cap exhaustion as StatusSkip + SkipKindOutOfContract + `unverified: ...` - never pass, never violated. Plus integer generated_inputs/discarded_inputs on all three property paths and in JSON/human output.",
    "non_scope": "NO seed work of any kind. #535 stays OPEN after M1: newRNG's `if seed == 0 { seed = time.Now().UnixNano() }` at runner.go:786-788 is UNTOUCHED, no --seed flag, no --random-seed flag, NO internal/testing/config.go, NO TestConfig, NO WorkspaceRoot, NO SeedMode, NO MasterSeed, NO seed/seed_mode/seed_derivation JSON fields, NO crypto/rand. Zero diff permitted in internal/testing/generator.go, internal/testing/harness.go, internal/testing/executor.go, cmd/ailang/main.go, cmd/ailang/test.go. No change to runRequiresProperty's standalone out_of_contract semantics beyond adding the two counters. No parser/typechecker/elaborator/Core/evaluator/VM/effects/codegen change. Do NOT fix the pre-existing named-test-body stripper bug found during planning (see doc_defects_corrected_by_this_plan) - route it as a separate issue.",
    "premises_verified_first_party": [
      "newRNG runner.go:784-790 substitutes time.Now().UnixNano() when seed==0 - CONFIRMED verbatim",
      "Three call sites runner.go:261/386/505 all newRNG(DefaultConfig().Seed), DefaultConfig().Seed==0 at generator.go:29 - CONFIRMED",
      "runEnsuresProperty runner.go:322-432, loop 388-427, zero requires evaluation - CONFIRMED",
      "runRequiresProperty runner.go:537-547 skips the identical condition, comment says such inputs 'aren't a function bug' - CONFIRMED",
      "findLoweredContractPredicate runner.go:566-605 reads kind-tagged meta.Contracts in source order; all-of-kind is a filter, not a pipeline change - CONFIRMED",
      "PAR_DUPLICATE_REQUIRES rejects repeated blocks - CONFIRMED by running ailang check on a 2-block fixture",
      "Comma-separated conditions enumerate at cols 12 then 19 - CONFIRMED EXACTLY (g_property_1 :5:12, g_property_2 :5:19, g_property_3 :6:11, h_property_1 :12:11)",
      "TestConfig.WorkspaceRoot absent; no internal/testing/config.go - CONFIRMED with a known-positive control (the same grep DID find `generator.go:17:type GenConfig struct {`), so the empty result is a fact not a claim",
      "CI never runs `ailang test` over examples: scripts/verify_examples.go:104-115 passes `run` in all three exec forms - CONFIRMED",
      "27 example files contain `requires {` - CONFIRMED",
      "runner.go is 790 lines; make check-file-sizes caps at 800 via `wc -l`, wired at ci.yml:97 - CONFIRMED, 10 lines of headroom",
      "#547 reproduced 3/3 on precond2.ail; #535 reproduced with exit codes 1,1,0,1,1 across 5 runs of an unchanged list_recursive_verify.ail",
      "Existing ensures tests (clampBuggy/clampOk/maxOf) all use functions with NO requires, so the empty-slice fast path must keep them byte-identical - verified by reading runner_ensures_test.go",
      "SkipKindOutOfContract already exists (result.go:20) and is already excluded from VacuousSkips (result.go:97-99), so an out-of-contract skip does not by itself fail a suite with other passing work"
    ],
    "doc_defects_corrected_by_this_plan": [
      "BLOCKING (resolved): AC1/AC3/AC8 all invoke `./bin/ailang test --seed 42`, but --seed is an M2 deliverable. `ailang test` accepts exactly --format/--no-color/--package/--allow-skips (cmd/ailang/main.go:112-117). As written NONE of M1's acceptance criteria could be run after M1, contradicting the M1-independently-landable constraint. Plan section 4 restates AC1/AC2/AC3/AC8 seed-free, with the probabilistic margins computed (precond2 accepts p~0.45 -> mean 450 of 1000; unreachable accepts p=0 exactly for every seed; ordered.ail accepts p~0.25 -> mean 250).",
      "BLOCKING (resolved): AC3's fixture cannot pass for TWO independent reasons. (1) `assert(true)` does not parse - `assert` is a RESERVED KEYWORD (std/debug.ail:18 says so explicitly); measured PAR_NO_PREFIX_PARSE. (2) Even corrected to `test \"...\" { true }` it still fails, because of a PRE-EXISTING UNREPORTED BUG: Executor.executeNamedTestBody (executor.go:190-300) rebuilds the module via stripNonPureFunctions (executor.go:475) and leaves ORPHANED `requires`/`ensures` clauses at top level in _namedtest_body_*.ail, which the parser rejects. Isolated with 4 one-variable fixtures: test-block-alone PASSES, test-block-plus-contracts FAILS. Therefore ANY .ail file containing both a named test block and a contract-bearing function has a broken named-test path today. Plan section 4 AC3-M1 drops the sentinel test block and uses a second contract-bearing function (`ok`) to keep ran>0; verified this substitute works at HEAD.",
      "BLOCKING FOR M2 (parked): AC9's module-less fixture does not parse. ExtractFunctionBinding copies the source to a temp dir and re-parses; the module-less `export func` + `ensures` form fails there with PAR_NO_PREFIX_PARSE at the `ensures` line. Reproduced BOTH inside /tmp and inside the repo, so it is not the MOD010 temp-path relaxation. AC9 can never exit 0 as written, and it is the ONLY criterion proving WorkspaceRoot reached the derivation. Must be resolved before M2 starts at the 2026-08-03 re-arm.",
      "GAP (corrected): the doc adds generated_inputs/discarded_inputs to the ensures path only, so runForallProperty and runRequiresProperty would emit 0/0 - indistinguishable from 'generated nothing'. That is a silent-fallback shape and exactly the dead-field state the M1-only constraint forbids. Plan binds all three paths to populate both counters (~4 extra lines).",
      "SIGNATURE (corrected): the doc specifies findAllLoweredContractPredicates(propCase, core.RequiresKind), but the existing findLoweredContractPredicate takes BOTH an ast.ContractKind and a core.ContractKind (runner.go:566). The new accessor correctly needs only the core kind because it does no index matching against propCase.Function.Properties. Noted so this reads as a deliberate simplification, not an omission."
    ],
    "point_of_no_return": "None in M1. M1 touches no CLI surface, no shared config, and creates no new public API; `git revert` of the M1 commits is a clean full rollback. The design doc's 'a partial rollback is unsafe' warning applies to the COMBINED M1+M2 state, and specifically to keeping M2's deterministic seed while dropping M1's filter (which would freeze a false positive into a permanent deterministic failure). Dropping M2 while keeping M1 is the SAFE direction and is exactly what this iteration ships.",
    "m1_only_leaves_in_tree": {
      "present_and_complete": [
        "internal/testing/contract_domain.go with the full requires filter; runner.go at 669 lines",
        "PropertyResult.GeneratedInputs / .DiscardedInputs populated on ALL THREE property paths",
        "JSON integer keys generated_inputs / discarded_inputs on every property object",
        "Human output printing `accepted A, discarded D, generated G`",
        "out_of_contract skip carrying `unverified: requires filter accepted A of G generated inputs; need 100 (D discarded)`",
        "Fixtures under internal/testing/testdata/contracts/"
      ],
      "absent_cleanly_no_stubs": [
        "No --seed, no --random-seed; ailang test's flag set is unchanged",
        "No internal/testing/config.go, no TestConfig, no WorkspaceRoot, no SeedMode, no MasterSeed, no seed_derivation - not created, not stubbed, not referenced",
        "newRNG's wall-clock branch untouched; #535 stays open; default samples still vary run to run",
        "No seed / seed_mode JSON fields",
        "generator.go unchanged; DefaultConfig().Seed still 0"
      ],
      "strictly_better_because": "The vacuous-FAILURE class (#547) is closed with an honest skip taxonomy, and the flagship symptom - list_recursive_verify.ail reporting a false containsImpliesNonEmpty(xs=[]) violation in ~half of runs - stops occurring. AC4-M1 proves no existing test needed editing. No field, flag or file exists that nothing reads or writes."
    },
    "sandbox_caveat": "The codex executor cannot bind loopback sockets. Any `bind: operation not permitted` (or equivalent) result in `go test ./cmd/ailang/...` is UNINFORMATIVE UNDER SANDBOX - record it verbatim, never as pass and never as product failure. The controller re-runs AC12-M1 outside the sandbox before landing."
  },
  "velocity": {
    "target_loc_per_day": 400,
    "estimated_total_loc": 470,
    "estimated_days": 0.6,
    "basis": "M1 alone is ~590 gross / ~470 net LOC (the 121-line runEnsuresProperty relocation is behavior-neutral). Recent comparable internal/+cmd/ailang test-heavy sprints: m-nightly-run-validity-gate 635 LOC / 1.1d, m-nightly-sustained-failure-label 415 LOC / 0.9d -> sustained 350-450 net LOC/day. The design doc's full 650-830 LOC / 1.5-2 day estimate is consistent with that rate and correctly does NOT fit one unattended night, which is why only M1 is armed. Descope valve in plan section 3.4: must-ship = pure move + accessor + filtered loop + cap skip + counters on all three paths + T1-T7; the cmd/ailang e2e file may defer to M3."
  },
  "features": [
    {
      "id": "M1_ENSURES_CONTRACT_DOMAIN_FILTER",
      "description": "Close #547. STEP 1 (its own commit, no logic change): create internal/testing/contract_domain.go and MOVE runEnsuresProperty (runner.go:312-432, 121 lines incl. doc comment) into it byte-for-byte; verify go build + go test green and `wc -l internal/testing/runner.go` == 669. This is mandatory: runner.go is 790 of an 800-line CI cap and M1 adds ~90 lines of logic to that function. STEP 2: add findAllLoweredContractPredicates(propCase, core.RequiresKind) []core.CoreExpr in the new file - it calls r.executor.LastDeclMeta(propCase.FunctionCtx) and appends every c.Expr where c.Kind == core.RequiresKind, in DeclMeta order (= source order, verified at cols 12 then 19). STEP 3: rewrite the ensures loop as accepted<100 && generated<1000; per attempt increment generated, and if the requires slice is non-empty evaluate each predicate via the EXISTING Executor.EvaluateRequiresHarnessFromCore(harnessParams, pred) with the SAME []EnsuresParam tuple (verified at runner.go:519 to bind params without calling the function), short-circuiting on the first false; a false predicate increments discarded and `continue`s WITHOUT calling the function or evaluating ensures; a non-bool or evaluation error is StatusFail, NEVER a discard. TestsRun == accepted. STEP 4: if accepted < 100 on exit, StatusSkip + SkipKindOutOfContract + `unverified: requires filter accepted A of G generated inputs; need 100 (D discarded)` - 99 accepted is NOT a pass. STEP 5: add GeneratedInputs/DiscardedInputs to PropertyResult and populate them on ALL THREE property paths (forall and requires set generated=attempts, discarded=0) - no dead fields. STEP 6: emit both as INTEGERS in reporter.go formatPropertiesJSON and print `accepted A, discarded D, generated G` in human output for any property with discards or an out_of_contract skip. Zero diff permitted in generator.go, harness.go, executor.go, cmd/ailang/main.go, cmd/ailang/test.go.",
      "estimated_loc": 470,
      "estimated_hours": 5.0,
      "dependencies": [],
      "acceptance_criteria": [
        "AC10-M1 (STRUCTURAL, check this FIRST and LAST): `make check-file-sizes` exits 0 AND `test \"$(wc -l < internal/testing/runner.go)\" -le 700`. The 700 bound (not 800) is deliberate - it fails loudly if the executor grew runner.go in place instead of doing the pure move. Expected value: 669.",
        "AC1-M1 (the fix): fixture internal/testing/testdata/contracts/precond2.ail (module precond2; `export func big(x: int) -> bool ! {} requires { x > 100 } ensures { result == true } { x > 100 }`; plus a main). `./bin/ailang check <f>` exits 0; `./bin/ailang test --format json --no-color <f> > /tmp/precond2.json` exits 0; `jq -e '.failed_tests == 0 and ([.properties[] | select(.name == \"big_property_2\") | select(.status == \"pass\" and .tests_run == 100 and .discarded_inputs > 0 and .generated_inputs == (100 + .discarded_inputs))] | length == 1)' /tmp/precond2.json` exits 0. NO --seed flag (it does not exist until M2). MEASURED AT HEAD, MUST FLIP: big_property_2 is `fail` with `ensures violated for input: x=-611` in 3 of 3 runs. big_property_1 may remain an out_of_contract skip - not asserted.",
        "AC2-M1 (NEGATIVE CONTROL - the criterion that proves no new vacuous PASS): fixture precond_negative.ail with `requires { x > 100 } ensures { result == true } { false }`. `ailang check` exits 0; the test invocation exits 1; `jq -e '.properties[] | select(.name == \"broken_property_2\") | select(.status == \"fail\" and (.error | contains(\"ensures violated\")) and (.error | test(\"x=[1-9][0-9]{2,}\")))'` exits 0. The x=[1-9][0-9]{2,} pattern is load-bearing: it proves the reported counterexample SATISFIES x > 100.",
        "AC3-M1 (unreachable domain, FIXTURE REWRITTEN - see doc_defects): fixture requires_unreachable.ail with NO test block: `export func impossible(x: int) -> bool ! {} requires { x > 1000 && x < -1000 } ensures { result == true } { false }` plus `export func ok(x: int) -> bool ! {} ensures { result == true } { true }`. `ailang check` exits 0; the test invocation exits 0; `jq -e '.failed_tests == 0 and .success == true and ([.properties[] | select(.name == \"impossible_property_2\") | select(.status == \"skip\" and .skip_kind == \"out_of_contract\" and .tests_run == 0 and .generated_inputs == 1000 and .discarded_inputs == 1000 and (.error | startswith(\"unverified:\")))] | length == 1) and ([.properties[] | select(.name == \"ok_property_1\" and .status == \"pass\")] | length == 1)'` exits 0. MEASURED AT HEAD, MUST FLIP: impossible_property_2 is `fail`, suite exits 1. DO NOT use the design doc's `test \"suite sentinel passes\" { assert(true) }` - it cannot work (see doc_defects).",
        "AC4-M1 (no-requires functions unaffected, MUST NOT EDIT THESE TESTS): `go test ./internal/testing -run 'TestRunEnsuresProperty_(CorrectImplPasses|ViolationReportsCounterexample|MultiArgPredicateReferencesParams|ArithmeticInPredicate|ArithmeticViolation|MultiArgViolationReportsAllArgs)' -count=1` exits 0 AND `go test ./internal/testing -run 'TestRunContractProperties_SkipKinds' -count=1` exits 0, both with those test bodies UNMODIFIED. Every ensures fixture in them has no `requires`, so the empty-slice fast path must keep them byte-identical. If any needs editing, the fast path is wrong - STOP and report.",
        "AC8-M1 (representation/association/ordering, seed-free): a 2-requires-block fixture makes `ailang check` exit non-zero with PAR_DUPLICATE_REQUIRES. The doc's ordered.ail (comma-separated `requires { x > 0, y > 0 }` on g, bare ensures on h) makes `ailang test --format json --no-color` exit 0 with `jq -e '[.properties[].name] == [\"g_property_1\",\"g_property_2\",\"g_property_3\",\"h_property_1\"] and (.properties[0].location | endswith(\":5:12\")) and (.properties[1].location | endswith(\":5:19\")) and (.properties[2].location | endswith(\":6:11\")) and (.properties[3].location | endswith(\":12:11\")) and (.properties[2].status == \"pass\") and (.properties[2].discarded_inputs > 0) and (.properties[3].status == \"pass\") and (.properties[3].discarded_inputs == 0)'`. MEASURED AT HEAD: the four locations ALREADY match exactly, but g_property_3 is currently `fail` and the suite exits 1; M1 flips it to pass. h's discarded_inputs == 0 proves zero-requires isolation.",
        "AC-UNIT-M1: `go test ./internal/testing -run 'TestFindAllLoweredContractPredicates_(OrderAndAssociation|ZeroRequires)' -count=1` exits 0. OrderAndAssociation asserts g returns exactly 2 predicates in source order; ZeroRequires asserts h returns an empty/nil slice. These exercise the NEW all-of-kind accessor, not the existing single-predicate lookup.",
        "AC-MUTATION-M1: `go test ./internal/testing -run 'TestEnsures(NinetyNineAcceptedIsNotAPass|RequiresEvaluationErrorFailsLoudly|DiscardedTupleNeverCallsFunction)' -count=1` exits 0. NinetyNineAccepted uses a domain tuned to accept ~5% and asserts StatusSkip, NOT pass - it is the mutation guard on the `accepted < 100` boundary. RequiresEvaluationError asserts a non-bool/erroring requires predicate is StatusFail, never a discard.",
        "AC11-M1 (the flagship symptom stops): 5 consecutive runs of `./bin/ailang test --format json --no-color examples/runnable/contracts/list_recursive_verify.ail`; in NONE of the 5 may containsImpliesNonEmpty_property_2 have status `fail`. Its stable verdict may be `pass` OR an honest out_of_contract skip - both acceptable. MEASURED AT HEAD: exit codes 1,1,0,1,1 with property_2 = fail,fail,pass,fail,fail. EXIT CODES MAY STILL VARY after M1 alone because #535 is still open - AC11 asserts the VERDICT CLASS, not the exit code.",
        "AC13-M1 (cost bound on the new loop): `/usr/bin/time -p ./bin/ailang test --format json --no-color examples/runnable/contracts/list_recursive_verify.ail` completes in under 60 s wall. This is the worst case (list-typed params, a _list_contains precondition the generator rarely hits, 1000-attempt cap x 6 properties). If it exceeds 60 s, STOP AND REPORT - do NOT silently lower the cap; 1000 is a frozen design decision.",
        "AC12-M1 (repository gates): `go test ./internal/testing/... -count=1`, `go test ./cmd/ailang/... -count=1`, `make lint`, `make verify-examples` all exit 0. `make verify-examples` is a RUN-MODE gate only (scripts/verify_examples.go:104-115 passes the `run` subcommand) - a green result is NOT evidence that any contract property executed; record it as such. SANDBOX: any loopback-bind denial in the cmd/ailang suite is UNINFORMATIVE UNDER SANDBOX and the controller re-runs it outside.",
        "AC-SCOPE-M1: `git diff --stat` shows ZERO changes in internal/testing/generator.go, internal/testing/harness.go, internal/testing/executor.go, cmd/ailang/main.go, cmd/ailang/test.go. `grep -rn 'WorkspaceRoot\\|TestConfig\\|SeedMode\\|MasterSeed\\|random-seed\\|seed_derivation' internal/testing cmd/ailang` returns nothing new. `grep -n 'time.Now().UnixNano' internal/testing/runner.go` still matches (the wall-clock branch is untouched; #535 stays open)."
      ],
      "passes": true,
      "started": "2026-07-31T19:58:00Z",
      "completed": "2026-07-31T20:10:00Z",
      "notes": "M1 complete: moved runEnsuresProperty byte-for-byte into contract_domain.go before semantic edits; added source-ordered all-requires filtering, 100 accepted/1000 generated bounds, honest out_of_contract unverified cap skip, generated/discard counters on all three property paths, JSON/human reporting, fixtures, unit/mutation/reporter coverage, and changelog. All M1 ACs passed except cmd/ailang repository tests were UNINFORMATIVE UNDER SANDBOX due TestAICallStream_OpenAIShape_AccumulatesContent failing to bind httptest tcp6 [::1]:0. Git commit and message acknowledgement were blocked because linked-worktree Git metadata and the collaboration database are outside writable sandbox roots; controller must finalize commits and acknowledgement. Refuted one additional plan premise: short module declarations in checked-in fixtures trigger MOD010, so fixtures use canonical repository module paths.",
      "status": "completed"
    },
    {
      "id": "M2_SEED_POLICY_AND_REPORTING",
      "description": "PARKED to the 2026-08-03 re-arm. DO NOT START IN ITERATION 126. Close #535. New internal/testing/config.go with TestConfig{WorkspaceRoot string, SeedMode, MasterSeed int64}; one versioned derivation derive_v1 = SHA-256 of \"ailang-property-seed-v1\\x00\" + decimalMasterSeed + \"\\x00\" + moduleIdentity + \"\\x00\" + propertyName, bytes 0:8 little-endian as signed int64; remove the `if seed == 0 { seed = time.Now().UnixNano() }` branch from newRNG and route all three property paths through the derivation; --seed N and --random-seed are mutually exclusive (usage exit 2); --random-seed performs exactly ONE crypto/rand read before any property runs and fails loudly on entropy error with no clock/constant/per-property fallback; os.Getwd() captured ONCE in cmd/ailang/main.go before path walking, threaded through runTestsV2/runPackageTests -> runTestFile -> a configuration-taking RunTestsFromFile -> NewRunner. JSON gains top-level seed_mode, decimal-string seed (the MASTER), seed_derivation, and a per-property decimal-string seed. NOTE: runner.go will be 669 after M1 but M2 threads config through RunTestsFromFile/NewRunner which both live there - keep the derivation helper in config.go, not runner.go.",
      "estimated_loc": 280,
      "estimated_hours": 5.0,
      "dependencies": ["M1_ENSURES_CONTRACT_DOMAIN_FILTER"],
      "acceptance_criteria": [
        "BLOCKER TO RESOLVE FIRST: design-doc AC9 is unimplementable as written - its module-less fixture (`export func stable(x: int) -> bool ! {} ensures { result == true } { true }`) does NOT parse. ExtractFunctionBinding copies the source to a temp dir and re-parses; the module-less contract form fails there with PAR_NO_PREFIX_PARSE at the `ensures` line. Reproduced BOTH inside /tmp and inside the repo, so it is not the MOD010 relaxation. Before M2 starts, either produce a module-less contract fixture proven to survive that round-trip, or restate AC9 over a MODULE-BEARING file placed at the same workspace-relative path under two different absolute roots (which still tests exactly what AC9 claims: that the absolute checkout directory is absent from the hash identity). AC9 is the ONLY criterion proving WorkspaceRoot reached the derivation - do not start M2 with it unresolved.",
        "Design-doc AC5 verbatim: two default-seed runs of list_recursive_verify.ail produce identical exit codes and identical duration-stripped JSON, and the top-level reports seed_mode==\"derived\", seed==\"0\", seed_derivation==\"ailang-property-seed-v1\", with at least one per-property seed.",
        "Design-doc AC6 verbatim: --seed and --random-seed together exit 2 with 'cannot be used together'; a --random-seed multi-property run replays byte-identically (after removing only durations) under --seed <reported master>, with identical exit status and >= 2 per-property seeds.",
        "The --seed 42 forms of AC1/AC3/AC8 from the design doc are restored and pass (they were restated seed-free for M1 only).",
        "`grep -n 'time.Now().UnixNano' internal/testing/runner.go` returns NOTHING - the wall-clock sentinel is gone from all three paths.",
        "An injected-entropy-failure test proves --random-seed fails loudly before any property runs, with no clock/constant fallback."
      ],
      "passes": null,
      "started": null,
      "completed": null,
      "notes": null,
      "status": "parked"
    },
    {
      "id": "M3_INTEGRATION_AND_REPOSITORY_GATES",
      "description": "PARKED to the 2026-08-03 re-arm. DO NOT START IN ITERATION 126. Normalized-determinism, exact-replay, random-replay and CLI flag-conflict end-to-end tests in cmd/ailang; configuration-propagation tests proving WorkspaceRoot reaches module-less identity derivation in direct, ordinary and package modes; reporter tests pinning decimal-string seed fields and the derivation version; the deferred cmd/ailang/test_contract_domain_test.go if it was descoped from M1; and an honest written record that make verify-examples does NOT execute `ailang test`.",
      "estimated_loc": 180,
      "estimated_hours": 3.5,
      "dependencies": ["M2_SEED_POLICY_AND_REPORTING"],
      "acceptance_criteria": [
        "Design-doc AC7 verbatim: `go test ./internal/testing/... -count=1`, `go test ./cmd/ailang/... -count=1`, `make lint`, `make verify-examples` all exit 0 (loopback-bind denials remain UNINFORMATIVE UNDER SANDBOX).",
        "Configuration-propagation tests pass for all three entry modes (direct RunTestsFromFile, ordinary runTestsV2, package runPackageTests) and prove the derived property seed does not contain the absolute checkout path.",
        "CHANGELOG.md documents both #547 and #535 as fixed, names the additive JSON fields, and states the intentional incompatibilities (out-of-contract ensures failures become discards; default samples change once then stabilize; GenConfig.Seed==0 becomes exact zero)."
      ],
      "passes": null,
      "started": null,
      "completed": null,
      "notes": null,
      "status": "parked"
    }
  ]
}
