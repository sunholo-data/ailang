./scripts/test_mission_pi_run.sh:2:# test_mission_pi_run.sh — prove each verdict of scripts/mission_pi_run.sh actually fires.
./scripts/test_mission_pi_run.sh:11:SUT="$HERE/mission_pi_run.sh"
./scripts/test_mission_pi_run.sh:47:echo "TEST 2: pi succeeds but changes nothing -> empty_worktree (rc 10)"
./scripts/test_mission_pi_run.sh:51:check "empty worktree" 10 $? empty_worktree "$TMP/o2.ndjson.verdict.json"
./scripts/test_mission_pi_run.sh:88:# guard — it reports empty_worktree for good work, and would report ok for none.
./scripts/mission_pi_run.sh:2:# mission_pi_run.sh — run the pi executor lane under guards that can actually see
./scripts/mission_pi_run.sh:52:#   10 empty_worktree   — pi finished, changed nothing. The false-green in its pure form.
./scripts/mission_pi_run.sh:74:usage: mission_pi_run.sh --model M --directive FILE --workdir DIR --out NDJSON
./scripts/mission_pi_run.sh:101:    *) echo "mission_pi_run.sh: unknown argument '$1'" >&2; usage; exit 14 ;;
./scripts/mission_pi_run.sh:106:  echo "mission_pi_run.sh: --model, --directive, --workdir and --out are all required" >&2
./scripts/mission_pi_run.sh:109:[ -f "$DIRECTIVE" ] || { echo "mission_pi_run.sh: directive file not found: $DIRECTIVE" >&2; exit 14; }
./scripts/mission_pi_run.sh:110:[ -d "$WORKDIR" ]   || { echo "mission_pi_run.sh: workdir not found: $WORKDIR" >&2; exit 14; }
./scripts/mission_pi_run.sh:150:  # the model does real work somewhere else and the verdict reads empty_worktree — or,
./scripts/mission_pi_run.sh:243:    else VERDICT_NAME="empty_worktree"; RC=10; fi ;;
./scripts/mission_pi_run.sh:257:  "worktree_changed_files": ${DIFF_LINES:-0},
./changelogs/v0.32-current.md:2039:The mission pi lane now runs through `scripts/mission_pi_run.sh`, which returns a typed
./changelogs/v0.32-current.md:2040:verdict (`ok` / `empty_worktree` / `reasoning_stall` / `stream_dead` / `wall_timeout`)
./design_docs/v1-mission-status-archive.md:11:## STATUS 2026-09-03 — ITERATION 324: **The loop's own spawn path now has a hard gate: `resolve-role-spawn.sh` + a PreToolUse hook that denies an Agent-tool alias spawn of any `provider:model`-pinned role — M1+M2 of `m-spawn-pin-enforcement` LANDED, inert until M3 exports the marker.** Gate 0/1: kill switch armed; billing tripwire CLEAN; gh `sunholo-voight-kampff`; codex lane DOWN this fire (probe rc=1, `404 … chatgpt.com/backend-api/codex/responses`) so the driver handed planner→`pi:ollama/kimi-k3:cloud` and executor→`pi:ollama/deepseek-v4-flash:0731-cloud`, and opus probes timed out twice so the controller is `claude:claude-fable-5-1`; the 13:51 fire died on an `API Error: 500` before any gate (a reaped slot, not an iteration). Canonical inbox (`store: gcp`), **19 unread, complete enumeration (`--limit 200`)**: 3 eval-suite (one 0/23 in 0.02 s = a harness/lane failure, not a regression), 6 task completions, 10 dated 08-31 already triaged by 315–319; none directive-class. **0 directives** on #972 (24 comments; watermark `2026-09-02T07:17:34Z`). **D-53 RESOLVED by an ATTENDED ruling** — commit `84d74f1b2` authored by Mark, not the fleet account (provenance compared, not recorded), Goal block now **N = 12**; acknowledged here as the channel working. Ledger valid, 54 rows, **0 OPEN**. No rotation owed, no weekly sweep owed. Running skill byte-identical to origin at Gate 1 (RESOLVED `readlink -f` target; then #1035's own lossless skill move landed, so main is 1 doc-commit behind by design). **Died-mid-flight sweep found iteration 323's ENTIRE record — PR #1035, 21/21 green, MERGEABLE — unmerged, and no report on #972**: verified its claims first-party (3 stamps, archive gained 318 with 317 firing, entries for 321&322 and 323 present, skill edit a lossless move to `resources/ci-health.md`) and merged it as `08ab6ba7c`. That is the FOURTH slot in eight to die holding finished work. dev HEAD checks=16, one NOT-GREEN = inherited SonarCloud. **Pick = queue head `m-spawn-pin-enforcement`** (Mark-approved design, no plan). Quorum at pick: **round 1 BLOCKED 3/3** (hook→env premise unverified; new script vs parameterising `derive-planner-lane.sh`; stale SKILL line cites). Rule 3f: I measured every premise myself before routing the revision — a live PreToolUse spike under `bypassPermissions` (env inherited incl. a `SPIKE_MARKER` control; Agent call denied, model quoted the reason and did not retry), grep counts 0/0/0/**5** on the resolver script, anchors re-cited by text. Designer = rotation next entry **`pi:ollama/deepseek-v4-flash:0731-cloud`** (61 s, one file); I restored four round-1 test arms it dropped. **Round 2 BLOCKED 3/3 but localised on ONE surface** (evasion by omitting the skill name; one multi-hook premise), every fix reviewer-authored → **narrow-refinement carve-out applied** (ratified iter-95): two more measurements (two overlapping hooks BOTH fire and the deny wins, on a second alias; the real docs-9/10 prompts are NOT on disk — 0/0/0 in three logs, 0 in both PR bodies — and prose matching false-positived), then `MISSION_CONTROL_ACTIVE=1` marker + explicit `MISSION-ROLE:` token as the only mapping input + `subagent_type: Explore` as the machine-readable read-only path. Planner **opus** (`derive-planner-lane.sh` → `opus fail-closed:path-not-in-codex-allowlist`, verbatim): 4 milestones, frozen contracts, and three corrections to the design (Layer-3 exports must sit AFTER the per-fire lane rewrites at driver lines 722/770/779, not beside the exports; line 904 is the one-shot override; a sibling test suite is NOT auto-picked-up by `make test-launchd-drivers`). Executor **pi deepseek**, two bounded runs (488 s, 712 s), no git writes, snapshots `shasum`-faithful; every gate re-run by me outside the lane; **end-to-end through the repo's own `.claude/settings.json`**: docs-10 replay DENIED naming the pin, Explore allowed, unlabelled spawn denied. **Judge sonnet in its own worktree: round 1 FAIL 66/100** — it upgraded my own open question to BLOCKING: the passthrough branch's explicit `allow` would bypass the permission flow for **100 % of Agent/Task calls in every attended session** loading this repo's settings; plus F2 (unparsable payload → allow), arm L checking 2 of 7 log fields, and a plan table row wrong (deleting the Explore exception kills 7a AND L). Fixed all four (passthrough now prints NOTHING; payload gate denies; L asserts all 7 fields; new arms NS/8p/8e), drilled each as a sole killer; **round 2 PASS 92/100, zero blocking**, its one probe (bare `null` payload untested) became arm 8n. Routing suite 36→45, hook suite 17, `launchd drivers (bash 3.2)` green. Metered **$0.10** (two quorum rounds); pi lanes flat-rate. **Friction, one instance**: `scripts/mission_pi_run.sh` runs pi WITHOUT the `-e` sandbox/fence extensions the pi recipe mandates (measured: `~/.pi/extensions/` holds only `sandbox.json`); post-hoc main-checkout check held (7 dirty before and after, none mine). Progress: **N = 12, unmoved** (HARNESS). → PR [#1038](https://github.com/sunholo-data/ailang/pull/1038) → [`70e453060`](https://github.com/sunholo-data/ailang/commit/70e453060), Gate 3b `test`/CI success SHA-addressed, SonarCloud inherited.
./design_docs/planned/v0_35_0/m-pi-harness-upgrade.md:89:| Wire size, `message_update` | avg **1043 B**, max 1222 B | avg **274 B**, max 365 B | — | on a 20-token reply. The 0.73.1 form grows with the message; this is the quadratic replay that `scripts/mission_pi_run.sh` was written to filter around (its header records the ~28 GB implied budget and the size ceiling that followed) |
./design_docs/planned/v0_35_0/m-pi-harness-upgrade.md:158:| `scripts/mission_pi_run.sh` | **substring** match on `"type":"message_update"`; reads no sub-field of it. Greps `"type":"agent_end"` and `"type":"tool_execution_end"` | yes — but not the way it looks: the filter keeps working, the **snapshot** it feeds silently loses its value (V29) |
./design_docs/planned/v0_35_0/m-pi-harness-upgrade.md:183:6. **`scripts/mission_pi_run.sh`'s forensic snapshot** (V29): the script keeps the newest `message_update` in a snapshot file so "a run killed mid-turn still has full forensics." At 0.73.1 that record was the full cumulative message; at 0.84.4 it is a ~274-byte delta, so the guarantee quietly becomes false. Either keep a rolling window of the last N deltas, or correct the comment — but do not leave a stated guarantee that the wire no longer supports. Its `message_update` **filter** and its `agent_end`/`tool_execution_end` greps are unaffected (substring matches on fields that survive).
./design_docs/planned/v0_35_0/m-pi-harness-upgrade.md:225:- `scripts/mission_pi_run.sh` — snapshot window or corrected guarantee (V29); note the filter's upstream cause is fixed (~+15/−4)
./design_docs/planned/v0_35_0/m-pi-harness-upgrade.md:275:   - `scripts/test_mission_pi_run.sh` → unchanged (its assertions are substring greps on `agent_end` / `tool_execution_end`, both of which survive — V29)
./design_docs/planned/v0_35_0/m-pi-harness-upgrade.md:280:   - `mission_pi_run.sh`'s snapshot stops being a full-message forensic record (V29). This is upstream's change, not ours; the deliverable is that the script stops *claiming* otherwise.
./design_docs/planned/v0_35_0/m-pi-harness-upgrade.md:387:| V22 | Four in-repo consumers of the stream | repo-wide grep for `assistantMessageEvent|tool_execution_end|message_end` | `pi.go`, `docker/resident/lib/pi.mjs`, `scripts/mission_pi_run.sh` (+ its test), `ail-fmt-autolint.ts` (API, not wire) |
./design_docs/planned/v0_35_0/m-pi-harness-upgrade.md:394:| V29 | What `mission_pi_run.sh` actually does with `message_update` — and the guarantee that breaks | read [scripts/mission_pi_run.sh:31-38, 155-163, 233-240](../../../scripts/mission_pi_run.sh) | It **substring-matches** `"type":"message_update"` in awk and routes those lines to a snapshot file; it parses **no sub-field**, so the REMOVED `message`/`partial` cannot break it. Its `agent_end` and `tool_execution_end` greps also survive. **But** its stated purpose — "a run killed mid-turn still has full forensics" — depended on that snapshot being the full cumulative message (avg 1043 B at 0.73.1); at 0.84.4 it is a lone delta (avg 274 B, V7). The filter survives; the guarantee does not |
./design_docs/planned/v0_35_0/m-pi-harness-upgrade.md:420:| oc-glm-5-2 | `pi.mjs` "already verified (D10)" was a cross-reference, not evidence; `mission_pi_run.sh` was hand-waved despite two REMOVED fields | **V28** — ran the resident module itself against 0.84.4. **V29** — read the script: it substring-matches and parses no sub-field, so it survives; but its *forensic-snapshot guarantee* does not, which is now a work item (M2.6) |
./design_docs/v1-mission.md:217:## STATUS 2026-09-06 — ITERATION 334: **A designer, a quorum, a planner and a judge all ran — and the two most valuable things any of them produced were a refusal and a negative result.** Gate 0/1: kill switch armed (`mission-control.disabled`, V1's correct literal); billing tripwire **CLEAN**; gh `sunholo-voight-kampff`; running skill **byte-identical to origin** — `cmp` rc=0 against the **RESOLVED** `readlink -f` target (main-checkout inode `67997727` vs the pin's own `70307109`, a DIFFERENT file). Main checkout `dev` **0 ahead / 16 behind** with 14 dirty files from a concurrent attended session (`m-zai-window-routing`, model registry); Principle 0 untouched, and its `SKILL.md` is modified-vs-its-own-stale-HEAD yet byte-identical to origin, which is why the `cmp` is green. Canonical cloud inbox (`store: gcp`, `ailang-multivac`), **12 unread triaged by sender**: this loop's OWN `D-55` approvals row (left UNACKED — `D-55` is still OPEN, so acking it would retire a live ask) and 11 rows addressed to `pkg:*`/`mcp-public`/`aitana-platform` inboxes for other repos. None directive-class. **0 directives** on #972 (36 comments; watermark `2026-09-03T19:35:40Z`, unmoved). Ledger valid, **55 rows, 1 OPEN (`D-55`, unchanged)**. No rotation owed (#972 created Monday `05:56:11Z` = 07:56 CEST, after the 07:00-local boundary; 36 < 80); no weekly sweep owed. dev HEAD `e50066037` was **mid-flight at Gate 1** (checks=13, 8 pending, **zero failures**) — not red, runs EXIST so no dropped-event class — and settled **GREEN at 16 checks, 0 not-green** before Gate 4. Died-mid-flight sweep: no matching worktree in V1's own clone; the 3 open PRs on the shared FLEET account (`#1041`/`#1033`/`#945`) were all previously attributed and none matches this pick. **PICK: the queue head**, `m-cache-sanitize-module-id-windows-colon` (iter-333) — and the iteration's first routing call was to **MERGE it with `m-cache-sanitize-module-id-collision`** (iter-328) into one design, because they are two symptoms of one function: `sanitizeModuleID` (`cache_store.go:161`) maps only `/` and `\` to `__`, so a Windows drive-letter ID yields a component containing a **colon** and the artifact cache publishes NOTHING on that platform, while everywhere else `a/b` and `a__b` collide onto one directory. Both confirmed first-party at HEAD before any routing; sole production call site `moduleArtifactDir` (`cache_artifacts.go:403`), control firing. **OUTCOME: LANDED — a design doc and a sprint plan, both judged.** `design_docs/planned/v0_36_0/m-cache-module-id-encoding{,-sprint-plan}.md`, PR [#1060](https://github.com/sunholo-data/ailang/pull/1060) from three commits: `8cd3bc783` (design), `5ef1058ef` (plan), `6ebc71a54` (judge findings). Chosen encoding `m-<slug>-<16hex>`: the `m-` prefix is the Windows reserved-device-name guard, the 16-hex SHA-256 prefix over the FULL module ID is the uniqueness authority, the slug is a truncated readability aid carrying no correctness weight, max component 57 chars independent of input length. **THE QUORUM EARNED ITS MONEY TWICE, AND THE SECOND TIME WAS A REFUSAL.** Round 1: `gpt6-astra` **ABSENT on budget** — pre-flight refusal, `estimated cost $0.1170 … exceeds cap $0.1000`, zero spend — so it was re-run ALONE at a raised cap exactly as the absent-reviewer rule requires, and **it was the reviewer whose objection mattered**: the doc's legality argument ("the output always ends in `-<hex>` so it is never a reserved device name") is **FALSE**, because Windows reserves the basename *before the first dot*, so `con.txt-<hex>` still has basename `con`. Had the quorum been read as a bare 2-reviewer block, that hole would have shipped. `gemini-3-1-pro` and `oc-glm-5-2` both rejected on the SAME unverified premise — `pipeline_module.go` "treats non-verified as a recompile" and `maxModuleArtifactBytes ~32 MiB`, neither in the Verification Log — and rule 3f says the controller MEASURES an objection rather than forwarding it, so both were measured and **both held**: the only early exit is the `continue` inside `verified && cached != nil`, so a `LoadArtifacts` error falls through to the ordinary compile path with no error, panic or silent fallback; `maxModuleArtifactBytes` is exactly `32 << 20` at `cache_artifacts.go:29`, test-pinned at `cache_artifacts_test.go:193`, control `cacheKeyVersion` = 14 hits. Round 2, all three PRESENT (`absent_reviewers` empty), **3/3 reject on three DIFFERENT surfaces**, every one carrying a concrete reviewer-authored `proposed_fix` and none disputing the direction — the Gate-2 narrow-refinement carve-out exactly, so the reviewers' own fixes were applied rather than parking. gemini's premise was measured and **REFUTED in the doc's favour** (`Clear()` does reset the manifest and `Save()` it before `removeAll`). **`oc-glm-5-2`'s objection was the one that found a defect nobody had**: it noticed the Conflict Surface conflated *"no other package"* with *"no other call site"*, and measuring that turned up `cmd/ailang/serve_api_mcp_surface_test.go:601` — `compileArtifactDir`, a **named helper in another package that reimplements the old mapping** and is invisible to every `grep sanitizeModuleID` the doc ran. **THE DESIGNER ROTATION FELL THROUGH, AND THE RUNNER'S OWN LOAD-BEARING ASSERTION IS WHAT HID IT.** Rotation pointer read `codex:gpt-6-astra`, so the next entry was `pi:ollama/deepseek-v4-flash:0731-cloud`; it authored the doc well (`ok`, 214s, 34 tool executions, a real Verification Log with a firing negative control). Its REVISION pass then returned verdict **`ok` with `worktree_changed_files: 1` and wrote NOTHING** — 9 tool executions, all reads of a file whose contents the directive already quoted verbatim, 4 `agent_start`/`agent_end` with 3 `auto_retry_start`. The doc's mtime `04:42:02` predated the run. **`mission_pi_run.sh`'s worktree-diff assertion — the one assertion its own header calls load-bearing because `stopReason` is evadable — is VACUOUS on a revision**, because the file was ALREADY untracked from round 1, so `git status --porcelain` counts the PREVIOUS run's output as this run's proof. Filed as a queue row; the fix is a content hash taken before and after, which is what caught it here. Fell to the NEXT rotation entry, `claude:claude-fable-5-1` via `claude-sub` (probe rc=0, subscription-only by construction), which applied astra's fix verbatim, dropped `.` from the slug alphabet as a second independent guard, and recomputed four new hash suffixes with a real `shasum`. **Fable spend: ONE bounded revision run — the diet's unit is one DOC (authoring + at most one protocol-mandated revision) and the authoring half ran on pi, so this is inside the ceiling.** **THE PLANNER'S BEST OUTPUT IS A NEGATIVE RESULT.** `codex:gpt-5.6-sol` in an ephemeral detached worktree at the design commit, 4 milestones / 1.35 days. Applying rule 3o it flags **M3 and M4 as having NO non-vacuous mutation of their own diff** — M3 ships a legality table plus a skip deletion, M4 a regression guard over `Clear()` behaviour that already exists at base, and both production hunks land in M1/M2. It said so plainly instead of naming a green test. It also extended the Conflict Surface by THREE more sites, all verified first-party before the commit: `cache_invalidation_test.go:313,328` and `cache_artifacts_test.go:363` hard-code `filepath.Join(…, "compile", "modules", "answer", …)`. Its in-sandbox `cmd/ailang` panic was an `httptest [::1]:0` bind denial, correctly labelled **UNINFORMATIVE UNDER SANDBOX**; the controller's own out-of-sandbox baseline at the design commit is the independent confirmation (`ok internal/pipeline 5.300s`, `ok cmd/ailang 29.801s`, rc=0). **THE JUDGE: `sonnet`, its OWN worktree, PASS 85/100, ZERO blocking, three non-blocking — and finding (1) is mine.** Round 2 made me WITHDRAW the 32 MiB orphan-footprint guarantee from the Migration prose and I left the Verification Log row asserting it standing, still bolded *"Orphan disk bound is a real constant"* — the doc withdrew a claim in one section and cited it as settled fact in another. That is the same shape as the defect the reviewer was complaining about: I applied the fix where the reviewer pointed and never swept for the claim's echo. (2) The doc and the plan used *"not vacuous"* to mean two different things for the SAME test/mutation pair and neither flagged it; the plan's stricter reading is right and the doc now concedes it. (3) The plan required M3 to "show explicit PASS events" on Windows without instructing anyone to wire it — the no-silent-skip gate is a hand-maintained literal list at `ci.yml:111` (unix, 5 names) and `:480` (windows, 4), already asymmetric, so M3 now carries an explicit work item to edit both. All three reproduced first-party before being acted on. The judge also re-derived **all eleven** worked-example SHA-256 prefixes with two independent instruments (every one matched — the controller had already done the same, independently), implemented the specified encoder as a scratch Go program and **failed to break** its legality, injectivity, 57-char bound, slug-truncation interaction or case-fold safety, and independently confirmed the M3/M4 vacuity verdict by reading the pre-existing `TestCacheStore_ClearArtifacts`. Its first run was killed mid-flight by a transient API/DNS drop at 48 tool calls; it was **resumed by name** rather than re-run, per standing rule 7's sub-agent amendment, and the resumed run produced the whole report. **THE EXECUTOR IS THE ONE ROLE OF FOUR NOT SPAWNED, and that is a deliberate routing call, stated rather than buried:** the plan came into existence in this same iteration and was materially amended by its judge minutes before Gate 3b, so M1 executes next iteration against a plan that has been reviewed and has settled. Resume predicate: *"execute M1"*. **Verification the controller did not delegate:** every load-bearing designer claim re-measured before routing; all eleven hashes re-derived; the planner's three new call sites read at their cited lines; base gate green out-of-sandbox; the eight CI gates a docs-only diff can break DERIVED from `ci.yml` rather than remembered (`check-autoclose`, `check-context-docs`, `check-file-sizes`, `check-referenced-paths`, `check-changelog`, `check-no-personal-email`, `check-tmpfile-hygiene`, `check-skills`) — all rc=0. Autoclose scan over all three commit messages and the PR title/body: **0** hits, known-bad control matching **1**. **Routing.** Resolver run for all four roles, output verbatim. **Planner resolver disagreement reproduces a 7th time** (`agent-tool opus fail-closed:planner-lane-field-missing`, for the picked doc) — routed straight to the `codex:gpt-5.6-sol` pin without burning a spawn on a guaranteed hook denial, as the amendment requires; fix still belongs in the TOOL. `metered=$0.28` of the $5 ceiling (quorum R1 `$0.03115` + astra solo re-run `$0.08279` + quorum R2 `$0.16619` = `$0.28014`); pi and fable are flat-rate/subscription and cost $0. No GPU, no `rig.lock`. **`D-55` REMAINS OPEN by design** — the loop may not resolve its own row, and the sprint it gated is complete. **Progress: N = 12 design docs before v1.0.0 (was 12, ±0) — goal unmoved.** A doc leaves the count when it LANDS; this one is designed and planned, zero milestones executed, and it ABSORBS two existing queue rows rather than adding a thirteenth. [PRODUCT]
./design_docs/v1-mission.md:512:**[NEXT] [iter-334, first-party; the runner's OWN load-bearing assertion, and it failed silently in the direction it exists to prevent] `m-pi-runner-worktree-assertion-vacuous-on-revision` — `mission_pi_run.sh` reports verdict `ok` for a run that wrote nothing, whenever the target file was already dirty or untracked before the run.**
./design_docs/v1-mission.md:513:`scripts/mission_pi_run.sh`'s header states the case plainly: `stopReason` is evadable in both directions and fired on **0 of 4** real failures, so *"the load-bearing assertion is the worktree diff"*. That assertion is `worktree_changed_files`, derived from `git status --porcelain` — a snapshot of the tree's state, **not a measurement of what THIS run did**. On a first authoring run the two coincide. On a REVISION of a file the previous run created, they do not: the file is still untracked, the count is still ≥1, and a run that read nine files and wrote zero bytes returns `{"verdict":"ok","worktree_changed_files":1}`. Measured first-party at iteration 334: the deepseek revision pass returned `ok` at 234s with 9 tool executions, 4 `agent_start`/`agent_end` and 3 `auto_retry_start`, and the design doc's mtime (`04:42:02`, the controller's own earlier `sed`) **predated the run**. Caught only because the controller had taken a `shasum` before the next lane's run for an unrelated reason. This is the stale-artifact class (standing rule 7's amendment) aimed at the runner that exists to detect exactly this. **Fix:** hash the worktree's tracked+untracked content before and after and compare, or capture `git status --porcelain` as a BASELINE at launch and diff the two snapshots, so the verdict reports the delta rather than the level. Cheap, and it makes `empty_worktree` (rc 10) mean what its name says. **Resume predicate:** re-read the verdict JSON's fields against a deliberately no-op run on a dirty tree — today it returns `ok`.
./design_docs/v1-mission.md:521:**[NEXT] [iter-336; first recorded session-protocol instance, no skill edit spent] `m-pi-evaluator-session-handshake`** — the independent pi evaluator was told to reuse controller inbox triage, but the loaded session protocol requires a CLAUDE read and an `ailang messages` command in the SAME session before `session_protocol_ack`. Repeated denied writes produced no report (446s,226tools,rc10 empty_worktree after verified-process termination). Same MiniMax model succeeded after an explicit bounded inbox list and local acknowledgement; no guard removed. Record this handshake in the evaluator recipe if the same friction recurs; supplied controller triage does not satisfy the per-session guard. Raw receipt and successful invocation metadata: `docs/sprint-retros/iter336-cache-module-id-encoding-transport.json`.
./design_docs/motoko-mission-status-archive.md:7:## STATUS 2026-08-28 — ITERATION 27 COMPLETE: **THE GUARD WAS NOT MISSING, IT WAS INEXPRESSIBLE — A VALUE TYPE MADE "ABSENT" AND "ZERO" THE SAME VALUE.** Pick was the **queue head**, row **6h** — the first iteration since 23 to reach it, because 24, 25 and 26 were each preempted by a loop-health regression. Re-verified at a fresh `origin/dev` rather than inherited: `Usage` is still a value type at `internal/ai/openai/step.go:300`, `#842` still **OPEN**, no merged PR or direct commit touches it. PR [#946](https://github.com/sunholo-data/ailang/pull/946) → `75e4e12be`, evaluator **round 1 PASS 94/100, ZERO blocking**. Gate 3b GREEN on head `75e4e12be` (**21** checks, **0** pending, **0** not-green, **4/4** required — build/docs-gate/lint/test — `mergeable` read FIRST per the iteration-198 rule: `MERGEABLE`/`CLEAN`), merged as [`0e3a96585`](https://github.com/sunholo-data/ailang/commit/0e3a96585). **THE FIX IS ONE LINE OF TYPE DECLARATION AND THAT IS THE WHOLE FINDING.** A provider failure arrives as a *successful empty completion* (`finish_reason:"stop"`, `"content":null`, no `usage` key); the guard `#842` asked for could not be written because `Usage` was a **value**, so an omitted key unmarshals to the zero struct and `absent.Usage == allZero.Usage` is **true** — no expression over that type separates them. Now `*ChatStepUsage`. Step 2 (policy) stays deferred **by design**: Anthropic and Gemini do not share this parser and streaming usage delivery is opt-in on some providers, so a uniform guard turns a legitimate empty completion into an error — the same defect pointed the other way. Standing negative control for any future guard: the legitimate tool call, which also carries `content:null` and *does* report usage. **BLAST RADIUS MEASURED, NOT ASSUMED, AND WIDER THAN FILED:** `ChatStepResponse` is referenced only in `step.go` (:295 decl, :561 sole use) and by **zero** test files; `raw.Usage.*` reads at exactly five sites; controls `ChatStepUsage` **4** / invented symbol **0**. `internal/ai/ollama/step.go:345` builds an `openai` client against `<endpoint>/v1`, so **every ollama tool-calling turn parses here** — the defect sat on our own eval rig, where a masked provider failure is indistinguishable from a local model declining to act. **MUTATION DRILL ANCHORED TO THE DIFF HUNK BY HUNK (rule 3n), EACH MUTANT LANDED (sha256) + BUILDS + EFFECT-VERIFIED AGAINST THE GOFMT-PARSED FORM (V1 iter-274's rule) + RESTORED FROM A `cp` BACKUP BYTE-IDENTICAL:** **M1** break the `usage` tag → **2** tests red (new arms + pre-existing `TestStep_TextOnly_HappyPath`), kill-set member; **M2** nil no longer yields zero tokens → **SOLE KILLER**, and because its blast radius is one test the inverse arm applies and was run — suite `-skip`-ing my own test, under the mutant, **rc=0**; **M3** neuter the deref guard → reds **only** the pre-existing test, so the hunk is pinned but **not by anything this branch wrote**; **M4** revert to the value type → **DOES NOT BUILD** (`invalid operation: raw.Usage != nil (mismatched types ChatStepUsage and untyped nil)`) — the "mutant does not build" class, recorded as a **compile-time arm** and never as a behavioural kill, because here it *is* the row's finding restated by the compiler. **THE JUDGE FOUND THE GAP MY DRILL MISSED, AND THAT IS THE ITERATION'S BEST RESULT.** It reproduced every controller claim including M2's inverse arm, then built a **16-case differential harness** across parent `d5305fa79` — `usage:null`, `usage:[]`, `usage:"none"`, `usage:42`, negative tokens, malformed JSON — and found output **byte-identical in all 16**, covering the two cases I had named as untested. Its finding #1, reproduced first-party before being believed: `cacheRead = usage.PromptTokensDetails.CachedTokens` has **zero killers** — mutated to `+ 999` it lands, builds, effect-verifies 0→1, and leaves openai + openrouter + ollama at **rc=0 with 0 `--- FAIL` lines**. The mirage is rule 3i's shape: `cache_usage_test.go` has **0** references to `ParseChatStepResponse` and **7** to `Generate`. Pre-existing (only the receiver was renamed), so **filed as row 6m**, not used to widen the PR — the disposition iteration 24 gave row 6i. **GATES RE-RUN OUTSIDE THE SANDBOX, WITH A FRESH BINARY, AND BOTH HALVES MATTERED.** 15 test files across the three `internal/ai` packages bind loopback sockets, which `workspace-write` denies — so the directive carried three in-sandbox-satisfiable gates and named the suites `UNINFORMATIVE UNDER SANDBOX` (V1 iter-270's rule, which iteration 24 paid for first-party), and the controller ran all of them outside: `go build ./internal/ai/...` **rc=0**, `go vet ./internal/ai/...` **rc=0**, `go test` openai/openrouter/ollama **rc=0/0/0**, `gofmt -l` empty, `check-file-sizes` **rc=0**, `check-changelog` **rc=0**, `test-check-changelog` **rc=0** (13 arms); judge additionally `golangci-lint` **0 issues**. Binary built to a scratch dir with version ldflags and prepended to PATH rather than `make quick-install` (shared `~/go/bin`): `v0.34.0-118-gd5305fa79-dirty` == `git describe`, against the system copy's `v0.34.0-75-gfb6084f4b-dirty` — **43 commits adrift**. **THE PIN STILL FAILS AND THIS FIRE IS AGAIN THE INSTANCE — BUT THE PREDICATE IS NOW SHARPER THAN "HOW FAR BEHIND".** `DRIVER PIN FAILED` at **02:03:09**, executing the clone at `e3ed9467f`, **205** behind (172 → 205 in one day). Iteration 26 wrote that *"V1 escaped because its clone happens to be current"*; measured this iteration, that is not the predicate. `git merge-base --is-ancestor ff0da7445 HEAD` → **NO** for motoko, **YES** for V1 — and **V1 is still 18 commits behind** while pinning cleanly, its last `DRIVER PIN FAILED` dated 2026-08-27 07:10 with clean fires since. So V1 **recovered** by crossing the fix, which motoko structurally cannot, because the tree that must read the fix is the stale one. Non-comment `hasTrustDialogAccepted` occurrences: motoko **0** (its single hit is the line-185 comment iteration 26 identified), V1 **3**; invented-token negative control **0** in both. Fleet third arm (rule 3l): world **0** pin failures, ever. **`D-MOTOKO-WORKDIR-2` STAYS OPEN — SIXTH ASK**, and the four non-destructiveness obligations are measured again, not assumed: **0** ahead, **0** dirty in the clone and **0** across all nine worktrees, nothing to back up. Also recorded: the driver's own lane-degradation notice **failed to send** (`WARNING: driver-pin notice FAILED to send via ailang messages`), i.e. queue row 2's human channel was silent for exactly the event it exists to report. **DEV CI: ONE NOT-GREEN, INHERITED AND NOT OURS.** `checks=16` (control fires), `SonarCloud Code Analysis` = `failure`; walked commit by commit — green at `7dff0942d`, first red at `caea1f9e1` (V1's M-EVAL-ROLLING-ELO merge). Conditions read from the check's own output rather than inherited from iteration 25's framing: **64.2% coverage on new code** (56.9% at iter 25, 52.8% at iter 24 — a live gate, not a stuck reading) and **B security rating on new code**. Non-required; handed to V1 with **delivery asserted** (body re-read, real content). Gate 0/1: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**. **0** human directives on `#850` since the watermark (of **13** comments). Ledger valid at **5** rows, **1 OPEN**. Inbox: **3** unread on the cloud store (a coordinator timeout, V1's iteration-295 report, one external `mcp-public` package report — external-origin, never auto-outranks) and **43** on the local store, all eval-suite/nightly bot traffic; none a directive, none outranking. Blocked-row predicates re-run **as commands**: upstream `#154` still **OPEN** (control `#175` **MERGED**, negative control 404s) so rows 10/11/12 stay parked. Died-mid-flight traces clean: the one open PR on the fleet account (`#942`, `worktree-sprint-m-eval-rolling-elo`) has **no branch** in this clone's worktree list and is **not attributable** to this mission — left alone per the fleet-filter rule. Weekly sweep and rotation both **not due** (`#850` created `2026-08-24T07:39:32Z` = 09:39 **local**, after the Monday-07:00 local boundary; 13 comments < 80). **RUNNING-SKILL CHECK ON THE RESOLVED SYMLINK, AND IT MATTERED AGAIN:** `~/.claude/skills/mission-control` → **V1's** checkout (inode `51683298`), **byte-identical to `origin/dev`** (`cmp` rc=0), while this fire's CWD carries a copy **12,699 bytes short** (inode `48752546`, `cmp` rc=1). The 187-line delta was read **before any gate ran** and carried the replaced designer rotation (`pi:ollama/kimi-k3:cloud`), the comma-separated fallback chains, the `mission_pi_run.sh` typed-verdict lane, the `approvals` decision-spine rule, and three new Gate-2 rules — two of which (V1 iter-270's lane-baseline rule and iter-274's effect-verification rule) were applied above and changed what this iteration did. Routing: controller `claude:claude-opus-5`; executor `codex:gpt-5.6-sol` (probe rc=0 replying `ok`, ONE run, no cap, no fallback); evaluator **sonnet in its own worktree**, distinct provider so generator≠judge holds. No designer, no planner, no quorum — the row specifies its own scope. Rotation pointer untouched at `claude:claude-fable-5`; Fable **unspent**. Metered **$0.00** of $5. No GPU, no `rig.lock`. Gates on **darwin/arm64**; windows and ubuntu legs unrun locally and read from Gate 3b. Recorded but **not** acted on (one instance each, below the ≥2 bar): `MISSION_EXECUTOR_FALLBACK` on this rig is still the single pre-2026-08-26 value while the running skill documents a comma-separated chain with an ollama-cloud rung ahead of it; and the codex executor spent its opening turns on the repo `CLAUDE.md`'s "Session start" inbox instruction, which cannot complete under `workspace-write`. Gate-5: **NO skill edit** — every friction this iteration is an instance of a rule the rulebook already carries and that **worked** (the resolved-symlink check caught the stale copy; iter-270's lane rule kept an unsatisfiable gate out of the directive; iter-274's effect-verification caught nothing but cost nothing; rule 3n's diff-anchored drill found M3's pre-existing-only pin, and the judge found the hunk even that missed). Next: row **6i**.
./design_docs/motoko-mission-status-archive.md:9:## STATUS 2026-08-27 — ITERATION 26 COMPLETE: **ITERATION 25's FIX IS CORRECT AND CANNOT RUN — THE PIN GATE IS LOADED FROM THE TREE THE PIN EXISTS TO REPLACE.** Pick was **NOT** the queue head: a genuine regression outranks it and **this fire is the instance, again** — the driver logged `DRIVER PIN FAILED` at `11:56:34` and executed the source clone at `e3ed9467f`, **172** commits behind `origin/dev`, up from **152** one fire earlier. **THE FIX LANDED WHERE THE DEFECT PREVENTS IT FROM BEING READ.** launchd's `ProgramArguments` invokes the *source clone's* `tools/launchd/mission-control.sh`, which sources the *source clone's* `tools/launchd/lib/pin-root.sh` — 172 commits behind, carrying the **pre-fix** `_pin_onboarded`. So the gate deciding whether to execute committed code is itself loaded from the stale tree the pin exists to bypass, and `ff0da7445` — correct, evaluator-passed, Gate-3b-green — has never executed on this mission. **BOTH PREDICATES RUN VERBATIM AGAINST THE LIVE `~/.claude.json`, IN ONE CALL:** the clone's (retired key only, **the copy that ran**) returns `false` for the pin worktree **and** `false` for the clone, so the *"neither path onboarded"* branch is unconditionally true → **REFUSE-TO-PIN**; `origin/dev`'s (the copy that did not run) returns `false` for the pin worktree and **`true`** for the clone → **PIN-OK**, since the refusal needs *both* false. Controls in the same call: retired `hasCompletedProjectOnboarding` **0** occurrences whole-file, `hasTrustDialogAccepted` **15** (positive), an invented key **0** (negative), **15** entries enumerated. The fix is therefore not merely plausible — it is measured to pass **on this exact machine and this exact state**. **A COUNT WOULD HAVE READ AS COVERAGE; READING IT IS WHAT CAUGHT IT.** `grep -c 'hasTrustDialogAccepted'` on the clone's `pin-root.sh` returns **1**, not 0 — and that occurrence is a **comment at line 185**, not code (`origin/dev`'s copy: **4**; invented-token negative control: **0** in both). A bare non-zero count is exactly what "the fix is present" looks like. **MOTOKO-SPECIFIC, NOT FLEET-WIDE, AND THE THIRD ARM IS WHAT SAYS SO (rule 3l):** control/V1 clone **0** behind with **4** fix hits, world **0** behind with the file **absent** (verified by `ls`, not read as a zero — rule 3a), motoko **172** behind with 1 comment-only hit; `DRIVER PIN FAILED` per driver log 4 / 0 / 3, positive control (`iteration`) 19 / 15 / 5, negative control **0** in all three. **V1 escaped because its clone happens to be current, not because of anything the fix did** — so the mission that found and fixed the bug is the only one still living with it, and the cause is **drift, not code**. **WHY NOTHING LANDED THIS ITERATION COULD HAVE FIXED IT:** any change goes to `origin/dev`, precisely the tree the broken clone cannot reach, so a class fix to `pin-root.sh` would be **inert for motoko** (live only for V1) — i.e. it would repeat iteration 25's mistake one level up. Recognising that is this iteration's deliverable; the class is **filed as row 6l**, not executed. **THE REMEDY IS ONE WORD AND THIS IS THE FIFTH ASK.** `D-MOTOKO-WORKDIR-2` stays **OPEN**. The skill's four non-destructiveness obligations are **measured, not assumed**: **0** commits ahead, **0** dirty lines in the clone and **0** across all eight sibling worktrees, nothing to back up, and `git checkout -B dev origin/dev` is the protective form that errors rather than clobbers. What changed since asks 1–4 is the **class** of the ask — iterations 21–25 raised it as hygiene against a *predicted* cost; the cost is now **measured**: the loop cannot heal itself, iteration 25's landed work is dead letter, and drift grew **152 → 172 in a single day**. Standing authorization is a human decision, so it is parked and **not taken**. **AN UNBLOCK THAT WOULD WORK AND WAS REFUSED:** re-adding the retired key to `~/.claude.json` would make the stale gate pass and the loop self-heal next fire — rejected because it satisfies a **retired schema** to trick a gate already correctly fixed upstream, mutates shared out-of-repo harness state no review gate can see, and Claude Code may rewrite it at any time. Named for Mark as an option; not taken unattended. **DEV CI: ONE NOT-GREEN, INHERITED AND NOT OURS.** `checks=20` (control fires, so the endpoint answered); `SonarCloud Code Analysis` = `failure` on HEAD and on V1's `20ce815bf` and `0911d1089`, while `71693ead0` carries **no** Sonar check at all (negative control). Non-required; `sunholo-data/ailang` is V1's to own. **QUEUE HEAD RE-CHECKED AT A FRESH ORIGIN AND IS GENUINELY OPEN:** `ChatStepResponse.Usage` is still a **value type** at `internal/ai/openai/step.go:300`, no merged PR matches, and the issue reported at #842 is still OPEN — row **6h** stays next. Gate 0/1: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**. **0** human directives on `#850` since the watermark (of **11** comments). Ledger valid at **5** rows, **1 OPEN**. Inbox **9** unread on the cloud store — 3 coordinator approvals, 2 external `mcp-public` package reports (external-origin, never auto-outrank), 1 eval-suite stat, 1 parser-cascade note: none a directive, none outranking. Phase-0 re-measured as a command: G1 `#154` still **OPEN**, control `#175` **MERGED** — rows 10/11/12 stay parked. Died-mid-flight traces clean: the only open PR on the fleet account is `#695` (`coordinator/task-d98bb271`), absent from this clone's 9-entry worktree list and **not attributable** to this mission — left alone per the fleet-filter rule; clone and all eight worktrees **0** dirty. Weekly sweep and rotation both **not due** (`#850` created `2026-08-24T07:39:32Z` = 09:39 **local**, after the Monday-07:00 local boundary; 11 comments < 80). **RUNNING-SKILL CHECK ON THE RESOLVED SYMLINK, AND IT MATTERED:** `~/.claude/skills/mission-control` → **V1's** checkout (inode `51683298`), **byte-identical to `origin/dev`** (`cmp` rc=0), while this fire's CWD carries a copy **139 lines short** (inode `48752546`, `cmp` rc=1) — the delta was read **before any gate ran** and carried the designer-rotation replacement, the comma-separated fallback chains, the `mission_pi_run.sh` typed-verdict lane and three new Gate-2 rules. On an unpinned fire the relative-path form of that check is actively wrong, which is iteration 241's hazard met from the other direction. Routing: controller `claude:claude-opus-5` and **no other role ran** — no designer, planner, executor, evaluator or quorum, because the deliverable is a measurement and an escalation and the only remedy is human-gated. Rotation pointer untouched at `claude:claude-fable-5`; Fable **unspent**. Metered **$0.00** of $5. No GPU, no `rig.lock`. Gates on **darwin/arm64**. Gate-5: **NO skill edit** — the rules that mattered (the resolved-symlink check, rule 3a's read-don't-count, rule 3l's third arm) all exist and all **worked**. Next: row **6h**.
./design_docs/motoko-mission-log.md:2773:`mission_pi_run.sh` typed-verdict lane and three new Gate-2 rules.
./design_docs/motoko-mission-log.md:3469:`claude:claude-fable-5`; probe rc=0; two runs via `scripts/mission_pi_run.sh`, verdict `ok` both
./design_docs/v1-mission-log.md:20311:**Routing evidence**: controller=`claude-opus-5` (session) · designer=`pi:ollama/deepseek-v4-flash:0731-cloud` via `scripts/mission_pi_run.sh`, probe rc=0, verdict `ok`, **rotation pointer advanced** · planner=`opus` via the Agent tool, lane used VERBATIM, no codex probe for that role · executor=`codex:gpt-5.6-sol` via the cross-provider recipe, probe rc=0, directive 12,904 B, terminal rc-file marker, inside the 30-min cap · evaluator=`sonnet` via the Agent tool in its own detached worktree, tree left byte-identical. **Fable diet UNSPENT** — the rotation designer was the pi lane, so no Fable run occurred at all. **metered=$0.2262** of $5 (two quorum rounds, $0.1003 + $0.1258); designer $0 flat-rate; no managed_agents call, no GPU. Gates on **darwin/arm64**; ubuntu and windows legs green in CI.
./design_docs/v1-mission-log.md:20340:**Routing evidence:** controller `claude-opus-5` (session). Executor `pi:ollama/deepseek-v4-flash:0731-cloud` via `scripts/mission_pi_run.sh`, probe rc=0, verdict **`wall_timeout` (rc=13) after 1803s — FLAGGED lane failure**; it had produced a complete in-scope 2-file diff and 44 tool executions, so the diff was verified first-party rather than discarded, and the controller re-ran every gate and the drill itself. Evaluator `sonnet`, two rounds, own dedicated worktree (`.wt-v1-iter306-judge`) — generator≠judge holds across providers (DeepSeek wrote, Anthropic judged). **Designer and planner did not fire**: a CI-red preemption produces no new design doc and no sprint plan, so per Gate 3's role triggers there was nothing for them to do; no role was silently omitted. Codex lanes were quota-exhausted this fire (driver probe rc=1, resets ~09:36) — `PARKED-ON-LANE` class, no human decision implied.
./design_docs/v1-mission-log.md:20985:`claude:claude-fable-5`; pointer advanced; `mission_pi_run.sh` verdict `ok`, 61 s, 12 tool calls,
./design_docs/v1-mission-log.md:21001:**Friction (one instance, recorded for the ≥2 bar)**: `scripts/mission_pi_run.sh` invokes pi
./design_docs/v1-mission-log.md:22230:verdict **`ok` with `worktree_changed_files: 1` and wrote nothing** — 9 tool executions, all reads
./design_docs/v1-mission-log.md:22232:`mission_pi_run.sh`'s worktree-diff assertion — the one its own header calls load-bearing,
./design_docs/v1-mission-log.md:22421:termination; runner rc10 empty_worktree/pi143,446 s, 226 tools, zero changed files and no report.
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:6:**Estimated**: ~1.25 days (≈0.5 d bounded snapshot helper + watchdog in `scripts/mission_pi_run.sh`, ≈0.5 d new hermetic `test_mission_pi_snapshot.sh` + revising TEST 1/TEST 5 + artifact/physical-path normalization, ≈0.25 d verification and judgement)
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:14:`scripts/mission_pi_run.sh` is the pi executor lane's guard. Its own header says the worktree
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:25:- `scripts/mission_pi_run.sh:232` — `DIFF_LINES=$(git -C "$WORKDIR" status --porcelain 2>/dev/null | wc -l | tr -d ' ')`.
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:26:- `scripts/mission_pi_run.sh:241-243` — `if [ "${DIFF_LINES:-0}" -gt 0 ]; then VERDICT_NAME="ok"; RC=0 else VERDICT_NAME="empty_worktree"; RC=10`.
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:27:- `scripts/mission_pi_run.sh:60/146` — `set -u` and `set -m` only; **no** `set -e`, **no** `set -o pipefail`, **no** `trap`.
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:28:- Verified runner integration: `START=$(now)` at line 167 (after launch); process-group kill pattern at lines 172-179; `wait "$RUNNER_PID"`; verdict JSON built with `worktree_changed_files` at line 257. Repo has **24,380 tracked paths**.
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:35:| clean (nothing written) | `empty_worktree` | 10 | 10 (correct) |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:36:| pre-dirty tracked, no write | `ok` | 0 | **10 / empty_worktree** (defect) |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:37:| pre-existing untracked, no write | `ok` | 0 | **10 / empty_worktree** (defect) |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:47:**Primary Goal:** make `empty_worktree` (rc 10) mean byte-for-byte identity of the eligible
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:52:reports `worktree_changed_files == 1` (distinct paths, not symmetric records); a snapshot that
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:53:cannot be established reports `snapshot_error` (rc 15), **never** `ok` or `empty_worktree`; every
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:61:  Existing stall verdicts are **preserved unchanged**; `snapshot_error` only replaces the `finished`
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:69:  unsupported and out of scope (worst case: `snapshot_error` or a stale-but-consistent digest, never
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:72:  `test_mission_pi_run.sh` (inherited, measured planner 8/9 vs controller 9/9 under `POLL1`); not
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:87:- **Unchanged:** `0 ok`, `10 empty_worktree`, `11 reasoning_stall`, `12 stream_dead`,
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:89:- **New:** `15 snapshot_error` — the before or after eligible-content snapshot could not be
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:91:  failure **never** resolves to `ok` or `empty_worktree`. `empty_worktree` is emitted only when
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:93:- `worktree_changed_files` becomes the **distinct changed-path count** (deleted+created+edited
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:95:- `snapshot_error` carries an **honest** reason in the verdict JSON (e.g. `"snapshot_error":
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:109:(`scripts/mission_pi_run.sh:172-179`): launch the snapshot in its own group (job control is already
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:111:`kill -TERM -- -PGID; sleep 1; kill -KILL -- -PGID; wait` (reap), returning rc 125 → `snapshot_error`.
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:125:NUL/tab/CR/LF (reject → `snapshot_error`, fail closed). Tag:
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:133:| other object type | — | **reject** → `snapshot_error` (explicit, no silent fallback) |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:143:- **equality:** `sha256(before-manifest) == sha256(after-manifest)` ⇒ `empty_worktree` rc 10.
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:144:- **changed:** else `ok` rc 0. `worktree_changed_files` = distinct changed **path** count:
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:188:Dispatch: in the `finished` branch, `if SNAP_ERR ⇒ snapshot_error rc 15`, else digest decision.
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:191:finished run anyway). `snapshot_error` JSON includes a reason field; it never claims
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:196:- `scripts/mission_pi_run.sh`: add `content_snapshot`, `snapshot_guarded`, `snapshot_delta` helpers;
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:199:  rc 15; emit `snapshot_error` reason; one `trap 'rm -rf "$SNAPDIR"' EXIT` for `$TMPDIR`-scoped temp
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:204:- `scripts/test_mission_pi_run.sh`: revise TEST 1/TEST 5 to start clean and actually write a file
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:207:  `mission_pi_run.sh:35-40`; `mission-control/SKILL.md:1410`): append `15 snapshot_error`. No
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:236:| A7: Machines First | +1 | Replaces a human-readable count with a machine-verifiable content digest; `empty_worktree` becomes a falsifiable claim |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:239:| A10: Composability | 0 | Additive rc 15 + `worktree_changed_files` meaning; existing fields/codes unchanged; no consumer code change |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:240:| A11: Structured Failure | +1 | New explicit `snapshot_error` rc 15, fail-closed, honest reason field, never resolves to `ok`/`empty` |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:248:a synthetic stub `pi`-free direct-helper suite). **Existing** `test_mission_pi_run.sh` TEST 1 and
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:257:1. Finished, nothing written, clean repo ⇒ `empty_worktree` rc 10 (preserved).
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:258:2. Finished, nothing written, **pre-dirty (tracked)** ⇒ `empty_worktree` rc 10 (defect fixed).
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:259:3. Finished, nothing written, **pre-existing untracked** ⇒ `empty_worktree` rc 10 (defect fixed).
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:261:   content identity, not status/name; `worktree_changed_files` == **1** (distinct, not 2).
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:263:6. Write **only** a gitignored file ⇒ `empty_worktree` rc 10 (scoped).
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:264:7. Non-repo worktree ⇒ `snapshot_error` rc 15, never `ok`.
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:266:   `empty_worktree`**; a **neighbor** edit is not swallowed ⇒ `ok`; a symlinked/differently-spelled
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:273:13. Filenames containing newline/tab/CR ⇒ `snapshot_error` (fail-closed, honest diagnostic), never
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:275:14. Snapshot **deadline expiry** ⇒ `snapshot_error` rc 15; subprocesses reaped; verdict never
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:277:15. Snapshot failure emits an honest `snapshot_error` reason; never claims "unchanged".
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:285:- **Deadline false-empty.** A snapshot that times out fails to `snapshot_error` (not `empty`); the
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:307:| V1 | Verdict derives from a `git status` line count; `set` options are `-u`/`-m` only | `grep -n "DIFF_LINES\|VERDICT_NAME=\"ok\"\|set -\|pipefail\|trap" scripts/mission_pi_run.sh` | `60:set -u`, `146:set -m`; `232:DIFF_LINES=$(git -C "$WORKDIR" status --porcelain 2>/dev/null \| wc -l…)`; `242:if [ "${DIFF_LINES:-0}" -gt 0 ]…`; zero `pipefail`/`trap` hits |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:308:| V2 | `START` is taken after pi launch (independent of before-snapshot point) | `grep -n "RUNNER_PID\|\bSTART=" scripts/mission_pi_run.sh` | `153:RUNNER_PID=$!`, `167:START=$(now)` (after launch) |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:309:| V3 | Suite **codifies** the defect (TEST 1/TEST 5 pre-dirty + assert ok on writeless stub) | `grep -n "dirty\|check \"happy path\"\|check \"slow-but-working" scripts/test_mission_pi_run.sh` | TEST 1 L42-45 `mkrepo … dirty` + `check "happy path" 0 … ok`; TEST 5 L79-82 same |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:310:| V4 | **Live repro**: pre-dirty/pre-existing-untracked no-op ⇒ `ok` rc 0; clean ⇒ `empty_worktree` | `bash /tmp/claude/verify_mppi.sh` (writeless stub `pi`) | `clean rc=10 empty_worktree`; `dirty rc=0 ok` (**defect**); `untracked rc=0 ok` (**defect**) |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:311:| V5 | **No-op on pre-dirty/untracked ⇒ equality ⇒ `empty_worktree`; edit-of-`M`-file distinct=1** | `bash /tmp/claude/mppi_snap_proto.sh` (prototype manifest+delta; synthetic repos) | `B dirty-noop: UNCHANGED 0 [ M f.txt ]`; `C untrk-noop: UNCHANGED 0`; `D dirty-edit: CHANGED 1 (…edited=1) [M f.txt]`; `E create: CHANGED 1`; `F rename: CHANGED 2` |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:318:| V12 | Suite has **zero Make/CI calls** with a positive control in the same invocation | `grep -n "make \|CI" scripts/test_mission_pi_run.sh` (negative) **+** `grep -rln "make " scripts/` (positive control) | test file: `NONE`; positive control returns many hits (so the negative is real); repo tracked count `git ls-files \| wc -l` → `24380` |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:319:| V13 | Only operational consumer of the pi verdict treats non-zero as LANE FAILURE | `grep -rn "mission_pi_run\|empty_worktree\|worktree_changed_files" .claude/skills/mission-control/` | SKILL.md:1410 documents `0/10/11/12/13/14`; L1441 non-zero → fallback+flag. `nightly_classify.py`/`metrics.go` `reasoning_stall` is the AILANG runtime category, unrelated |
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:327:- `scripts/mission_pi_run.sh`, `scripts/test_mission_pi_run.sh` — affected files; proposed new
./design_docs/planned/v0_35_2/m-pi-runner-worktree-assertion-vacuous-on-revision.md:341:`mission_pi_run.sh`; 0.5 d new hermetic `test_mission_pi_snapshot.sh` + revising TEST 1/TEST 5 and
./docs/sprint-retros/iter336-cache-module-id-encoding-controller-addendum.md:13:The first MiniMax attempt produced **no report or verdict**. Its session-protocol guard repeatedly denied writes, and the runner process group was terminated after lineage verification; rc10/empty_worktree, pi143, 446s, 226 tools, zero file changes. The same model succeeded at the latest commit after an in-session bounded inbox list and local protocol acknowledgement, with the guard still active: rc0/pi0, 182s, 37 tools, exactly one fresh report. This is a transport repair, not a model fallback or a failed product evaluation. Both attempts' receipts and reported flat-rate usage are banked alongside this note.
./docs/internal/harness-upgrade-runbook.md:36:  4. `message_end` still present in the event stream (`mission_pi_run.sh` banks it).
./docs/docs/guides/debugging.md:905:  `scripts/mission_pi_run.sh`, which filters the updates out and gives a typed verdict.
./.claude/skills/mission-control/SKILL.md:1403:       RUN THE LANE THROUGH `scripts/mission_pi_run.sh` AND READ ITS TYPED VERDICT.**
./.claude/skills/mission-control/SKILL.md:1405:       scripts/mission_pi_run.sh \
./.claude/skills/mission-control/SKILL.md:1410:       # rc 0=ok · 10=empty_worktree · 11=reasoning_stall · 12=stream_dead
./.claude/skills/mission-control/SKILL.md:1440:       runner makes for you (`worktree_changed_files` in the verdict JSON).
./.claude/skills/mission-control/SKILL.md:1441:  3. **Fallback:** probe-fail / any non-zero verdict from `mission_pi_run.sh` → the next link in
