# Controller qualifications on iteration337 independent evaluation

Actual judge: pi:ollama/minimax-m3:cloud; raw PASS82/100 for docs-only park at
a46e4c015cecb26ad06a3b44d5b6f8895a2d111e, preserved byte-identically next to this file.
No implementation approval, no controller score substituted.

The judge's Method says canonical inbox list had a45s deadline. The actual command did not
finish at that bound: the sandbox's Firestore/observatory call stalled. The transport wrapper
verified runner→pi→bash→ailang PID lineage and terminated ONLY the inbox child at100s.
The judge then proceeded to evidence review. Its own cloud inbox triage was unavailable;
controller canonical triage and approval-body delivery verification succeeded separately.
Session protocol acknowledgement and CLAUDE/skill reads occurred, but a tool attempt is not
successful inbox access. The raw report's timing wording is therefore qualified here.

The rubric's “no ancestor-symlink test” refers at most to this judge's own checks. The controller
had already run a synthetic ancestor-symlink reproduction and banked its result; it demonstrates
the draft's no-follow hole, not a passing implementation. Likewise baseline Go and performance
results were inspected in banked evidence, not independently rerun by this docs-only judge.
Its grep was narrower than the controller census and supports only that searched corpus.

The adapted82/100 score is the judge's own docs-only rubric, not the standard implemented-sprint
score. Suggestions in its conclusion remain unapproved design advice. D-58 remains OPEN.
