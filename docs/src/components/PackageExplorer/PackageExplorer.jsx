import React from 'react';

/**
 * PackageExplorer — interactive package browser with search/filter.
 * Full implementation in M4.
 */
export default function PackageExplorer() {
  return (
    <div>
      <p><em>Interactive package explorer loading... Full implementation coming in M4.</em></p>
    </div>
  );
}
