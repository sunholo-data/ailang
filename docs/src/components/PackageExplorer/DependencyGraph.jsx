import React from 'react';

/**
 * DependencyGraph — interactive force-directed package graph.
 * Full implementation in M4.
 */
export default function DependencyGraph() {
  return (
    <div>
      <p><em>Dependency graph visualization loading... Full implementation coming in M4.</em></p>
    </div>
  );
}
