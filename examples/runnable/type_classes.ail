-- Showcase: Type Classes
-- Operators work on different types!
module examples/runnable/type_classes

export func main() -> () ! {IO} =
  let intSum = 5 + 10 in
  let floatSum = 3.14 + 2.86 in
  println("Type classes: Int=" ++ show(intSum) ++ " Float=" ++ show(floatSum))
