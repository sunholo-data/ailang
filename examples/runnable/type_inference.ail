-- Showcase: Type Inference
-- The type checker automatically infers types!
module examples/runnable/type_inference

export func main() -> () ! {IO} =
  let double = \x. x * 2 in
  let result = double(21) in
  print("Type inference: " ++ show(result))
