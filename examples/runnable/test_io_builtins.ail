-- Test builtin IO functions (M-R1 Phase 5)
module examples/runnable/test_io_builtins

export func greet() -> () ! {IO} {
    _io_println("Hello from AILANG with builtins!")
}

export func greet_name(name: string) -> () ! {IO} {
    _io_println(name)
}
