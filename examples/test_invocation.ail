-- Test function invocation (M-R1 Phase 5)
module examples/test_invocation

-- Simple zero-arg function
export func greet() -> int {
    42
}

-- Function that uses arithmetic
export func double(x: int) -> int {
    x * 2
}
