-- Simple GCD module without dependencies
module examples/v3_3/math/simple_gcd

-- Greatest common divisor using Euclidean algorithm
export func gcd(a: int, b: int) -> int {
  if b == 0 then a else gcd(b, a % b)
}