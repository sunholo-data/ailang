-- Using polymorphic imports at different types
module examples/v3_3/poly_imports

-- Import polymorphic identity function
import examples/v3_3/poly_id (id)

-- Use id at Int type
let intResult = id(42) in

-- Use id at String type  
let strResult = id("hello") in

-- Print both to verify polymorphism works
intResult