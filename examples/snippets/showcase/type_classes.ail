-- Showcase: Type Classes
-- Operators work on different types!
module examples/snippets/showcase/type_classes

export func main() -> () ! {IO} =
  let intSum = 5 + 10 in
  let floatSum = 3.14 + 2.86 in
  print("Type classes: Int=" ++ show(intSum) ++ " Float=" ++ show(floatSum))
