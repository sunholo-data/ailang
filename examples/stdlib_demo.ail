module examples/stdlib_demo
import stdlib/std/io (println)
import stdlib/std/list (map)
import stdlib/std/string (toUpper)
import stdlib/std/option (Some, getOrElse)

export func main() -> () ! {IO} {
  let s = toUpper("hello");
  println("Result: " ++ s)
}
