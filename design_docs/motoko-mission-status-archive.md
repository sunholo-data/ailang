# Rotated STATUS stamps (newest first)

## STATUS 2026-08-31 — ITERATION 30 COMPLETE: **TWO SLOTS DIED IN A ROW, AND THE SECOND ONE DIED HOLDING A FINISHED, UNREVIEWED, UNPUSHED MILESTONE.** Pick was the **queue head, row 6i**, reached by INHERITING iteration 29's corpse rather than redoing it (Gate 2's died-mid-flight rule). Traces (a) and (b) both fired and (c) is what actually carried it: the fleet-account open-PR filter returned **4** PRs, `iter305`/`iter306`/`iter296`/`iter308`, **all V1's** and none with a branch in this clone's **15**-entry worktree list, so the PR trace said *nothing exists* — while `.wt-motoko-iter29-exec` held commit `4bd9e7110`, **237 insertions**, a complete milestone on a branch that had never been pushed, and the pin worktree held its **573**-line design doc and **237**-line sprint plan as UNTRACKED files. **ITERATION 28 ALSO DIED**, one gate later than 27: it landed [`61859c35d`](https://github.com/sunholo-data/ailang/commit/61859c35d) (#964, the pin-gate bootstrap) and stamped `D-MOTOKO-WORKDIR-2` **RESOLVED** in the ledger, then wrote **zero** STATUS rows and **zero** log entries — `grep -ci 'ITERATION 28'` returns **1** in the charter (that ledger row) and **0** in the log, control `ITERATION 27` **5**/**3**. Both are credited in the log here; the frequency is the report line, because the loop cannot diagnose its own dying slots and Mark can. **THE INHERITED WORK WAS VERIFIED, NOT ADOPTED.** Baseline at the rebased tree `/bin/bash tools/eval/test_motoko_connection_probe.sh` → **rc=0, 41 ok, 0 not ok** (was 40), and arm 36 is non-vacuous by its own evidence line: `ready=yes`, distinct wrapper/child PIDs, `pre_timeout_child_live=yes`, `timeout=yes`, `outer_cap_fired=no`, `survivors=0`, `real_lsof=/usr/sbin/lsof` — so it RAN rather than structured-skipping. **THE ROW'S OWN BAR, AND THE MUTANT IS A SOLE KILLER:** reverting the production group kill to a single-PID kill (mutant LANDED by sha256, PARSES `bash -n` rc=0, intended effect asserted against the system's own view — group-kill sites 1→0) takes the suite to **rc=1 with arm 36 the ONLY `not ok`**, `survivors=1 outer_cap_fired=no`; 35 arms still pass; restore byte-identical and the site count returns to 1. Before this row the identical revert left the suite **green 40/40**. The red arrives in the direction predicted, so it is paired with the baseline on the identical tree — rc=0 vs rc=1, outcomes DIFFER, which is what makes it evidence rather than co-occurrence. **THE JUDGE FOUND THE HALF MY DRILL COULD NOT SEE, AND BOTH ITS FINDINGS WERE REPRODUCED BEFORE BEING BELIEVED *AND* BEFORE BEING DISMISSED.** Evaluator **PASS 87/100, ZERO blocking** (sonnet, own worktree, distinct provider from the codex executor). Finding A: mutating **only** the SIGKILL escalation site leaves the suite **rc=0, 41 ok, survivors=0** — the fixture grandchild is a plain `sleep` with no TERM trap, so it dies at the TERM stage and the `-9` group form is dead-for-discrimination. Row 6i's mandated mutant changes BOTH sites at once and therefore reds on the TERM half alone, so the doc's bar is honestly met and the gap is one the bar could not see — rule (i-e)'s shape exactly: a removal proves the check FIRES, only a differently-shaped mutant proves it LOOKS. Finding B: `command -p -v` does **not** honour POSIX's standard-path guarantee on the CI shell — measured on GNU bash **3.2.57** (arm64-apple-darwin25), a shadowing `lsof` ahead of `getconf PATH` in the AMBIENT environment resolves as `REAL_LSOF` (arm → the hijack; control clean PATH → `/usr/sbin/lsof`; negative control, hostile dir without an `lsof` → `/usr/sbin/lsof`). The defence the doc names — the stub PATH this suite installs for itself — holds; only the comment's *“can never”* was too wide, and it was narrowed to what is defended in `1caf02e44`, **comment-only, so the evaluation over the executable tree still stands**. Both filed as row **6o**; neither widened the PR. **A THIRD DEFECT CAME FROM A SIBLING'S JUDGE AND WAS GHOST-DISCIPLINED BEFORE ADOPTION** — reported at #975 by V1 iteration 308's evaluator, against motoko's own instrument: neutering the in-loop wall-clock check in `descendant_pids` leaves the suite **rc=0, 41/41 ok**, so the arm named *“refuses on the real wall-clock deadline”* cannot fail for the reason it names. Reproduced at motoko's HEAD, mutant LANDED and PARSING; filed as row **6n**, and its B3 half (on the runner, discovery refuses with NEITHER message) is explicitly recorded as UNREPRODUCED because it can only be measured on the runner. **THE DOC BLOCKED AT QUORUM FOUR TIMES AND THAT IS DATA ABOUT THIS LOOP'S SCOPING, NOT ABOUT THE DOC.** All four artifacts read `blocked`; every objection was real; round 4's two carried concrete reviewer-authored `proposed_fix` text and disputed no design DIRECTION, so the narrow-refinement carve-out applies and iteration 29 applied them VERBATIM (both appear as the last two rows of the doc's own Quorum Verification Log) before routing. Per V1 iteration 257's rule the objection surfaces were tracked rather than the round count: they are **spread** — survivor oracle, helper duplication, hermetic-drive premise, readiness determinism, outer bound, platform — with **no** reviewer flipping to pass, so the disposition is *immature, keep revising*, **not** SPLIT. Round 4's third reviewer `oc-glm-5-2` is recorded `absent(invalid)` on malformed JSON whose raw fragment nonetheless reads `reject`, which is the tracked defect at #941. **GATE 3b GREEN ON THE MERGE, NOT MERELY ON THE PR HEAD.** PR head `1caf02e44`: **21** checks, **0** pending, **0** not-green, `mergeable` read FIRST (`MERGEABLE`/`CLEAN`), **4/4** required (`test`/`lint`/`build`/`docs-gate`). Merge commit [`4bd58bef6`](https://github.com/sunholo-data/ailang/commit/4bd58bef6): **16** checks, **0** pending, **1** not-green — `SonarCloud Code Analysis`, which rule 3d's parent-walk shows was ALREADY `failure` on the parent `f78b1d451` and on `15cec372b`/`d3e4e59cf`, with `d65a0900c` and `ebc089c33` carrying **no Sonar check at all** as the negative control. Non-required, inherited, and V1's to own. `docs-gate` is absent from the merge's check set by path filter — it passed on the PR head, where the merge button was gated (the reading iteration 25 already recorded). `launchd drivers (bash 3.2)`, the leg that actually runs this suite, **success** on both. **DEV CI WAS RED AND IT WAS NOT OURS AND WE DID NOT TOUCH IT.** `test` failed at `d65a0900c` on step *Verify embedded pi assets are in sync*; walked back per commit — first red at `ebc089c33` (V1's README-only edit), green at `15cec372b` and `9f267cf1f`. V1 already had the fix open as `#983` when this fire started, so the owning-mission rule held twice over: the red did not displace the pick and no duplicate fix was opened (the `#758`/`#759` lesson). It merged as `f78b1d451` at 09:39:31Z and this branch was rebased onto it. **WEEKLY SWEEP RUN, AND ITS HEADLINE NUMBER IS A PROPERTY OF THE FLEET, NOT A BACKLOG.** **94** open issues enumerated (list-length control quoted beside the verdict), each grepped `-cE '#<n>\b'` across **4** corpora — charter, log, STATUS archive, dashboard — printed as a per-issue table, never a summary sentence; negative control fired. **79 of 94** have zero mentions, which is expected and not actionable: this repo carries four missions and motoko's charter is scoped to the harness. The motoko-territory orphan that mattered is `#975`, now row **6n**. Gate 0/1: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, invalid-store control REFUSED rc=1 so the inbox read is real (`store: gcp (Firestore, project ailang-multivac)`). **0** human directives on `#850` since the watermark; ledger valid at **5** rows, **0 OPEN** — `D-MOTOKO-WORKDIR-2` was answered `Yes` on 2026-08-29T09:09:20Z and discharged by iteration 28. **19** unread on the canonical store, **0** of them a motoko directive and **0** outranking (5 mission-world, 3 mission-v1, 3 mark-attended-steering fleet broadcasts already implemented by V1 in `aca6908bd`, 4 package-feedback, 4 eval-suite bot). Blocked-row predicates re-run **as commands**: upstream `#154` still **OPEN** (control `#175` **MERGED**, negative control 404s), so rows 10/11/12 stay Phase-0 parked. **RUNNING-SKILL CHECK ON THE RESOLVED SYMLINK:** `~/.claude/skills/mission-control` → V1's checkout (inode `56533333`), **byte-identical to `origin/dev`** (`cmp` rc=0), and this fire's pin worktree copy (inode `60252309`) is byte-identical too — the first fire in the recorded run where both agree. The source clone reconciled itself under the now-granted standing authorization and sits **0 ahead / 0 dirty**. Routing: controller `claude:claude-opus-5`; **no designer, planner or executor ran** — all three are iteration 29's, inherited, and Gate 2 says verify-and-land rather than redo; evaluator **sonnet in its own worktree**, distinct provider so generator≠judge holds. Rotation pointer untouched at `claude:claude-fable-5`; Fable **unspent**. Metered **$0.00** of $5 this iteration (iteration 29's quorum round 4 spent **$0.1108**). No GPU, no `rig.lock`. Gates on **darwin/arm64**, on the CI shell's own bash 3.2.57; windows and ubuntu legs unrun locally and read from Gate 3b. Gate-5: **NO skill edit** — every friction here is an instance of a rule the rulebook already carries and that WORKED (the died-mid-flight traces found the corpse, the fleet-filter rule kept me off three sibling PRs, the owning-mission rule kept me off V1's red, ghost discipline turned a sibling's claim into a first-party measurement, and reproduce-before-dismissing turned two non-blocking findings into a queue row). Next: row **6n**.

## STATUS 2026-08-28 — ITERATION 27 COMPLETE: **THE GUARD WAS NOT MISSING, IT WAS INEXPRESSIBLE — A VALUE TYPE MADE "ABSENT" AND "ZERO" THE SAME VALUE.** Pick was the **queue head**, row **6h** — the first iteration since 23 to reach it, because 24, 25 and 26 were each preempted by a loop-health regression. Re-verified at a fresh `origin/dev` rather than inherited: `Usage` is still a value type at `internal/ai/openai/step.go:300`, `#842` still **OPEN**, no merged PR or direct commit touches it. PR [#946](https://github.com/sunholo-data/ailang/pull/946) → `75e4e12be`, evaluator **round 1 PASS 94/100, ZERO blocking**. Gate 3b GREEN on head `75e4e12be` (**21** checks, **0** pending, **0** not-green, **4/4** required — build/docs-gate/lint/test — `mergeable` read FIRST per the iteration-198 rule: `MERGEABLE`/`CLEAN`), merged as [`0e3a96585`](https://github.com/sunholo-data/ailang/commit/0e3a96585). **THE FIX IS ONE LINE OF TYPE DECLARATION AND THAT IS THE WHOLE FINDING.** A provider failure arrives as a *successful empty completion* (`finish_reason:"stop"`, `"content":null`, no `usage` key); the guard `#842` asked for could not be written because `Usage` was a **value**, so an omitted key unmarshals to the zero struct and `absent.Usage == allZero.Usage` is **true** — no expression over that type separates them. Now `*ChatStepUsage`. Step 2 (policy) stays deferred **by design**: Anthropic and Gemini do not share this parser and streaming usage delivery is opt-in on some providers, so a uniform guard turns a legitimate empty completion into an error — the same defect pointed the other way. Standing negative control for any future guard: the legitimate tool call, which also carries `content:null` and *does* report usage. **BLAST RADIUS MEASURED, NOT ASSUMED, AND WIDER THAN FILED:** `ChatStepResponse` is referenced only in `step.go` (:295 decl, :561 sole use) and by **zero** test files; `raw.Usage.*` reads at exactly five sites; controls `ChatStepUsage` **4** / invented symbol **0**. `internal/ai/ollama/step.go:345` builds an `openai` client against `<endpoint>/v1`, so **every ollama tool-calling turn parses here** — the defect sat on our own eval rig, where a masked provider failure is indistinguishable from a local model declining to act. **MUTATION DRILL ANCHORED TO THE DIFF HUNK BY HUNK (rule 3n), EACH MUTANT LANDED (sha256) + BUILDS + EFFECT-VERIFIED AGAINST THE GOFMT-PARSED FORM (V1 iter-274's rule) + RESTORED FROM A `cp` BACKUP BYTE-IDENTICAL:** **M1** break the `usage` tag → **2** tests red (new arms + pre-existing `TestStep_TextOnly_HappyPath`), kill-set member; **M2** nil no longer yields zero tokens → **SOLE KILLER**, and because its blast radius is one test the inverse arm applies and was run — suite `-skip`-ing my own test, under the mutant, **rc=0**; **M3** neuter the deref guard → reds **only** the pre-existing test, so the hunk is pinned but **not by anything this branch wrote**; **M4** revert to the value type → **DOES NOT BUILD** (`invalid operation: raw.Usage != nil (mismatched types ChatStepUsage and untyped nil)`) — the "mutant does not build" class, recorded as a **compile-time arm** and never as a behavioural kill, because here it *is* the row's finding restated by the compiler. **THE JUDGE FOUND THE GAP MY DRILL MISSED, AND THAT IS THE ITERATION'S BEST RESULT.** It reproduced every controller claim including M2's inverse arm, then built a **16-case differential harness** across parent `d5305fa79` — `usage:null`, `usage:[]`, `usage:"none"`, `usage:42`, negative tokens, malformed JSON — and found output **byte-identical in all 16**, covering the two cases I had named as untested. Its finding #1, reproduced first-party before being believed: `cacheRead = usage.PromptTokensDetails.CachedTokens` has **zero killers** — mutated to `+ 999` it lands, builds, effect-verifies 0→1, and leaves openai + openrouter + ollama at **rc=0 with 0 `--- FAIL` lines**. The mirage is rule 3i's shape: `cache_usage_test.go` has **0** references to `ParseChatStepResponse` and **7** to `Generate`. Pre-existing (only the receiver was renamed), so **filed as row 6m**, not used to widen the PR — the disposition iteration 24 gave row 6i. **GATES RE-RUN OUTSIDE THE SANDBOX, WITH A FRESH BINARY, AND BOTH HALVES MATTERED.** 15 test files across the three `internal/ai` packages bind loopback sockets, which `workspace-write` denies — so the directive carried three in-sandbox-satisfiable gates and named the suites `UNINFORMATIVE UNDER SANDBOX` (V1 iter-270's rule, which iteration 24 paid for first-party), and the controller ran all of them outside: `go build ./internal/ai/...` **rc=0**, `go vet ./internal/ai/...` **rc=0**, `go test` openai/openrouter/ollama **rc=0/0/0**, `gofmt -l` empty, `check-file-sizes` **rc=0**, `check-changelog` **rc=0**, `test-check-changelog` **rc=0** (13 arms); judge additionally `golangci-lint` **0 issues**. Binary built to a scratch dir with version ldflags and prepended to PATH rather than `make quick-install` (shared `~/go/bin`): `v0.34.0-118-gd5305fa79-dirty` == `git describe`, against the system copy's `v0.34.0-75-gfb6084f4b-dirty` — **43 commits adrift**. **THE PIN STILL FAILS AND THIS FIRE IS AGAIN THE INSTANCE — BUT THE PREDICATE IS NOW SHARPER THAN "HOW FAR BEHIND".** `DRIVER PIN FAILED` at **02:03:09**, executing the clone at `e3ed9467f`, **205** behind (172 → 205 in one day). Iteration 26 wrote that *"V1 escaped because its clone happens to be current"*; measured this iteration, that is not the predicate. `git merge-base --is-ancestor ff0da7445 HEAD` → **NO** for motoko, **YES** for V1 — and **V1 is still 18 commits behind** while pinning cleanly, its last `DRIVER PIN FAILED` dated 2026-08-27 07:10 with clean fires since. So V1 **recovered** by crossing the fix, which motoko structurally cannot, because the tree that must read the fix is the stale one. Non-comment `hasTrustDialogAccepted` occurrences: motoko **0** (its single hit is the line-185 comment iteration 26 identified), V1 **3**; invented-token negative control **0** in both. Fleet third arm (rule 3l): world **0** pin failures, ever. **`D-MOTOKO-WORKDIR-2` STAYS OPEN — SIXTH ASK**, and the four non-destructiveness obligations are measured again, not assumed: **0** ahead, **0** dirty in the clone and **0** across all nine worktrees, nothing to back up. Also recorded: the driver's own lane-degradation notice **failed to send** (`WARNING: driver-pin notice FAILED to send via ailang messages`), i.e. queue row 2's human channel was silent for exactly the event it exists to report. **DEV CI: ONE NOT-GREEN, INHERITED AND NOT OURS.** `checks=16` (control fires), `SonarCloud Code Analysis` = `failure`; walked commit by commit — green at `7dff0942d`, first red at `caea1f9e1` (V1's M-EVAL-ROLLING-ELO merge). Conditions read from the check's own output rather than inherited from iteration 25's framing: **64.2% coverage on new code** (56.9% at iter 25, 52.8% at iter 24 — a live gate, not a stuck reading) and **B security rating on new code**. Non-required; handed to V1 with **delivery asserted** (body re-read, real content). Gate 0/1: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**. **0** human directives on `#850` since the watermark (of **13** comments). Ledger valid at **5** rows, **1 OPEN**. Inbox: **3** unread on the cloud store (a coordinator timeout, V1's iteration-295 report, one external `mcp-public` package report — external-origin, never auto-outranks) and **43** on the local store, all eval-suite/nightly bot traffic; none a directive, none outranking. Blocked-row predicates re-run **as commands**: upstream `#154` still **OPEN** (control `#175` **MERGED**, negative control 404s) so rows 10/11/12 stay parked. Died-mid-flight traces clean: the one open PR on the fleet account (`#942`, `worktree-sprint-m-eval-rolling-elo`) has **no branch** in this clone's worktree list and is **not attributable** to this mission — left alone per the fleet-filter rule. Weekly sweep and rotation both **not due** (`#850` created `2026-08-24T07:39:32Z` = 09:39 **local**, after the Monday-07:00 local boundary; 13 comments < 80). **RUNNING-SKILL CHECK ON THE RESOLVED SYMLINK, AND IT MATTERED AGAIN:** `~/.claude/skills/mission-control` → **V1's** checkout (inode `51683298`), **byte-identical to `origin/dev`** (`cmp` rc=0), while this fire's CWD carries a copy **12,699 bytes short** (inode `48752546`, `cmp` rc=1). The 187-line delta was read **before any gate ran** and carried the replaced designer rotation (`pi:ollama/kimi-k3:cloud`), the comma-separated fallback chains, the `mission_pi_run.sh` typed-verdict lane, the `approvals` decision-spine rule, and three new Gate-2 rules — two of which (V1 iter-270's lane-baseline rule and iter-274's effect-verification rule) were applied above and changed what this iteration did. Routing: controller `claude:claude-opus-5`; executor `codex:gpt-5.6-sol` (probe rc=0 replying `ok`, ONE run, no cap, no fallback); evaluator **sonnet in its own worktree**, distinct provider so generator≠judge holds. No designer, no planner, no quorum — the row specifies its own scope. Rotation pointer untouched at `claude:claude-fable-5`; Fable **unspent**. Metered **$0.00** of $5. No GPU, no `rig.lock`. Gates on **darwin/arm64**; windows and ubuntu legs unrun locally and read from Gate 3b. Recorded but **not** acted on (one instance each, below the ≥2 bar): `MISSION_EXECUTOR_FALLBACK` on this rig is still the single pre-2026-08-26 value while the running skill documents a comma-separated chain with an ollama-cloud rung ahead of it; and the codex executor spent its opening turns on the repo `CLAUDE.md`'s "Session start" inbox instruction, which cannot complete under `workspace-write`. Gate-5: **NO skill edit** — every friction this iteration is an instance of a rule the rulebook already carries and that **worked** (the resolved-symlink check caught the stale copy; iter-270's lane rule kept an unsatisfiable gate out of the directive; iter-274's effect-verification caught nothing but cost nothing; rule 3n's diff-anchored drill found M3's pre-existing-only pin, and the judge found the hunk even that missed). Next: row **6i**.

## STATUS 2026-08-27 — ITERATION 26 COMPLETE: **ITERATION 25's FIX IS CORRECT AND CANNOT RUN — THE PIN GATE IS LOADED FROM THE TREE THE PIN EXISTS TO REPLACE.** Pick was **NOT** the queue head: a genuine regression outranks it and **this fire is the instance, again** — the driver logged `DRIVER PIN FAILED` at `11:56:34` and executed the source clone at `e3ed9467f`, **172** commits behind `origin/dev`, up from **152** one fire earlier. **THE FIX LANDED WHERE THE DEFECT PREVENTS IT FROM BEING READ.** launchd's `ProgramArguments` invokes the *source clone's* `tools/launchd/mission-control.sh`, which sources the *source clone's* `tools/launchd/lib/pin-root.sh` — 172 commits behind, carrying the **pre-fix** `_pin_onboarded`. So the gate deciding whether to execute committed code is itself loaded from the stale tree the pin exists to bypass, and `ff0da7445` — correct, evaluator-passed, Gate-3b-green — has never executed on this mission. **BOTH PREDICATES RUN VERBATIM AGAINST THE LIVE `~/.claude.json`, IN ONE CALL:** the clone's (retired key only, **the copy that ran**) returns `false` for the pin worktree **and** `false` for the clone, so the *"neither path onboarded"* branch is unconditionally true → **REFUSE-TO-PIN**; `origin/dev`'s (the copy that did not run) returns `false` for the pin worktree and **`true`** for the clone → **PIN-OK**, since the refusal needs *both* false. Controls in the same call: retired `hasCompletedProjectOnboarding` **0** occurrences whole-file, `hasTrustDialogAccepted` **15** (positive), an invented key **0** (negative), **15** entries enumerated. The fix is therefore not merely plausible — it is measured to pass **on this exact machine and this exact state**. **A COUNT WOULD HAVE READ AS COVERAGE; READING IT IS WHAT CAUGHT IT.** `grep -c 'hasTrustDialogAccepted'` on the clone's `pin-root.sh` returns **1**, not 0 — and that occurrence is a **comment at line 185**, not code (`origin/dev`'s copy: **4**; invented-token negative control: **0** in both). A bare non-zero count is exactly what "the fix is present" looks like. **MOTOKO-SPECIFIC, NOT FLEET-WIDE, AND THE THIRD ARM IS WHAT SAYS SO (rule 3l):** control/V1 clone **0** behind with **4** fix hits, world **0** behind with the file **absent** (verified by `ls`, not read as a zero — rule 3a), motoko **172** behind with 1 comment-only hit; `DRIVER PIN FAILED` per driver log 4 / 0 / 3, positive control (`iteration`) 19 / 15 / 5, negative control **0** in all three. **V1 escaped because its clone happens to be current, not because of anything the fix did** — so the mission that found and fixed the bug is the only one still living with it, and the cause is **drift, not code**. **WHY NOTHING LANDED THIS ITERATION COULD HAVE FIXED IT:** any change goes to `origin/dev`, precisely the tree the broken clone cannot reach, so a class fix to `pin-root.sh` would be **inert for motoko** (live only for V1) — i.e. it would repeat iteration 25's mistake one level up. Recognising that is this iteration's deliverable; the class is **filed as row 6l**, not executed. **THE REMEDY IS ONE WORD AND THIS IS THE FIFTH ASK.** `D-MOTOKO-WORKDIR-2` stays **OPEN**. The skill's four non-destructiveness obligations are **measured, not assumed**: **0** commits ahead, **0** dirty lines in the clone and **0** across all eight sibling worktrees, nothing to back up, and `git checkout -B dev origin/dev` is the protective form that errors rather than clobbers. What changed since asks 1–4 is the **class** of the ask — iterations 21–25 raised it as hygiene against a *predicted* cost; the cost is now **measured**: the loop cannot heal itself, iteration 25's landed work is dead letter, and drift grew **152 → 172 in a single day**. Standing authorization is a human decision, so it is parked and **not taken**. **AN UNBLOCK THAT WOULD WORK AND WAS REFUSED:** re-adding the retired key to `~/.claude.json` would make the stale gate pass and the loop self-heal next fire — rejected because it satisfies a **retired schema** to trick a gate already correctly fixed upstream, mutates shared out-of-repo harness state no review gate can see, and Claude Code may rewrite it at any time. Named for Mark as an option; not taken unattended. **DEV CI: ONE NOT-GREEN, INHERITED AND NOT OURS.** `checks=20` (control fires, so the endpoint answered); `SonarCloud Code Analysis` = `failure` on HEAD and on V1's `20ce815bf` and `0911d1089`, while `71693ead0` carries **no** Sonar check at all (negative control). Non-required; `sunholo-data/ailang` is V1's to own. **QUEUE HEAD RE-CHECKED AT A FRESH ORIGIN AND IS GENUINELY OPEN:** `ChatStepResponse.Usage` is still a **value type** at `internal/ai/openai/step.go:300`, no merged PR matches, and the issue reported at #842 is still OPEN — row **6h** stays next. Gate 0/1: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**. **0** human directives on `#850` since the watermark (of **11** comments). Ledger valid at **5** rows, **1 OPEN**. Inbox **9** unread on the cloud store — 3 coordinator approvals, 2 external `mcp-public` package reports (external-origin, never auto-outrank), 1 eval-suite stat, 1 parser-cascade note: none a directive, none outranking. Phase-0 re-measured as a command: G1 `#154` still **OPEN**, control `#175` **MERGED** — rows 10/11/12 stay parked. Died-mid-flight traces clean: the only open PR on the fleet account is `#695` (`coordinator/task-d98bb271`), absent from this clone's 9-entry worktree list and **not attributable** to this mission — left alone per the fleet-filter rule; clone and all eight worktrees **0** dirty. Weekly sweep and rotation both **not due** (`#850` created `2026-08-24T07:39:32Z` = 09:39 **local**, after the Monday-07:00 local boundary; 11 comments < 80). **RUNNING-SKILL CHECK ON THE RESOLVED SYMLINK, AND IT MATTERED:** `~/.claude/skills/mission-control` → **V1's** checkout (inode `51683298`), **byte-identical to `origin/dev`** (`cmp` rc=0), while this fire's CWD carries a copy **139 lines short** (inode `48752546`, `cmp` rc=1) — the delta was read **before any gate ran** and carried the designer-rotation replacement, the comma-separated fallback chains, the `mission_pi_run.sh` typed-verdict lane and three new Gate-2 rules. On an unpinned fire the relative-path form of that check is actively wrong, which is iteration 241's hazard met from the other direction. Routing: controller `claude:claude-opus-5` and **no other role ran** — no designer, planner, executor, evaluator or quorum, because the deliverable is a measurement and an escalation and the only remedy is human-gated. Rotation pointer untouched at `claude:claude-fable-5`; Fable **unspent**. Metered **$0.00** of $5. No GPU, no `rig.lock`. Gates on **darwin/arm64**. Gate-5: **NO skill edit** — the rules that mattered (the resolved-symlink check, rule 3a's read-don't-count, rule 3l's third arm) all exist and all **worked**. Next: row **6h**.

## STATUS 2026-08-26 — ITERATION 25 COMPLETE: **THE PIN GATE HAD BEEN REFUSING UNCONDITIONALLY, BECAUSE THE KEY IT READS NO LONGER EXISTS — AND THE SUITE ASSERTED THE DEFECT WAS CORRECT.** Pick was **NOT** the queue head: a genuine regression outranks it, and this fire *was* the instance. `tools/launchd/lib/pin-root.sh`'s `_pin_onboarded` reads `.projects[<path>].hasCompletedProjectOnboarding` from `~/.claude.json`; Claude Code has retired that key. Measured, three readings in ONE call with both controls firing: the legacy key appears in **0 of 15** live project entries (whole-file `grep -c`: **0**), `hasTrustDialogAccepted` in **15 of 15** (positive control), an invented key in **0** (negative control). So the predicate answered `false` for every path, the *"neither path onboarded"* branch was **unconditionally true**, and every fire of every mission refused to pin — executing whatever the source clone's working tree held. Simulated before the change across all three missions: motoko, v1 and world all **REFUSE-TO-PIN**. **THIS ITERATION IS ITS OWN EVIDENCE:** the driver logged `DRIVER PIN FAILED` at 21:05:14 and ran the source clone at `e3ed9467f`, **152 behind** `origin/dev`; V1 reported the identical shape (`MISSION_WORKDIR unset`) in the same window, which is the two-mission corroboration rule 3l asks for before calling anything fleet-wide. It is the class tracked at issue #558, reinstated by the gate written for #558 (`019568cf1`, 2026-08-12) — correct when written, and a stale **capability** claim about the harness ever since. **THE SUITE COULD NOT HAVE CAUGHT IT, AND SAID THE OPPOSITE.** `test_pin_root.sh` writes its OWN synthetic `~/.claude.json` fixtures still carrying the retired key, and its arm 8 *asserted that the only key which now exists must be REFUSED* — a green gate pinning the production defect as correct behaviour. That is the fixture-vs-world gap: a gate can be complete over its own inputs and blind to the schema they model. PR [#923](https://github.com/sunholo-data/ailang/pull/923) → [`ff0da7445`](https://github.com/sunholo-data/ailang/commit/ff0da7445), evaluator **round 1 PASS 98/100, ZERO blocking**. Gate 3b GREEN on the merge: **16** checks, **0** pending, **1** not-green (`SonarCloud`, see below), required `test`/`lint`/`build` all **success** and `docs-gate` **N/A by path filter** on the merge commit (it passed on the PR head, where the merge button was gated); **`launchd drivers (bash 3.2)` — the job this change lives in — `success`** on both the PR head and the merge. `mergeable` read FIRST per the iteration-198 rule: `MERGEABLE`/`CLEAN`. **THE FIX RESTORES DISCRIMINATION RATHER THAN BLANKET-ACCEPTING, WHICH IS THE READING THAT MATTERS.** Simulated against the real `~/.claude.json` after the change: motoko **PIN-OK**, v1 **PIN-OK**, world **REFUSE-TO-PIN** — and world's refusal is a **TRUE** verdict, since that clone genuinely carries neither key, so the human fix its message names is the right advice. A fix that made all three pin would have been indistinguishable from deleting the gate. **THE ANTI-VACUITY FLOOR IS THE HALF THAT OUTLIVES THIS BUG.** When neither key appears in any entry, the gate now reports **Claude Code schema drift** — fail-closed still, #558's ratified posture deliberately unchanged; only the *sentence* moves — so the next rename is loud on its first fire instead of silent forever. **THE JUDGE FOUND A DEFECT THIS BRANCH INTRODUCED, IN THE EXACT FACULTY THE BRANCH EXISTS TO IMPROVE, AND IT WAS FIXED HERE.** Round 1 was a PASS with one non-blocking finding: the new drift diagnosis fired identically for a missing / invalid-JSON / non-object `~/.claude.json`, telling the next reader the **gate** needs a new key when the **file** is broken — this gate's own original defect one level down. Now three refusals, three sentences: unreadable · drift (naming the entry count it searched) · not-onboarded. A consequence worth naming: an **empty** `.projects` map is *"nothing onboarded yet"*, i.e. the third case, so arm 7's natural `{"projects":{}}` fixture was **RESTORED** — the executor's earlier fixture change was a correct workaround for the two-way split and unnecessary under the three-way one. Suite **35 → 53** arms. **MUTATION DRILL ANCHORED TO THE DIFF, NOT THE DEFECT (rule 3n), RE-RUN AT THE FINAL TREE.** Each mutant asserted **LANDED** (sha256), **PARSING** (`bash -n` rc=0) and **effect-verified against the queried form rather than the bytes** (V1 iteration 274's rule, read from the delta before it was applied), each restored byte-identical from a `cp` backup: **A** revert `_pin_onboarded` to legacy-key-only → **3** arms red, SOLE KILLER; **B** neuter the drift branch → **4** arms red; **E** neuter the unreadable branch → **3** arms red; **F** drop the `projects_len > 0` guard → **2** arms red, an empty map misdiagnosed as drift. **The load-bearing observation is which assertion DIDN'T move:** under B and E the `STATUS=STALE` assertion stayed **GREEN**, because every refusal produces STALE — only the message TEXT discriminates. That is rule 3i's *what else writes this value* met first-party, and it is why these arms assert prose rather than status. **C** revert the supporting `local` declaration hunk → **ZERO killers**, suite green — recorded as **UNPINNED**, not claimed as covered, since the only observable is variable-scope leakage no behavioural arm here can see. **D** revert arm 7's fixture → **2** arms red, which adjudicated the executor's **self-reported** deviation by measurement in both arms and **in its favour** (rule 3h(d)). **THE JUDGE ATTACKED BEYOND ITS BRIEF AND THE FIX HELD.** It built its own *widening-into-a-no-op* mutant (suite caught it, **7** arms red), ran the jq edge cases end-to-end through the real driver, and neutered the **precondition** of all three new arms — all three died, so none passes for a second reason. `/bin/bash` confirmed **3.2.57** on the rig. **THE RUNNING RULEBOOK WAS NOT THE ONE IN THIS CHECKOUT, AND THE PRESCRIBED CHECK IS WHAT CAUGHT IT.** `~/.claude/skills/mission-control` resolves to **V1's** checkout (inode `51683298`), which is **byte-identical to `origin/dev`** — while this fire's CWD, the source clone, carries a copy **139 lines short** (inode `48752546`). The 187-line delta was read BEFORE any gate ran; it contained V1 iteration 274's mutation rule, applied above, and the designer rotation's replacement of two structurally-dead entries with `pi:ollama/kimi-k3:cloud`. Unpinned fires make the relative-path form of that check actively wrong, which is exactly the hazard iteration 241 recorded. **DEV CI: ONE NOT-GREEN, INHERITED AND NOT OURS.** `SonarCloud Code Analysis` = `failure` on the merge and on **all three** preceding commits (`ae6e7e8c3`, `9da0b7b45`, `6b198f457` — all V1's), negative control `3f5ca3df9` carries no Sonar check at all. Non-required (required = `test`/`lint`/`build`/`docs-gate`). Conditions re-read rather than inherited from iteration 24's framing (rule 3n(d)): **56.9% coverage on new code** *and*, newly, **B security rating on new code** — the second condition is new since iteration 24 saw 52.8% coverage alone. This diff is shell + markdown, which Go coverage does not measure. Handed to V1 with the boundary and both conditions; it did **not** displace the pick. **A SECOND MOTOKO-OWNED RED WAS MEASURED AND FILED RATHER THAN PICKED** (row **6j**): `launchd drivers (bash 3.2)` failed twice and was cancelled once today — **3 non-success of 58** runs enumerated on `dev`, all three on *unrelated V1 coordinator commits*, all three at arm **33**, `not ok - descendant discovery refuses on the real wall-clock deadline exceeded its 120s arm cap`. Iteration 22's cap is doing exactly its job — converting a silent 15-minute cancellation into a loud named failure — and the underlying hang it exposes is still unfixed, with the arm passing locally in ~1s against a 120s cap on the runner. Gate 0/1: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**. **0** human directives on `#850` since the watermark (of **9** comments); `#743` re-read, its one Mark comment dated `2026-08-23`, before the watermark. Ledger valid at **5** rows, **1 OPEN** (`D-MOTOKO-WORKDIR-2`, unanswered since iteration 21 — **fourth** ask, and this iteration is the first to measure a cost of the drift rather than predict one). Phase-0 re-measured as a command: G1 `#154` still **OPEN**, control `#175` **MERGED** — rows 10/11/12 stay parked. Died-mid-flight traces clean: the two open PRs on the fleet account (`#918` `sprint/iter285-fmt-width-limit`, `#695` `coordinator/task-d98bb271`) have **no branch** in this clone's 9-entry worktree list and are **not attributable** to this mission — left alone per the fleet-filter rule. Source clone **0** dirty, **0** ahead. Weekly sweep and rotation both **not due** (`#850` created `2026-08-24T07:39:32Z` = 09:39 **local**, after the Monday-07:00 local boundary; 9 comments < 80). Routing: controller `claude:claude-opus-5`; executor `codex:gpt-5.6-sol` (probe rc=0, replied `ok`, ONE run, no cap, no fallback); evaluator **sonnet in its own worktree**, distinct provider so generator≠judge holds. No designer, no planner, no quorum — a measured regression with a named fix is not a design question. Rotation pointer untouched at `claude:claude-fable-5`; Fable **unspent**. Metered **$0.00** of $5. No GPU, no `rig.lock`. Gates on **darwin/arm64** — and for `launchd drivers (bash 3.2)` that IS the CI leg, which runs macOS-only by design for bash 3.2; windows/ubuntu legs read from Gate 3b's matrix. Gate-5: **NO skill edit** — this iteration's frictions are instances of rules the rulebook already carries and that *worked* (the resolved-symlink check caught the stale copy; rule 3n's diff-anchored drill found the unpinned hunk; 3d's parent-walk kept an inherited red from being blamed on the merge). Next: row **6h**.

## STATUS 2026-08-26 — ITERATION 24 COMPLETE: **THE CAP KILLED THE WRAPPER, AND THE FIX FOR THE HALF THAT MATTERS IS PINNED BY NOTHING.** Pick was the queue head, row **6g**. PR [#892](https://github.com/sunholo-data/ailang/pull/892) → [`fd1fa9e01`](https://github.com/sunholo-data/ailang/commit/fd1fa9e01), evaluator **round 1 PASS 82/100, ZERO blocking**. Gate 3b GREEN on head `c23a8c785` (**21** checks, 0 pending, 0 not-green, **4/4** required — build/docs-gate/lint/test — `mergeable` read FIRST per the iteration-198 rule, `MERGEABLE`/`CLEAN`). **THE ROW'S CLAIM WAS INHERITED AND WAS RE-MEASURED BEFORE ANYTHING WAS ROUTED.** Row 6g's evidence was iteration 22's evaluator; reproduced first-party against the shipped `run_bounded` with controls firing — PRE **0** live fixtures, cap fires `rc=199`, **1 survivor at `PPID 1`**, wrapper count **0**, POST cleanup **0**. The middle two readings are the whole finding: the suite's existing "process survived" check passes *because* the wrapper really is dead, so the old arm passed for exactly the reason it should have failed. **THE MECHANISM WAS MEASURED BEFORE IT WAS SPECIFIED, NOT AFTER.** `setsid` does not exist on macOS (`which setsid` → not found) and every process-group precedent in this repo is Go-side (`SysProcAttr{Setpgid:true}`), unusable from bash. Two arms that DIFFER, which is what makes it a discriminator: **without** `set -m` the child's pgid (`9361`) **is the script's own**, so a negative-PID kill would kill the suite itself and a single-PID kill leaves **1** orphan; **with** it, pgid == pid (`9379`), differs from the script's, and the group kill leaves **0**. That is why the shipped kill is *guarded* — `jobs -p` membership AND `pid != $$` AND a live `kill -0 "-$pid"` — rather than unconditional. **THE FINDING THAT OUTRANKS THE FIX: THE PRODUCTION HUNK IS PINNED BY NOTHING.** Walking the diff hunk-by-hunk rather than reasoning from the defect (rule 3n), mutant C reverts `tools/eval/motoko_connection_probe.sh` **entirely** to `origin/dev` — and the suite stays **green at 40/40, rc=0**. **Zero killers.** Its only gate is `bash -n`. So the half row 6g itself calls *"the one that matters"* — the production lane bound on the GPU rig, where a surviving descendant is indistinguishable from a model declining to act — shipped with no behavioural pin. Filed as row **6i** rather than growing the PR, per the rule that a hunk with no killer is a finding, not a failure. The self-test half **is** pinned: mutant A (group → single-PID kill) is the **SOLE KILLER** (`survivors=1`, 34 arms green before it); mutant B (neuter `set -m`) fires the safety refusal **twice** and the suite **reports** the failure instead of killing itself, which is the guard's entire purpose. Each mutant asserted LANDED (sha256), PARSING (`bash -n` rc=0) and **effect-verified against the parsed form** rather than the bytes, then restored byte-identical from a `cp` backup. **MY OWN GATE LIST WAS UNSATISFIABLE IN THE LANE I ROUTED IT TO, AND THE EXECUTOR CAUGHT IT.** Run 1 stopped with **zero files changed**: under codex `--sandbox workspace-write` the full suite reaches arm 32, prints `UNINFORMATIVE UNDER SANDBOX: loopback bind denied`, and arm 33's live-socket bind **terminates the enclosing session** with no rc. It stopped because my directive says a base-red gate is a finding rather than an obstacle — the rule was obeyed correctly and was pointed at a gate that could never be green there. This is the skill's own V1-iteration-270 defect (*a baseline is a claim about the environment you ran it in*) met first-party: all three gates were baselined **outside** the sandbox, in my shell, and handed to a lane that cannot satisfy one. Re-issued with the full suite labelled `UNINFORMATIVE UNDER SANDBOX`, a satisfiable in-sandbox harness in its place, and the suite re-run by the controller outside — which is what false-green #3 already prescribes. Cost one executor run; **not** a lane failure and **not** a fallback. **A GREP-SHAPED LEAK CHECK MATCHED A PROCESS THAT ONLY *MENTIONED* THE FIXTURE.** After the green run, `ps | grep "sleep 2849"` returned **1** and read as a leaked orphan; it was a codex notification process whose argv embeds the executor's own report, which contains the string. Scoped to `comm == sleep` the count is **0**, control firing (1 while a real one runs, 0 after). Worth recording because it **vindicates the executor's deviation from my directive**: I specified matching a distinctive sleep duration, and it instead scoped by a unique fixture **cwd** via `lsof -c sleep`. Its design is strictly better and mine produced a false positive within minutes (rule 3h(d) — a deviation adjudicated by measurement, in the executor's favour). **THE EVALUATOR CORROBORATED RATHER THAN TOOK ON TRUST**: it reproduced **all five** controller claims exactly including mutant C's zero-killer result, tried and **failed** to construct a false positive for the `group_safe` guard, confirmed `set -m` leaks no job-status lines by diffing arm lines against the 39-arm baseline, and verified `{ wait "$pid"; } 2>/dev/null || rc=$?` still propagates a real exit code (42, 5/5). Of three non-blocking findings, the one naming a defect **this PR introduced** was fixed here: the guarded fallback printed `INSTRUMENT FAILURE:`, byte-for-byte the prefix of `instrument_failure()` which prints it and then `exit 1`, so a log-grep for the abort signal matched **4** lines where only **2** meant termination — renamed to `INSTRUMENT DEGRADED:`, verified against the parsed form (4 → 2, 0 → 2). In a probe whose job is to tell a harness bug from a model verdict, an abort signal that does not mean abort is the wrong direction to be wrong in. Suite **39 → 40** arms; refusal-branch drift gate unchanged at **24** (no branch added or removed). **`~/go/bin/ailang` SILENTLY IGNORES `AILANG_MESSAGES_STORE`, SO GATE 0's CANONICAL INBOX WAS VACUOUS UNTIL MEASURED.** The documented control — an INVALID store must error — returned **rc=0** and listed local SQLite. Built a fresh binary to a scratch dir carrying the version ldflags (a bare `go build` in a linked worktree stamps `"dev"`), confirmed `v0.33.2-26-gfadbdc4e2`, and the control then correctly returned **rc=1** `unknown message store mode`. The cloud store holds **62** unread against the local store's **12** — different queues, and the header names the store only on the fresh binary. Enumeration widened after `--json` returned **20** against a header claiming 62; at `-limit 500` it returns **62 == 62**. No directive, no cross-mission request, nothing outranking. **DEV CI RED AND NOT OURS.** `SonarCloud Code Analysis` = `failure` on `origin/dev`, condition read from the check's own output: *52.8% coverage on new code (required ≥80%)*. Walked back commit by commit to a clean boundary — green at `6193bb712` (18:55), red at `6759ea4fa` (19:14, V1's messaging-store change) and on every commit since. Non-required, all 4 required contexts green. Recorded and handed to V1 on the cross-mission channel with the boundary and the condition, and **delivery asserted** (body re-read: 1,796 B of real content, not a `--body-file` path — iteration 252's rule); it did **not** displace the pick. Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, ran from the pin root detached and clean at `fadbdc4e2` == `origin/dev`. **RUNNING-SKILL CHECK ON THE RESOLVED SYMLINK TARGET:** `~/.claude/skills/mission-control` resolves to **V1's** checkout (inode `49847168`) and that copy **DIFFERS** from origin — **27 lines short**, missing V1 iteration 274's *"LANDED is necessary, not sufficient"* mutation rule. Read the delta before proceeding and applied that rule anyway (every mutant above was effect-verified against the parsed form). This pin root's own tracked copy (inode `50865888`) is byte-identical to `origin/dev`, so the rulebook followed here is the agreed one; V1's divergence is recorded, not inherited. **0** human directives on `#850` since the watermark (of **7** comments); `#743` re-read for the rotation-week catch, **0**. Ledger valid at **5** rows, **1 OPEN** (`D-MOTOKO-WORKDIR-2`, unanswered since iteration 21 — **third** ask). Phase-0 re-measured as a command: G1 `#154` still **OPEN**, control `#175` **MERGED**, so rows 10/11/12 stay parked. Died-mid-flight traces clean: the only open PR on the fleet account was `#695` (`coordinator/task-d98bb271`), absent from this clone's **9**-entry worktree list and **not attributable** to this mission — left alone per the fleet-filter rule; pin root and source clone both **0** dirty lines, all 8 sibling worktrees clean. Weekly sweep and rotation **both not due** (`#850` created `2026-08-24T07:39:32Z` = 09:39 **local**, after the Monday-07:00 local boundary; 7 comments < 80). Source clone **46 behind / 0 ahead**, clean — up from 35, above the notice threshold for a third consecutive iteration, exactly as `D-MOTOKO-WORKDIR-2` predicts. Routing: controller `claude:claude-opus-5`; executor `codex:gpt-5.6-sol` (probe rc=0, replied `ok`) — **two runs, the first wasted by my own gate list**; evaluator **sonnet in its own worktree** (iteration-199 rule), distinct provider from the executor so generator≠judge holds. No planner, no designer, no quorum — the row specifies its own scope and the parent doc's quorum is spent. Rotation pointer untouched at `claude:claude-fable-5`; Fable **unspent**. Metered **$0.00** of $5 — codex and sonnet are quota buckets. No GPU, no `rig.lock`. Gates on **darwin/arm64** — which is the **only** platform the `launchd drivers (bash 3.2)` CI job runs on, deliberately, for bash 3.2, so the local green **is** that CI leg rather than a proxy for it. Gate-5: **NO skill edit** — the iteration's sharpest friction (a gate list baselined in the wrong lane) is an instance of a rule the skill ALREADY carries and I failed to apply, not a gap in the rulebook; it goes to the log's Ruled-out. Next: row **6h**.

## STATUS 2026-08-25 — ITERATION 23 COMPLETE: **TWO ISSUES FILED IN THE SAME SESSION BY THE SAME REPORTER, AND ONLY ONE WAS EVER A BUG — THE CHEAPER CHECK IS THE ONE THAT SAID SO.** Pick was the queue head, row **6f**: triage-lite the two motoko-owned issues from iteration 21's sweep. **No sprint, no PR of code** — the row names its own procedure with machine-checkable postconditions, so no designer, planner, executor or evaluator was spawned (same disposition and reasoning as iteration 21's ops pick); record landed by PR. **`#839` IS A VERSION SKEW, NOT A DEFECT, AND THE CONTROL IS WHAT MAKES THAT A MEASUREMENT.** The reported binary is `v0.33.0`/`ae36986`, **2026-08-04**; the request-aware proxy transport landed **2026-08-20** in `e5ee6c5e5` (PR `#613`) — sixteen days *after* the build and three days *before* the filing. `git ls-tree ae36986 internal/effects/net_proxy.go` is **empty** while the control `internal/effects/context.go` returns a blob in the same call, and `git merge-base --is-ancestor e5ee6c5e5 ae36986` is **false**. So the reporter's three byte-identical DNS errors were correct observations of a build with no proxy path to consult. **CLOSED DURABLY, NOT AS BOOKKEEPING:** the behaviour the decisive third repro isolates is covered by committed tests CI runs (`ci.yml:101` `go test -timeout 300s ./...`) — `TestNetProxyBoundary/proxy_selected_from_environment` and `TestNetProxyTargetValidation/proxy_hostname_remains_unresolved`, plus `TestNetProxyNoProxy`, `TestNetProxyDirectPin`, `TestNetProxyRedirectControls`; `-run TestNetProxy` **rc=0** captured without a pipe, negative control `-run TestNetProxyZZZNoSuchThing` → `[no tests to run]`. **THE SUITE'S ONE `SKIP` WAS CHECKED RATHER THAN COUNTED** (rule 2 — a parked test is a claim): `TestNetProxyEnvProxyHelper` skips unless `AILANG_M1_PROXY_HELPER=1`, and `TestNetProxyBoundary` re-execs it as a subprocess with that variable and asserts `--- PASS:` in its output — it is the **only** arm exercising production `http.ProxyFromEnvironment` instead of the injected `proxySelector` hook, i.e. the skip is the mechanism, not a hole. Ran alone: rc=0. **`#842` IS REAL AND THE MEASUREMENT OUTRUNS THE REPORT.** The verbatim failing body through `openai.ParseChatStepResponse` (`step.go:560`) returns **OK** — no error, `text=""`, `toolcalls=0`, `in=0 out=0 total=0` — with three controls in the same run: healthy → `"pong"` with real usage (positive fires); legitimate tool call with `content:null` and usage **present** → 1 tool call, which is exactly what a *"null content is suspicious"* heuristic would false-positive on and is why keying on `usage` is right; and usage **present-but-all-zero** → **byte-identical output to the failing arm**. **THE LOAD-BEARING FIND IS THAT THE SUGGESTED GUARD IS NOT EXPRESSIBLE:** `ChatStepResponse.Usage` is a **value type** (`step.go:300`), so absent unmarshals to zero — `absent.Usage == zeroed.Usage` is **true**. The precondition for any policy is a representation change (`*ChatStepUsage` or a raw-key presence check), behaviour-free and separable. **AND THE BLAST RADIUS IS WIDER THAN FILED, IN THE DIRECTION THIS MISSION CARES ABOUT:** `ParseChatStepResponse` has exactly **two** production callers (`openrouter/step.go:162`, `openai/step.go:170`; controls **77** and **0**) and `internal/ai/ollama/step.go:345` builds `openai.NewClient(…)` against `<endpoint>/v1` — so **every ollama tool-calling turn parses through it**, putting the defect on our own Mac Studio rig, where a masked provider failure is indistinguishable from a local model declining to act. That is the charter guardrail *"never conclude model wall"* arriving from outside the mission, from a reporter with no way to know it was ours. Filed as row **6h** with the fix ordered, and the reporter's own caveat **upheld** — the Anthropic/Gemini paths do not share this parser and streaming usage delivery is opt-in on some providers, so a uniform guard would turn a legitimate empty completion into an error, the same defect pointed the other way. Both verdicts posted as their own `gh issue comment --body-file` **before** any close and **asserted landed by comment-count growth** against a pre-count control (`#839` 1 → 2 and `CLOSED`; `#842` 0 → 1 and `OPEN`), per Gate 0's mechanism-B rule. **DEV CI WAS RED AT PICK TIME AND THIS MISSION DOES NOT OWN IT.** `CI` `failure` on `02bf43668`, **1** not-green of **15** exact-SHA checks, job `test`; failing step is *Download all Go modules* — `proxy.golang.org … stream error: stream ID 1187; INTERNAL_ERROR` — on V1's **docs-only** iteration-276 record (4 markdown files, **0** code), which cannot affect a module download, with parent `f4828cc89` green 2h46m earlier as the before-arm. Fired `gh run rerun --failed` as rule 3d's strongest control and handed it to V1 on the cross-mission channel per the charter guardrail; it did **not** displace the pick. Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, ran from the pin root detached and clean at `02bf43668` == `origin/dev`. **RUNNING-SKILL CHECK, AND WHICH FILE ACTUALLY EXECUTES:** `~/.claude/skills/mission-control` resolves to **V1's** checkout (inode `49847168`) and that copy **DIFFERS** from origin — but a session run from this pin root reads the pin's own tracked `.claude/skills/` (inode `50865888`), which is **byte-identical to `origin/dev`**, so the rulebook followed here is the one the mission agreed on; V1's divergence is recorded, not inherited. **0** human directives on `#850` since the watermark (of **6** comments); `#743` re-read for the rotation-week catch, **0**. Ledger valid at **5** rows, **1 OPEN** (`D-MOTOKO-WORKDIR-2`, unanswered since iteration 21). Inbox **3** unread: this loop's own drift notice, V1's iteration-276 report, an eval-suite start — none a directive, none outranking. Phase-0 re-measured as a command: `G1 #154` still **OPEN**, control `#175` **MERGED**, so rows 10/11/12 stay parked. Died-mid-flight traces clean: the only open PR on the fleet account is `#695` (`coordinator/task-d98bb271`), absent from this clone's **9**-entry worktree list and **not attributable** to this mission — left alone per the fleet-filter rule; main checkout `git status --porcelain` **0** lines. Weekly sweep and rotation **both not due** (`#850` created `2026-08-24T07:39:32Z` = 09:39 **local**, after the Monday-07:00 local boundary; 6 comments < 80). **THE SOURCE CLONE CROSSED THE NOTICE THRESHOLD EXACTLY AS `D-MOTOKO-WORKDIR-2` PREDICTED** — **35 behind / 0 ahead**, up from 24 one iteration ago, and the drift notice fired on schedule. This is the second ask for the same one-word answer for the same mechanical operation. Routing: controller `claude:claude-opus-5`; rotation pointer untouched at `claude:claude-fable-5`, Fable **unspent**; metered **$0.00** of $5; no GPU, no `rig.lock`; gates on **darwin/arm64** only. Gate-5: **NO skill edit** — the iteration's one friction (a triage row whose two issues needed opposite dispositions) is an instance of the ghost-discipline rule the skill already carries, and that rule *worked*: it is what put the version check before the code read. Next: row **6g**.

## STATUS 2026-08-25 — ITERATION 22 COMPLETE: **ROW 6e LANDED — AND THE TWO GUARDS WRITTEN TO CLOSE IT WERE BOTH DECORATION UNTIL A JUDGE MUTATED THEM.** Pick was the queue head, row **6e**. PR [#871](https://github.com/sunholo-data/ailang/pull/871) → [`086b72184`](https://github.com/sunholo-data/ailang/commit/086b72184), evaluator **round 1 FAIL 54/100 (3 blocking) → round 2 PASS 91/100, ZERO blocking**. Gate 3b GREEN on head `ddd8f3f09` (**21** checks, 0 pending, 0 not-green, **4/4** required — build/docs-gate/lint/test — `mergeStateStatus=CLEAN`), `mergeable` read FIRST per the iteration-198 rule and `MERGEABLE` throughout. **THE ROW'S ATTRIBUTION WAS RIGHT AND ITS COUNT WAS LOW, AND ONLY THE CI LOGS COULD SAY SO.** Row 6e recorded **two** cancellations; measured across the last **100** CI runs (oldest `2026-08-22T08:42Z`) the `launchd drivers (bash 3.2)` job is **95 success / 2 failure / 3 cancelled**, and the third — `32673098414`, `23:15:56Z` — is a **push to `dev` itself**, not a PR. All three sit at **~918s**, which is `timeout-minutes: 15` firing, so these are hangs and not flakes. The arm-33 attribution was verified rather than inherited: in all three the last suite line is byte-identical (`UNINFORMATIVE UNDER SANDBOX: loopback socket sampling yielded no peer…`) and `ok 33` never appears, and the only code between that echo and arm 33 is arm 33's own invocation — control, green run `32785167377`, emits the same line and then `ok 33` **1.06s** later with all 34 arms. **THE MECHANISM IS NOT ISOLATED AND THIS SAYS SO.** It has not recurred in the ~44 runs since, on an unchanged file (`git log` over the probe, the suite, `make/test.mk` and `ci.yml` returns nothing in that path since the last cancellation; the one commit in the window, `ba2eeb4b4`, touches a different `ci.yml` step and a different make target). A synthetic near-copy of the walk **aborts bash 3.2 with SIGABRT rc=134, 5/5**, while the shipped arm passes locally in 1.06s — so the stimulus sits near a cliff and the shipped arm's margin is unmeasured; that divergence means the synthetic repro is **not** the CI mechanism and is not claimed as it. What IS established is structural and is what shipped: **the suite had no bound of its own**, so its only bound was the job timeout and a hang was a silent 15-minute cancellation carrying zero diagnostics. **EVERY ARM NOW HAS A HARD CAP** (`ARM_CAP_SECS` 120, validated, `run_bounded` backgrounds with closed stdin, TERM then KILL after 5s, real rc captured without a pipe, on expiry a named `not ok` plus both captured tails), and `descendant_pids` is bounded by **node count** as well as by the clock, with a **distinct** message so a reader can tell which bound fired — the row's own first option, since a wall-clock bound calibrated on the author's machine is the shape it names. Refusal-branch drift gate 23 → **24**; suite 34 → **39** arms. **THE EXECUTOR CAPPED BEFORE REPORTING, SO THE ENTIRE MUTATION DRILL IS FIRST-PARTY.** `codex:gpt-5.6-sol` hit the 30-minute wall (**FLAGGED**) with `.snap/M1`–`M3` complete; the work was VERIFIED, not adopted. Four mutants, each `if false && …` so it still parses, each asserted LANDED by sha256 and VALID by `bash -n` **before** its result was read, each restored byte-identical: M1 cap expiry, `ARM_CAP_SECS` validation, M2 node ceiling (run at `PROBE_TIMEOUT_SECS=60` and **not** falling through to the clock bound's message, which is what proves the arm discriminates), and the `PROBE_MAX_TREE_NODES` validation neutered **while keeping its message** so the branch-count gate could not be the killer. **A RED BANKED FOR THE WRONG REASON, CAUGHT BY THE RULE THAT EXISTS FOR IT:** the first batch run of the M1 mutant redded at **arm 30**, not at the cap arm; re-run isolated it reds at its own arm **2/2** with the unmutated suite green **3/3** in the same block. A second identical shape appeared at arm 32 (rc=1 then rc=0 on the immediate re-run). Read the exit code alone and this iteration banks a pin that does not exist. **THE JUDGE FOUND WHAT THE DRILL DID NOT, AND TWO OF THE THREE ARE DEFECTS IN MY OWN GUARDS.** Round 1 **FAIL 54/100**: **(B1)** `report_arm_cap` — the function implementing M1's headline promise — had **zero coverage**; arm 34 calls `run_bounded` directly and stops there, and neutering its `exit 1` left the suite green at 38/38. **(B2)** the cap arm asserted only `cap_rc == 199` and an upper elapsed bound, so a fixture exiting 199 of its own accord passed with **no TERM and no KILL**. **(B3)** my own PR body claimed *"fast arms are unaffected — the poll loop is not entered when the child has already exited"*, and the judge **measured it false**: the suite went **30s → 66-93s**, because the first `kill -0` runs after a `date` subshell so anything slower than a trivial binary pays a flat mandatory second. All three reproduced first-party before acting. **AND THE FIRST FIX FOR B1 PASSED FOR THE WRONG REASON — ITS OWN MUTANT IS WHAT SHOWED IT.** With `exit 1` removed, `expect_failure` falls through to its `lacked expected message` refusal and **still exits 1 with every marker present**, so rc=1-plus-markers does not discriminate; the arm now requires that fall-through message to be **ABSENT**, which is the only observable unique to `report_arm_cap` having ended the arm. Proven with a **line-range-scoped** mutation (the first attempt was an unscoped `s/^  exit 1$/` that hit the validation's `exit 1` too and redded at the wrong arm — the same lesson twice in one iteration). B3 answered by backing the poll off `0.05 → 0.2 → 1s` and by **correcting the claim in the PR body rather than leaving it standing**. Round 2 **PASS 91/100, ZERO blocking**, and the judge re-derived the round-1 table itself, reproducing two rows at **byte-identical** sha256 — the controller's evidence is corroborated, not taken on trust. Timing re-measured by the judge: pre-PR **29.89s** / flat-sleep **66.61s** / shipped **44.98-45.07s**, `make test-launchd-drivers` **70-73s**. **ONE NON-BLOCKING FINDING ROUTED TO THE QUEUE RATHER THAN INTO THIS PR** (row **6g**): `run_bounded`'s cap kills the recorded wrapper PID, not the process **group**, so a `sleep 30` fixture is reparented to `PPID 1` and survives — the suite's own "process survived" check passes because the wrapper really is dead. Pre-existing since M1 and present in production `run_lane` too, so it is a defect the doc did not introduce and it belongs on the queue, not in a growing PR. Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, ran from the pin root detached and clean at `b28800ddf` == `origin/dev`; running-skill check on the **RESOLVED** symlink target (it resolves to **V1's** checkout, inode `49847168` vs this pin's `49847664`) — byte-identical to origin. dev at pick time: HEAD's `test` was mid-flight, **0** not-green of 14, parent control **16**. **0** human directives on `#850` since the watermark (of **4** comments); `#743` re-read for the rotation-week catch, **0**. Ledger valid, **1 OPEN** (`D-MOTOKO-WORKDIR-2`, unanswered). Inbox **1** unread, informational. External predicates re-run as commands: Phase-0 **G1 `#154` still OPEN** with control `#175` **MERGED**, so rows 10/11/12 stay parked. No died-mid-flight traces: `#695` has no branch in this clone's worktree list and is **not attributable to this mission** — left alone per the fleet-filter rule. Weekly sweep and rotation **both not due** (`#850` created `2026-08-24T07:39:32Z`, after the Monday-07:00 **local** boundary; 4 comments < 80). Source clone re-measured at **24 behind / 0 ahead**, clean — just under the notice threshold, exactly as `D-MOTOKO-WORKDIR-2` predicted. Routing: controller `claude:claude-opus-5`; executor `codex:gpt-5.6-sol` (probe rc=0, replied `ok`) **CAPPED at 30 min, FLAGGED**; evaluator **sonnet** in **its own worktree**, two rounds — distinct provider from the executor, so generator≠judge holds there, and **FLAGGED**: the mutation drill and every round-1 fix are Anthropic-authored and Anthropic-judged, which is why the judge was pointed at them by name. No planner, no designer, no quorum — the row specifies its own scope and the parent doc's quorum is spent. Rotation pointer untouched at `claude:claude-fable-5`; Fable **unspent**. Metered **$0.00** of $5 — codex and sonnet are quota buckets. No GPU, no `rig.lock`. Gates on **darwin/arm64** only; windows and ubuntu legs unrun locally and read from Gate 3b's matrix. Gate-5: **NO skill edit** — this iteration's two frictions (an unscoped `sed` mutation killing the wrong arm; an assertion satisfied by a fall-through) are instances of rules the skill ALREADY has (3j's corollary *read WHICH test failed*, and 3i's *what else writes this value*), so they go to the log's Ruled-out rather than into the rulebook. Next: row **6f**.

## STATUS 2026-08-24 — ITERATION 21 COMPLETE: **THE RECONCILE THE SKILL CALLS A HUMAN CALL HAD ZERO AHEAD-COMMITS TO LOSE — AND THE PREDICATE THAT MADE IT ONE CANNOT TELL "YOUR WORK WOULD BE LOST" FROM "YOUR WORK IS ALREADY UPSTREAM".** Pick was **NOT the queue head**: Mark answered `D-MOTOKO-WORKDIR-1` on `#743` at `2026-08-23T18:59:43Z` with one word, **"Yes"**, 42 minutes after iteration 20 asked it — and Gate 0's contract makes an allowlisted answer to a parked item unpark it and *become* the pick, outranking row 6e. Iteration 20's drift notice fired in the same window at **178 behind** (up from 170): the doubling-dedupe working, not a second defect. **NUMBERS RE-MEASURED FIRST-PARTY, NOT INHERITED — 178 COMMITS HAD LANDED SINCE THE ROW WAS WRITTEN, AND THE RE-MEASURE CHANGED THE VERDICT'S BASIS.** Ahead-commits: **0**, so Gate 1's reconcile obligation 1 holds **vacuously** — strictly stronger than the `git patch-id --stable` comparison it prescribes, and not knowable from the row. Obligation 2 fails **7 of 7** (`comm -12` intersection = 7; positive control `CHANGELOG.md` = **1**, negative control = **0**, so the instrument fires in both directions). Supersession re-derived: **132 of 136** added lines byte-present on `origin/dev`; the **4** absent are decision-ledger prose origin carries in a superseding form (`git grep` positive and negative controls both fired). The two untracked files **split, and only measuring them showed it**: `scripts/mission_decisions.sh` is **byte-identical** to origin's, while `tools/launchd/test_mission_routing.sh` is superseded **and would actively regress** — the local copy asserts the executor fallback still carries `:floor`, which origin deliberately dropped on 2026-08-18 with the rationale in a comment above the assertion. Discarding it removes a red, not a fix. **THE PRESCRIBED COMMAND WAS RUN FIRST AND ITS REFUSAL RECORDED AS EVIDENCE.** `git checkout -B dev origin/dev` returned **rc=1** naming all 7 files and left the tree **byte-unchanged** — that refusal is precisely what distinguishes this operation from the `reset --hard` Critical Principle 0 forbids, and it is worth a measurement rather than a route-around. Then, under Mark's authorization: backup of all 7 files plus the full `git diff` patch to `~/.ailang/backups/motoko-clone-reconcile-2026-08-24`, sha256-manifested and verified byte-identical **with a corruption negative control that fired** (append a byte → verifier reds → restore → verifier greens); `git checkout origin/dev -- <5 tracked>`; `rm` of the 2 untracked; `checkout -B` retried → `Reset branch 'dev'`. **VERIFIED, NOT ASSUMED:** behind **0** / ahead **0**, HEAD == `origin/dev` == `e3ed9467f`, `git status --porcelain` **0 lines**, `SKILL.md` **3682** lines byte-identical to the pin's copy (negative control vs `CLAUDE.md` fired), charter byte-identical to the pin's, **all 8 worktrees intact** — including `.wt-motoko-iter8-fmt`, which holds this mission's only quorum artifacts in a gitignored directory — and the backup re-verified **OK on all 7 files after** the operation. **THE NOTICE RE-ARMS ITSELF AND THAT WAS PROVED, NOT ASSERTED:** `mission-control.sh` removes `$PIN_DRIFT_FILE` below `AILANG_DRIVER_DRIFT_WARN` (25); the real branch was extracted and driven three ways — drift **0** → removed ("re-armed"), drift **178** unchanged → deduped and file kept (control fires), drift **356** → EMIT. Live drift is now **0**. **THE WEEKLY SWEEP'S FIRST ANSWER WAS AN ARTIFACT OF ITS CORPUS, AND THE RULE'S OWN PRECEDENT SAYS SO.** Against motoko's four docs the orphan count is **67 of 76**; this repo's issue queue is **shared with V1**, and iteration 170's instance of this rule measured *"zero mentions across ALL FOUR mission docs"*. Re-swept across all **9** mission docs the real count is **8 of 76** (enumeration asserted against `gh issue list … | wc` = 76; positive control `#558` = **57** hits; negative control fired, literal not published). Filed as row **6f**: **2 are motoko's** — `#842`, *provider failure masked as successful empty completion*, which is this mission's own harness false-green class and sits under the *never conclude model wall* guardrail, and `#839` — and **6 are not**, handed to V1 on the cross-mission channel and **explicitly not triaged here**. All 8 internally authored, so no external-origin class applies. **ROUTING: NO SPRINT.** No designer, planner, executor or evaluator was spawned — the pick is a human-authorized ops action whose procedure Gate 1 writes out step-by-step with machine-checkable postconditions, so adding a judge would have given it nothing to judge. Rotation pointer untouched at `claude:claude-fable-5`; Fable unspent; metered **$0.00** of $5. Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, ran from the pin root detached and clean at `e3ed9467f` == `origin/dev`; running-skill check done on the **RESOLVED** symlink target, which resolves to **V1's** checkout and is byte-identical to origin. dev **verified green, not merely un-red**: **20** exact-SHA checks, **0** not-green, `runs_total=3` so a run exists, parent control **16**. Ledger valid at **4** rows, **0 OPEN**. Inbox: **1** unread, this loop's own drift notice. Bookkeeping thread **ROTATED** (`#743` created `2026-08-17T05:48:23Z` = 07:48 **local**, before today's Monday-07:00 **local** boundary — the timezone is load-bearing). Gate-5: **NO skill edit** — both frictions are instances of shapes the skill already names, pre-registered as instance 1 rather than spent on one datapoint. Next: row **6e**.

## STATUS 2026-08-23 — ITERATION 20 COMPLETE: **ROW 6d LANDED — AND THE REASON THE CLONE REACHED 170 COMMITS STALE UNNOTICED IS A DOCUMENTED ASSUMPTION THAT HAS GONE FALSE, NOT AN OVERSIGHT.** Pick was the queue head, row **6d**. PR [#838](https://github.com/sunholo-data/ailang/pull/838) → [`50460040c`](https://github.com/sunholo-data/ailang/commit/50460040c), evaluator **PASS 90/100, ZERO blocking**; three of five non-blocking findings answered in-iteration, two accepted as reported and said so. Gate 3b GREEN on head `15b89569d` (**21** checks, 0 pending, 0 not-green, 4/4 required, `CLEAN`). **THE ROW ASKED FOR A DECISION AND THE MEASUREMENT FOUND A MECHANISM.** `pin-root.sh` re-execs every driver into a worktree pinned to `origin/dev`, so the driver, skill and charter move together — and it leaves the SOURCE CLONE behind, while `mission-control.sh` posts to the human channel only when `PIN_STATUS=STALE`. The comment above that emit block states the reason in full: *"The shared clone being behind is not itself reportable — once drivers pin, that drift is harmless, and posting it every 90 minutes would train the channel to be ignored"*. **Half of that is true and half is measured FALSE.** Drift is harmless to the DRIVER, whose pin holds; it is not harmless to a HUMAN SESSION, because this charter named that clone the working checkout and a session started there resolves ITS `.claude/skills/`. The driver log carried the growth and nothing else did — **119 → 132 → 144 → 159 → 170** over five fires, on the SUCCESS path, in a log nobody reads. That is Critical Principle 2 aimed at the very helper written to close it: **guard the helper, miss the call site**, this loop's own named recurring shape. Fix keeps the true half — notify on crossing `AILANG_DRIVER_DRIFT_WARN` (default 25), thereafter only on a **doubling**, persisted in a per-mission state file removed below threshold so it re-arms after a reconcile. **THE CONTROLLER CAUGHT THE EXECUTOR'S NOTICE NAMING THE WRONG PATH, AND THE PATH IT NAMED IS SELF-REFUTING.** The body used `$REPO`; on the pinned pass `pin-root.sh` has already exported `MISSION_WORKDIR=<pin worktree>` and `mission-control.sh:40` derives `REPO` from it — so the notice would have told a human to reconcile a detached throwaway **whose drift is 0 by construction**. Verified from this session's own live environment: `MISSION_WORKDIR=~/.ailang-driver-pin/motoko`, `AILANG_DRIVER_SRC=~/dev/sunholo-data/ailang-motoko@170`. The pre-existing STALE body's `$REPO` is CORRECT — that arm fires only on the pre-exec pass — which the evaluator confirmed independently, so it is one arm right and one arm wrong in the same block. Suite **17 → 27** arms, awk-extracted from the real blocks; six mutants, each LANDED by sha256 and `bash -n` rc=0, red sets **produced by running them** (the previous message asserted a set nobody had run — the measured neutering set is **7** arms, not 4). **GATE 3b's OWN INSTRUMENT NEARLY MIS-ATTRIBUTED THIS:** `launchd drivers (bash 3.2)` came back **cancelled after 15m18s** against a **~68s** success on dev's own HEAD and on the other 18 of the last 20 CI runs — a red in a direction nothing in the diff predicts. Separated by rule 3d's strongest control, a re-run on a **byte-identical tree**: attempt 2 **success in 88s**. The diff touches no file the probe suite reads. The hang is in iteration 19's own arm 33 (*descendant discovery refuses on the real wall-clock deadline*), first observation, filed as row **6e** rather than declared a flake class off one instance. **PARKED FOR MARK: `D-MOTOKO-WORKDIR-1`** — the reconcile itself, which Gate 1's obligation 2 and `pin-root.sh`'s own header both make a human call. Phase-0 re-measured this iteration and still CLOSED: G1 `#154` OPEN (control `#175` MERGED), G2 rc=128 with its README control rc=0, G3 registry `latest=2.2.0`. Next: row **7**.

## STATUS 2026-08-22 — ITERATION 18 COMPLETE: **THE M2 INSTRUMENT LANDS AND ITS OWN LIVE SWEEP CAME BACK *VOID* — WHICH IS THE DESIGN DOC'S §12.4 RULE WORKING, NOT A FAILED MILESTONE.** Pick was queue row **6**'s named resume point, milestone **M2 (`AC-D1-live`)** — the last of five and the only one needing the rig. **M2 does NOT close**, and the row keeps it as the resume point. PR [#829](https://github.com/sunholo-data/ailang/pull/829) (`3e446f8c7` + `627c67d2d`), evaluator **FAIL 58/100 with TWO blocking findings**, one fixed here and one filed. **THE INSTRUMENT REFUSED TO CERTIFY, WHICH IS WHAT IT IS FOR.** Run under `rig.lock`, both lanes returned `driver_rc=1` with peer set `[]` in **8m15s / 8m17s**; `AC-M2-control` says a control that does not fire makes `AC-M2-treatment` **VOID — the probe proved nothing**, so the verdict is VOID rather than FAIL and the probe exited 1 on `INSTRUMENT FAILURE: empty peer set; absence of evidence cannot prove routing`. `OR_IPS` resolved and recorded (`104.18.2.115`, `104.18.3.115`); the artifact is written **before** the verdict is evaluated, so the evidence survived the refusal. **"THE RUNS NEVER CONNECTED" AND "THE SAMPLER IS BLIND" ARE THE SAME EMPTY OUTPUT, SO THEY WERE MEASURED APART** (rows **V36–V38**): scoped `lsof` **does** see an ESTABLISHED peer of a child process here — the probe's own command shape returned `curl … TCP 127.0.0.1:49914->127.0.0.1:11434 (ESTABLISHED)` against an unscoped same-call control of **67** lines — and the treatment lane is `rc=0` standalone (2/2, 1m53s) **and** `rc=0` under a faithful replication of the probe's own `run_lane` shape (2/2, 1m1s, **244** `lsof` lines) with **`127.0.0.1:11434` PRESENT**. So the required observable is reachable and the probe **as shipped** is what breaks the runs it observes. **MECHANISM NOT ISOLATED, AND SAID SO**: it lies in what the replication did not reproduce — the deadline-carrying `descendant_pids`, the `classify_lsof` pipeline, or the two-lanes-in-one-process sequencing; the evaluator ruled out `classify_lsof` hermetically (it is a pure post-hoc transform called after `wait`, so it cannot affect the driver's exit code) and could isolate no further without the rig. The probe **discards both driver logs** via its `trap … EXIT`, which is why the first diagnosis needed a re-run — for a lane that exits non-zero that log is the whole diagnostic, and keeping it is the named first fix. **MY OWN CONTROL WAS STALE AND I CORRECTED IT RATHER THAN BANKING IT:** the first `lsof` reading returned 0 lines and was taken after the holder process had already exited; re-taken against a holder asserted ALIVE (`kill -0`) and streaming (95,869 B), it returns the connection — V1 iteration 247's stale-artifact class, met first-party in my own instrument. **EVALUATOR B1 — A FALSE NEGATIVE IN EXACTLY THE HALF OF THE CRITERION THAT MUST NOT FAIL, FOUND BY THE JUDGE AND REPRODUCED BEFORE ACTING** (row **V39**): `dig +short openrouter.ai` returns **A records only** while openrouter.ai has genuine **AAAA** records (`2606:4700::6812:373`, `:273`), and `lsof` brackets an IPv6 peer where `dig` emits it bare, so `grep -Fqx` could never match — measured, a v6 OpenRouter peer classified **`other`** while the IPv4 positive control in the same call classified **`openrouter`**. The probe would have certified *"zero connections to openrouter.ai"* for a run that leaked over IPv6. Fixed by unioning A+AAAA (with `+time=5 +tries=2`, which also bounds the one wait that had no explicit bound — the judge's N2) and comparing on the bracket-stripped host; re-measured in **four** directions (v6 OR → `openrouter`, IPv4 OR → `openrouter`, non-OR v6 → **`other`** so it does not over-match, loopback → `loopback`). **GATED, NOT MERELY FIXED:** a new arm asserts a v6 leak is refused and reverting the normalisation **reds** it (`missing openrouter [2001:db8::8]:443`) — mutant LANDED (sha256 differs), VALID (`bash -n` rc=0), restore byte-identical from a `cp` backup, suite **8/8** green again. The fixture already listed `2001:db8::8` in `OR_IPS` with nothing pointing at it, which is precisely why the path was never exercised. **EVALUATOR B2 — FILED, NOT PATCHED:** the self-test covers `classify_lsof` and the four `assert_*` front doors only; ~**15** refusal branches in the live path have zero coverage, demonstrated by a neutered darwin/arm64 gate surviving with a byte-identical `PASS: 7`. That is the same territory as the unisolated V38 defect, so it belongs to the iteration that isolates it — new queue row **6c**. **THE EXECUTOR SELF-REPORTED A SURVIVING MUTANT** (`assert_control`'s OR-membership branch, as first delivered) and repaired the harness so `expect_success` inspects positive-arm exit codes — a self-reported finding, better evidence than a silent run (rule 3h(d)); **verified first-party rather than banked**: mutant LANDED, VALID, killed arm 3 with `rc=1` against a baseline of `rc=0` / 7 arms, restore byte-identical. **GATE LIST BASELINED BEFORE THE DIRECTIVE WAS WRITTEN** (recipe false-green (4)): all seven rc=0 on the unmodified tree, and `go build ./...` deliberately excluded because it is **red at base** (`cmd/wasm` has no native `main`) — the finding iterations 145 and 245 already recorded. Gates re-run by the controller **outside** the executor sandbox with an explicitly built absolute-path binary (`/tmp/ailang_i18/ailang`, `git describe v0.33.1-201-gb59255831`) prepended to `PATH`; `make quick-install` deliberately NOT run (shared-write guardrail). Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, ran from the `#558` driver pin root detached and clean at `b59255831` == `origin/dev` at start; **running-skill check done on the RESOLVED symlink target** (V1 iteration 241's rule) — `~/.claude/skills/mission-control` resolves to V1's main checkout and is **byte-identical to origin** (`cmp` rc=0), as is this pin's own copy, negative control (origin skill vs `CLAUDE.md`) rc=1. dev **verified green, not merely un-red** — **16** exact-SHA checks, **0** not-green, `runs_total=2` so a run exists, parent control **16**. **0** human directives on `#743` since the watermark `2026-08-17T05:48:45Z` (of **16** comments); ledger valid at **3** rows, **0 OPEN**; inbox **1** unread, a `mission-v1` iteration-249 report carrying no request or bug against motoko — read, never obeyed; **0** open `[nightly-eval]` alarms (control: **30** closed ones exist). External-predicate re-read of the Phase-0 rows run as commands with controls: G1 `#154` `state=OPEN`/`mergedAt=-` (control `#161` MERGED with non-null `mergedAt`), G2 rc=**128** with the mandatory `README.md` control rc=**0**, G3 `latest=2.2.0`, versions 1.0.0–2.2.0, no 5.x, G4 unrunnable while G3 is FALSE, G5 outstanding — rows 10/11/12 stay parked, Phase 0 CLOSED. **A SCOPE ERROR OF MY OWN, CAUGHT BY WIDENING:** the doc's §11 cites its two quorum artifacts by a *repo-relative* path, and searching the pin, the motoko checkout, V1's checkout and `$HOME` returned **0** with controls firing at 4 and 95 — the artifacts live in `.wt-motoko-iter8-fmt/.ailang/state/`, a gitignored per-worktree directory, and only enumerating **every** worktree found them. Verified first-party once found: **2** rounds, both reviewers `present: true`, `absent_reviewers` **empty** in both, both `blocked` — spent, not passed, exactly as the charter records. No died-mid-flight traces: the only open PR on this account is `#695`, a stale coordinator PR with no worktree in this clone and therefore **not attributable to motoko** — left alone per the fleet-filter rule. Weekly sweep and rotation **both not due** (`#743` created `2026-08-17T05:48:23Z` = 07:48 local, AFTER the Monday-07:00 local boundary; 16 comments < 80). AC-DEPLOY re-read with a no-pipe control (`rc_control=1` vs `rc_real=0`, arms asserted to differ): the installed plist still runs `nightly-eval.sh` from **V1's** checkout, so `#558` stands unchanged. Routing: controller `claude:claude-opus-5`; executor `codex:gpt-5.6-sol` (probe rc=0, replied `ok`, 360 tokens); evaluator **sonnet** — distinct provider from the executor, so generator≠judge holds. **No planner** (the plan specifies M2 in full), **no designer, no quorum** (the doc's quorum is spent). Rotation pointer untouched at `claude:claude-fable-5`. Metered **$0.00** of $5 — both lanes are quota buckets, and the OpenRouter control leg billed nothing because the run exited before connecting. GPU: `rig.lock` acquired `nowait` (bounded — the helper's `wait` mode is an unbounded `sleep 30` loop, Standing rule 6) and released, three times. Gates on **darwin/arm64 only** except where Gate 3b's matrix is cited (rule 3b(viii)). Gate-5: **NO skill edit** — this iteration's frictions (a stale control artifact; a relative path that resolves only where it was written) are instances of rules the skill ALREADY has (V1 iteration 247's freshness rule; the Repo Profile's *a relative path is a claim about where you are standing*), so they go to the log's Ruled-out rather than into the rulebook.

## STATUS 2026-08-21 — ITERATION 17 COMPLETE: **M3 + M5 LANDED — BUT ITERATION 16 IS WHO BUILT THEM, AND IT DIED WITH THE WORK FINISHED, REVIEWED AND ONE COMMAND SHORT OF LANDING.** Pick was **not** the queue head and not a milestone: Gate 2's died-mid-flight traces found **iteration 16's entire inner loop complete and unrecorded**, so the deliverable was to VERIFY AND LAND it, not to redo it. **Iteration 16 fired 08:02:25 and was killed at 09:00:33 CEST by the driver's STALL watchdog** — `claude 94489 idle with a descendant alive ≥2400s across 3 samples (unbounded poll loop?)`, `rc=143`. It left **zero** charter rows, **zero** log entries, **zero** STATUS stamps (`grep -ci "ITERATION 16"` = **0** in charter and log; control `ITERATION 15` = **3**), so the charter read `[NEXT]` for two milestones that were built, self-reviewed and sitting in an open PR. Its traces were exactly the three Gate 2 names: PR [#813](https://github.com/sunholo-data/ailang/pull/813) from this account, worktrees `.wt-motoko-iter16-d1b` **and** `.wt-motoko-iter16-eval` (so an evaluator had been spawned), and `.snap/` — the codex executor's per-milestone snapshots — as the only uncommitted residue. The sprint JSON already read *"M3 + M5 delivered in iteration 16"*, which is the record that survived. **THE WATCHDOG IS THE SAFETY NET WORKING, AND LOUDLY:** Standing rule 7 is written about the *silent* 600 s reap that exits `rc=0`; this rig's driver instead detected the idle-with-live-descendant shape, killed early, exited **143**, and posted the failure to `#743` at `07:00:35Z` — so the human channel saw it the same hour. The loss was one landing step, not a slot's work. **VERIFIED FIRST-PARTY, NOT ADOPTED** (nobody had reviewed that work since the agent which wrote it stopped existing). Rebased onto `origin/dev`: `make/test.mk` auto-merged (the two hunks are 215 lines apart), `changelogs/v0.18-current.md` conflicted **twice** — the **fifth** consecutive iteration where that file is the cross-mission collision surface, V1's iteration 245/246 this time — both resolved keeping BOTH sides, verified by a 4-of-4 entry presence check with a firing marker control and a fresh negative literal, and the rebased diff re-derived at **+407/-27 across 7 files**, identical to the pre-rebase PR. **MUTATION DRILL RUN AGAINST THE INHERITED CLAIMS, FOUR MUTANTS, EACH ASSERTED LANDED (sha256 ≠ pre) AND VALID (`bash -n` rc=0) BEFORE ITS RESULT WAS READ.** The PR claims all three smoke refusal branches are pinned; measured, each is killed by its **own named arm** and only that arm: neutering `banked-row` → `FAIL: no-banked-row smoke` (count=12, want 0); `fmt_hook_state` → `FAIL: failing smoke`; `treatment-integrity` → `FAIL: invalid-treatment smoke`; baseline rc=0, restore byte-identical by sha256 (`cp` backup, never `git checkout --`). The M3 half too: ON-always-leads reds `counterbalanced sequence` in the shell harness **and** — the load-bearing one, since AC-M3-4's whole point is that the analyzer is the discriminator — makes `TestFmtDriverScheduleSatisfiesOrderIntegrity` FAIL at `censored_test.go:100: CheckFmtOrderIntegrity rejected shell artifact: order_integrity_lead_not_alternating`, with `tests_run=1` printed on both arms so the green is not a zero. That also confirms iteration 16's `go test` cache-key fix: the Go test really does see a `/bin/bash` child's mutation. **MY OWN VACUOUS PASS, CAUGHT BY THE COUNT AND NOT BY THE EXIT CODE:** the first attempt at that drill used `-run 'TestFmtABScheduleOrderIntegrity|TestFmtAB'`, which matches **no test in the package** — both arms returned `rc=0` and the mutant read as *survived*. Only `ok … [no tests to run]` and an explicit `=== RUN` count exposed it; re-run against the real name, the arms are 0 and 1. Rule 3a(i) working on the controller's own instrument, and a reminder that a `-run` filter is an enumerator (rule 3a(i-e)). **GATE LIST DERIVED, NOT RECALLED** (rule 3g): read from `ci.yml`'s own `run:` lines rather than from the PR body, which surfaced **two gates dev added since iteration 16 branched** — `make test-check-autoclose` and the exact-count `make test-stdlib-ail` — both green on the merged `make/test.mk` (4 suites / 4 fixtures). Full sweep, all rc=0 on the rebased tree: `bash -n` ×2 · `shellcheck -S warning` (0 findings) · `make test-launchd-drivers` (**10 passed / 0 failed** — item 5b's fix holds on the rig) · `test_fmt_ab_schedule.sh` (11 PASS) · `go build ./internal/... ./cmd/ailang/...` · `go test -count=1 ./internal/eval_analysis/...` · `check-file-sizes` · `check-boundaries` · `check-changelog` · `test-check-changelog` · `test-check-autoclose` · `check-skills` · `fmt-check` · `vet` · `test-stdlib-ail`. Every binary invocation used an explicitly built absolute path (`/tmp/ailang_i17/ailang`, `git describe v0.33.1-191-gaa4543ba4`) prepended to `PATH`, never the shared `~/go/bin` copy (rule from V1 iteration 237); `make quick-install` deliberately NOT run (shared-write guardrail). **LANDED GREEN, OBSERVED NOT PREDICTED**: head `aa4543ba4` — **21** checks, **0** pending, **0** not-green, **4/4** required contexts (`build`/`docs-gate`/`lint`/`test`) pass, `mergeStateStatus=CLEAN` — then squash-merged as [`b2733201a`](https://github.com/sunholo-data/ailang/commit/b2733201a). The PR read `CONFLICTING`/`DIRTY` at pick time and `mergeable` was checked FIRST per the iteration-198 rule, so the boring cause was established in one call and no dropped-event lever was reached for. Autoclose scan run on both commit messages and the PR title/body before pushing: **0** hits, with a known-bad control string matching **1**, so the clean read is a measurement. **QUEUE ITEM 6 IS NOW M1+M3+M4+M5 LANDED; ONLY M2 (`AC-D1-live`) REMAINS, AND IT NEEDS THE RIG** — the first time this row's resume point is a single milestone, and it is the one that cannot run without `rig.lock`. Doc §6's deployment precondition (`#558`) still stands and is unchanged by this landing: merging to `dev` does not put D1b or the smoke gate on the rig, because the installed plist runs `nightly-eval.sh` in place from V1's checkout. Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, ran from the `#558` driver pin root detached and clean at `8040dfd41` == `origin/dev` at start; **running-skill check done on the RESOLVED path, both of them** (V1 iteration 241's rule) — the file this session actually read is the pin worktree's own copy (inode `46496692`), `cmp` vs origin rc=**0**, while `~/.claude/skills/mission-control` resolves to V1's main checkout (inode `45241676`), which is **1 commit ahead of origin with an unpushed Gate-5 edit** and therefore differs by 3,074 B — V1's divergence, recorded here rather than acted on; negative control (origin skill vs the charter) rc=1. dev **verified green, not merely un-red** — **16** exact-SHA checks, **0** not-green, `runs_total=2` so a run exists, parent control **16**. **0** human directives on `#743` since the watermark `2026-08-17T05:48:45Z` — corroborated by a raw author enumeration showing all **15** comments are the bot's, with the script's positive control firing on `#745` (Mark's `D-19 : B`); ledger valid at **3** rows, **0 OPEN**; inbox **0** unread; **0** open `[nightly-eval]` alarms (control: 3 closed ones exist). Weekly sweep and rotation **both not due** (`#743` created `2026-08-17T05:48:23Z` = 07:48 local, AFTER the Monday-07:00 local boundary; 15 comments < 80). Routing: controller `claude:claude-opus-5` only; **no designer, planner, executor, evaluator or quorum spawned** — verifying and landing a predecessor's finished work has no doc to design, no plan to write, and a judge would be adjudicating an artifact whose author no longer exists; the executor credit stays with iteration 16's `codex:gpt-5.6-sol`, preserved in both commit trailers. Rotation pointer untouched at `claude:claude-fable-5`. Metered **$0.00** of $5. No GPU, no `rig.lock`. Gates on **darwin/arm64 only** except where Gate 3b's matrix is cited (rule 3b(viii)). Gate-5: **ONE skill edit** — Gate 2's died-mid-flight trace (a), because `--author` is a FLEET filter on a shared repo and the charter+log carry **20** occurrences of hand-disambiguating *"is V1's"*; this iteration is the first where that filter returned a genuinely-mine PR beside a sibling's **live** one (`#818`, opened 20 minutes earlier).

## STATUS 2026-08-20 — ITERATION 15 COMPLETE: **M4 LANDED — AND THE INTEGRITY GATE THIS MILESTONE EXISTS TO BUILD WAS UNREACHABLE FROM ITS OWN CLI, TWICE, IN OPPOSITE DIRECTIONS.** Pick was queue item **6**'s named resume point, milestone **M4** (D2 censored-pair analyzer) — chosen over M2/M3/M5 because the plan declares it *parallel-safe with M1–M3*, it is the only remaining milestone with no rig dependency, and **M3's AC-M3-4 calls M4's order-integrity checker**, so M4-first is the ordering that leaves no milestone with an unclosable AC. **M2, M3, M5 remain the resume point.** PR [#806](https://github.com/sunholo-data/ailang/pull/806) → `d5bcfa0c8`, evaluator **FAIL 80/100 with one BLOCKING finding — against the controller's own repair** — fixed, re-verified and landed. `ailang eval-censored-pairs` is a **sibling** of `eval-paired`, never an extension: `eval_paired.go` is byte-identical to base (control: a file we DID change differs) because its stdout JSON has two live callers (`nightly-eval.sh:339` microRAG, `:515` fmt). **FINDING 1 — the guard was written and its CALL SITE loaded past it.** The command loaded arms via `LoadArmForPairing`, which wraps `FilterValidResults` and drops invalid rows **at load time**; the `>20% of ON rows quarantined → VOID` gate counts exactly those rows, so through the CLI it could never fire, while the analyzer's unit tests passed regardless by constructing rows directly. Measured, both arms, control firing: filtering loader **5** ON rows / **0** quarantined visible; raw loader **6** / **1**. Sharper still, **AC-M4-5's verdict was right for the wrong reason**: the AC is cited as *"a real dataset whose correct verdict is known in advance"*, and the delivered code returned VOID `order_integrity_unpaired_block` — an artifact of the **odd row count** left by the silent drop — where the data's actual defect is whole-arm blocking, `order_integrity_nonadjacent_arms`, which is what it now returns. This repo's named recurring shape (*guard the helper, miss the call site*) reproduced inside the milestone whose subject is two integrity gates, for the **second consecutive iteration** (iteration 14's was the same shape). **FINDING 2, AND IT IS AGAINST ME — the repair traded one defect for another, and only the OFF arm paid.** `AnalyzeCensoredPairs` filtered only ON into `validOn`; that was invisible while the filtering loader dropped invalid rows from **both** arms, and my switch to the raw loader removed the protection for OFF with no compensating filter. The treatment gate scans OFF rows only for CONTAMINATION (`FmtHookEvents`), so an OFF row invalid for any other reason (`harness_error`, `config_mismatch`) reached `PairArms` and the win tally. Reproduced first-party before acting (rule: reproduce a judge's finding): all-valid arms give `off_wins=1 both_pass=2`; marking that same decisive row `Validity{Valid:false}` gives **`off_wins=1 both_pass=2`** — identical, a non-measurement deciding a §7 verdict. Fixed with `partitionMeasurements` on BOTH arms before any statistic, gates keeping the raw slices deliberately (executed order is a fact about the run; the quarantine RATE is defined over the banked set), `off_quarantined`/`off_rows` now reported. **THE EVALUATOR EARNED ITS SPAWN BY BEING POINTED AT ME**: the directive named my repair as a target and said it had had no independent review — and that is the finding it returned. **MUTATION DISCIPLINE, INCLUDING ITS OWN FAILURE:** the first drill on the repair **did not build** (`validOff` went unused) — the "a mutation red counts only when the mutant BUILDS" class, caught rather than banked — redone with the binding kept: mutant LANDED (grep=1), BUILDS scoped (rc=0), `TestD2InvalidOffRowNeverScores` alone rc=1 on `off_wins = 1, want 0` and `both_pass_pairs = 2, want 1`, inverse with that one row `-skip`ped **rc=0** → sole killer, green exactly as delivered. Same shape proven for `TestEvalCensoredPairsSeesQuarantinedRows`. **THREE NON-BLOCKING FINDINGS, ALL CLOSED:** `order_integrity_repeated_benchmark` is not merely untested but **unreachable** — blocks are deduplicated by (benchmark, arm), so a third block for one benchmark is always a duplicate key caught by `noncontiguous_block`, and a pair holding one of its blocks is caught by `nonadjacent_arms`; per the rule that an unreachable branch is acceptable only when DECLARED it is now declared in code and pinned by a test that fails loudly if it becomes reachable. The `>20%` boundary is pinned on both sides (1 of 5 = exactly 20% does **not** void; 1 of 4 does). And `TestCensoredVerdictMatrix` **passed with `[]` as its fixture** — zero subtests, zero assertions, one green line — now guarded. **GATE 3b CAUGHT A WINDOWS-ONLY DEFECT IN A TEST I WROTE** (rule 3b(viii), and the base arm was **green** on windows so the red was ours): the fixture filename embedded an RFC3339 timestamp, `:` is illegal in a Windows filename, `os.WriteFile` died in the test's own setup at `eval_censored_test.go:77`. Swept the milestone's other two test files for the same shape (control firing) — none. **LANDED GREEN, OBSERVED NOT PREDICTED**: head `922190dd3` **21** checks, **0** pending, **0** not-green, **4/4** required contexts pass, `mergeStateStatus=CLEAN`, `test-windows`/`Build windows-latest` both `success`, then squash-merged. The PR first read `CONFLICTING`/`DIRTY` — `mergeable` checked FIRST per the iteration-198 rule, so it was explained by the boring cause in one call; overlap was **one** file (`changelogs/v0.18-current.md`, V1's iteration 237 again — the fourth time that file has been the cross-mission collision surface), rebase, both entries kept, force-push. **SPECIFICATION GAP RESOLVED AT PICK TIME:** the plan lists `n_eff < 24` among the §7 thresholds without saying what verdict it produces, and the doc's RETIRE-at-low-`n_eff` is conditioned on *"after two slots plus the authorized third"* — state a directory analyzer cannot observe; so `n_eff < 24` returns INCONCLUSIVE/`insufficient-neff` and the slot-budget call stays with the driver and the human. **ANOTHER CONTAMINATED CONTROL, MINE, CAUGHT:** the first AC-M4-3 run built BOTH "before" and "after" binaries from the modified worktree — the same binary twice — and was redone against one built from a pristine `origin/dev` checkout, with a control asserting the two binaries differ (rule 3e(b)). **RULED OUT:** *the executor's `go build ./...` mutation precondition* — it is red on the **unmodified** tree (`cmd/wasm` has no native `main`), which the plan §4 already records, so the scoped build was the correct precondition and the executor self-reported this rather than hiding it (a self-reported deviation is better evidence than a silent one). *AC-M4-3 "DIFFERS"* — a stderr Observatory banner with a timestamp leaking into a `2>&1` capture, not the JSON contract; redone with stderr separated, 1185 B both sides. Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, ran from the `#558` driver pin root detached and clean at `daf881eaf` == `origin/dev` at start, running skill **byte-identical to origin** (`cmp` rc=0); dev **verified green, not merely un-red** — **16** exact-SHA checks, **0** not-green, `runs_total=2` so a run exists; **0** human directives on `#743` since the watermark (of **13** comments); ledger valid at **3** rows, **0 OPEN**; inbox **0** unread. External-predicate re-read of the blocked rows run as commands with controls: G1 `#154` `state=OPEN`/`mergedAt=-` (control `#161`/`#162` MERGED with non-null `mergedAt`), G2 rc=**128** with the mandatory `README.md` control rc=**0**, G3 registry `latest=2.2.0` no 5.x, G4 unrunnable, G5 unchanged — 10/11/12 stay parked. Row 5's upstream artifact re-read too: `#165` is OPEN, **labelled by `arniwesth` today**, and upstream's own bot reports it **implemented in PR [#166](https://github.com/arniwesth/motoko_agent/pull/166)** (`MERGEABLE`, base `main_dst`, not yet merged) — the issue we filed is being acted on. No died-mid-flight traces: `#804` is V1's iteration 237, `#695` is V1's, the four motoko worktrees are iterations 6–9 already adjudicated. Weekly sweep and rotation **both not due** (`#743` created `2026-08-17T05:48:23Z` = 07:48 local, AFTER the Monday-07:00 local boundary; 13 comments < 80). **A SKILL EDIT LANDED ON ORIGIN MID-ITERATION** — `3c4bbbda2` (#805, V1's iteration 237: *the stale binary reaches you through TESTS*) — so the copy this iteration executed is one commit behind origin by the end of it, and the delta was read before recording. It applies here and is satisfied: every load-bearing binary invocation used an explicitly built absolute path (`/tmp/ailang_TRUEbase_iter15`, `/tmp/ailang_final_iter15`), never PATH, and no test red needed attributing. Routing: controller `claude:claude-opus-5`; executor `codex:gpt-5.6-sol` (probe rc=0, replied `ok`, 20.7k tokens; the ratified primary, **not** the pi fallback iteration 14 used); evaluator **sonnet** — distinct provider from the executor, so generator≠judge holds against the executor; **FLAGGED**: the controller-authored repair is Anthropic-authored and judged by an Anthropic evaluator, same provider, different model. **No planner** — the sprint plan already specifies M4 in full, so Gate 3's "plan exists → sprint-executor" applies. **No designer, no quorum** — the doc's quorum is spent (2 rounds, both reviewers present, `absent_reviewers` **empty in both artifacts**, verified first-party rather than transcribed; note both verdicts are `blocked`, and the doc proceeds because `D-MOTOKO-FMT-1` was RESOLVED by Mark and O3/O4 were answered by measurement — *spent*, not *passed*). Rotation pointer untouched at `claude:claude-fable-5`. Metered **$0.00** of $5 (codex and sonnet are both quota lanes). No GPU, no `rig.lock`; `make quick-install` deliberately NOT run (shared-write guardrail). Gates on **darwin/arm64 only** except where Gate 3b's matrix is cited (rule 3b(viii)). Gate-5: **NO skill edit** — this iteration's two frictions (*a guard whose call site loads past it*; *a repair that fixes one arm of a symmetric pair*) are instances of rules the skill ALREADY has (3j's call-site anchoring, 3i's observable question); they were broken, not missing, so they go to the log's Ruled-out rather than into the rulebook.

## STATUS 2026-08-20 — ITERATION 14 COMPLETE: **D1 LANDED — THE PREFLIGHT THAT KILLED BOTH WEDNESDAY fmt SLOTS IS GONE, AND THE MUTATION THAT WOULD HAVE PROVED IT SURVIVED THE FIRST DELIVERY.** Pick was queue item **6** (`m-motoko-fmt-remeasurement-instrument`), un-parked at iteration 13; **M1 of 5 delivered**, PR [#794](https://github.com/sunholo-data/ailang/pull/794) → `bc0b5a8d4`, evaluator **PASS 84/100, zero blocking**. M2–M5 are the **named resume point**, not dropped. **THE PLANNER REFUTED THE DESIGN DOC'S OWN §12.2 PREFERENCE, and both halves were re-verified first-party.** Option (1) ("set `cfg.MotokoModel` from models.yml at construction") is unsound: `ExecutorFactory.GetExecutor` caches by executor **name** (`factory.go:96-122`), so ONE `*MotokoExecutor` serves all **17** `agent_cli: "motoko"` lanes — **7 ollama** (no credential) + **10** requiring one (control: **89** total `agent_cli` lines) — and one string cannot be right for both; and `HealthCheck` is **`sync.Once`-cached** (`motoko.go:134`, `healthcheck.go:40`), so a model-dependent verdict there freezes at whichever model canaried first, converting a loud uniform refusal into a silent order-dependent one. So the refusal moved to `ExecuteStreaming` — the per-task choke point — keyed on `e.getModel(task)`, expressed on the **resolved provider** (`ai.EnvVarForProvider(ai.GuessProvider(model))`, terminal `return ""` read first-party) and never on a literal env-var name, and strictly downstream of repo discovery so §12.3 holds for free. **THE FINDING: the guard was pinned and its WIRING was not.** Neutering the call site (`if err := error(nil); err != nil {`) left the **entire package green** as delivered — every arm called the helper directly, and the one `Execute`-level arm drives an `ollama/…` task the guard ADMITS, so its observable could not move. That is this repo's named recurring shape (*guard the helper, miss the call site*) reproduced **inside the milestone whose subject is a guard**. The ROW was repaired, not the code: `T-CALLSITE` asserts a refusal reaching `Execute` on the resolved provider + missing variable, plus a mock-not-invoked marker whose absence is admissible only because a control arm in the same test proves the marker fires for an admitted lane; MUT-4 now dies with it as **sole killer** (`-skip` inverse rc=0). **HONEST FULL-PACKAGE MUTATION TABLE** (the first one I published was wrong): neuter-ADMIT **12**, neuter-REFUSAL **3**, neuter-unresolvable-guard **1**, neuter-call-site **1**. My first table said 1/2/1/0 because those runs were scoped `-run 'TestRequireProviderCredential'` and I dropped the narrowing when quoting the number — **rule 3b(ii), my own error, and the evaluator caught a weaker form of it before I re-measured**. **EVALUATOR'S THREE FINDINGS, all reproduced before acting:** (a) `T-ORDER` was **decorative** — its surviving assertion was a bare nil-vs-error on `HealthCheck`, which fires identically for a check placed BEFORE the version query (violates §12.3) and AFTER it (does not), and the `MOTOKO_REPO` observable the plan calls the pin was never reached under either mutant; repaired so the ordering claim rests on `e.motokoRepo` read independently of the error, and **proven to discriminate** — before-placement fails at `execute_test.go:711` on `motokoRepo=""`, after-placement at `:718` on a different assertion; (b) the **same false claim was left one file over** — `internal/executor/motoko/README.md` still read *"motoko routes ALL models via OpenRouter"*, false for 7 of 17 lanes, i.e. CLAUDE.md rule 3 applied to the code and not the docs; (c) the stale table above. **GATE 3b CAUGHT TWO WINDOWS-ONLY DEFECTS THAT NO LOCAL COMMAND COULD** (rule 3b(viii), and the base arm was **green** on windows so the red was ours): `%q` **escapes backslashes**, so `strings.Contains(msg, dirPath)` can never match a windows path the message names correctly — now compares `strconv.Quote(dirPath)`; and a `#!/bin/bash` mock named `motoko` with no `.exe` is not executable there, so the version query never fired — skipped on windows, matching the **10** existing bash-mock skips (declared narrowing: T-B4's version-query half is darwin/posix-only). **LANDED GREEN, OBSERVED NOT PREDICTED**: PR head `fb1ef41b5` **21** checks, **0** not-green, **4/4** required contexts pass, `mergeStateStatus=CLEAN`, then squash-merged. The PR first read `CONFLICTING`/`DIRTY` — `mergeable` checked FIRST per the iteration-198 rule, so the missing `pull_request` runs were explained by the boring cause in one call rather than by a dropped-event hypothesis; the overlap was **one** file (`changelogs/v0.18-current.md`, V1's iteration 232), rebase + force-push, **5** runs within seconds. **LANE DEFECT FOUND AND FIXED: the pi executor lane had never been runnable from this mission's checkout.** The mandated sandbox extension failed to load — `Cannot find module '@anthropic-ai/sandbox-runtime'` — because `tools/pi-extensions/sandbox/node_modules` exists **only in V1's checkout**; the extension sources are byte-identical across checkouts (`cmp` rc=0, with a rc≠0 control), so it was a missing `npm ci`, not drift. Installed in the pin root; `node_modules/` is gitignored by the directory's own `.gitignore`, tree verified clean after. **RULED OUT:** *the first pi run's `agent_end=0` was a lane failure* — it was **mine**: I backgrounded the runner inside an already-backgrounded Bash call, so the harness reaped it; 425 turns all `stopReason:"stop"`, zero `"length"`, empty stderr — an external kill, not the iteration-173 output-cap stall. *The 300 MB NDJSON kill was a runaway turn* — growth had already fallen to ~93 KB/s by 268 MB and the executor had written `.snap/`, the changelog and cleaned its scratch dirs before the cap fired; it was killed during its final report turn, so the work was complete and the controller verified it independently. Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, ran from the `#558` driver pin root detached and clean at `44aa3cab4` == `origin/dev`, running skill **byte-identical to origin** (`cmp` rc=0); dev **verified green, not merely un-red** — **16** exact-SHA checks, **0** not-green; **0** human directives on `#743` since the watermark (of **12** comments); ledger valid at **3** rows, **0 OPEN**; inbox 2 unread, both sibling reports (`mission-world` 97, `mission-v1` 231) carrying no request or bug against motoko — read, acked, never obeyed. External-predicate re-read of the blocked rows run as commands with controls (G1/G2/G3 all still FALSE). No died-mid-flight traces: `#792` is V1's iteration 232, `#695`/`#613` are V1's as the last five iterations also measured. Weekly sweep and rotation **both not due** (`#743` created `2026-08-17T05:48:23Z` = 07:48 local, AFTER the Monday-07:00 local boundary; 12 comments < 80). Routing: controller `claude:claude-opus-5`; planner **opus** (`derive-planner-lane.sh` → `opus fail-closed:env-pin`, used verbatim); executor `pi:openrouter/deepseek/deepseek-v4-flash-0731`; evaluator **sonnet** — distinct provider from the executor, so generator≠judge holds. **No designer, no quorum** — the doc's own quorum is spent (2 rounds, both reviewers present, `absent_reviewers` empty) and §12 is a measurement answering a reviewer objection, not a new design. Rotation pointer untouched at `claude:claude-fable-5`. Metered **$0.2326** of $5 (pi executor $0.2325 + probe $0.0001). No GPU, no `rig.lock`. Gates on **darwin/arm64 only** except where Gate 3b's matrix is cited (rule 3b(viii)). Gate-5: **NO skill edit** — this iteration's own friction (*a controller quoting a `-run`-narrowed blast radius for a whole-package sentence*) is a rule the skill ALREADY has (3b(ii)); it was broken, not missing, so it goes to the log's Ruled-out rather than into the rulebook.


## STATUS 2026-08-19 — ITERATION 13 COMPLETE: **MARK'S RULING UN-PARKED ITEM 6 AND THE TRACE HE MADE A PRECONDITION ANSWERS THE REVIEWER BOTH WAYS — THEN FINDS THAT THE FIX IT AUTHORISES CANNOT BE WRITTEN WHERE EVERYONE ASSUMED.** Pick was **not** the untagged queue head (item 7): `D-MOTOKO-FMT-1` was RESOLVED **precondition** by Mark in an attended session dated **2026-08-19** and recorded in `1a3ca2d5f`, and an answer to a parked item unparks it and becomes the pick. The ruling: *"the sprint TRACES motoko's resolved runtime provider first, then changes the preflight. Do not redesign around the unknown"*, with the trace required to **discriminate** — does removing the unconditional `OPENROUTER_API_KEY` refusal at `internal/executor/motoko/healthcheck.go:64` delete a real fail-fast, or admit a silent OpenRouter fallback for `provider: "ollama"`, `env_var: ""` entries? **RUN, via the `mk-ast` fork-resolution-path arm the ruling names as sufficient (`and/or`), no GPU and no `rig.lock`. THE ANSWER IS BOTH, OF DIFFERENT LANES — so the remedy is a CONDITION, never a deletion.** (a) For the two fmt arms **no OpenRouter routing is reachable**: precedence in the fork is `process.env.MODEL ?? profileAgent.model ?? "anthropic/claude-sonnet-4-6"` (`index.ts:768-771`, read first-party — and the `models.yml:1854` comment citing `index.ts:580` for this is a **stale transcription**, that line is now event-printing code), tier 1 is always set by the Go executor (`motoko.go:343`) from `agent_model_name` (`models.go:493-494`) and tier 2 is the profile's own `ollama/qwen3.5:35b-a3b-mxfp8` — **measured** `GuessProvider("ollama/qwen3.6:35b-a3b-mxfp8")=ollama` with env var `""`, matched at `config.go:55-57` *before* the generic `vendor/model → OpenRouter` rule at `:67-73`, by an in-code comment demanding exactly that ordering; both profiles additionally pin `openai_base_url=http://localhost:11434/v1`; and motoko's own `required_secret_for_model` returns `""` for the `ollama/` prefix (`supervisor.ail:21-26`). **The measurement discriminates rather than passing vacuously** — the same call returned `openrouter`/`OPENROUTER_API_KEY` for all three fallback defaults (`anthropic/claude-sonnet-4-6`, `openrouter/auto`, `openrouter/anthropic/claude-haiku-4-5`), so both arms are present. (b) For the OpenRouter-routed motoko lanes the preflight **is** a real fail-fast **and the only one**, because motoko's own check is a WARNING that proceeds: `validate_secrets` → `emit_warnings` prints `{"type":"warning"}` and `main` continues to `run_with_config` (`supervisor.ail:11-19`, `:42-51`). **THE FINDING THAT RE-SHAPES D1, and it is the part no reading of `healthcheck.go` could give you: the condition is not expressible where the check sits.** `HealthCheck(ctx context.Context) error` takes **no task** — it is the shared `executor.Executor` interface method (`executor.go:31`) — so the only model it can read is `e.model`, and `cfg.MotokoModel` is **never set from models.yml**: **3** non-test hits total, of which the only assignment is the hardcoded `"openrouter/anthropic/claude-haiku-4-5"` at `factory.go:71` (control: `MotokoProfile` → **11** hits, so the grep finds wiring where it exists). The lane's real model arrives per-task as `task.Model` (`eval_benchmark_agent.go:174,195,253,389`), consumed by `getModel(task)` at Execute time — **after** the health check has already refused. So an `if` added at `healthcheck.go:64` would evaluate the OpenRouter default for **every** lane and refuse the ollama arms exactly as today: D1 is a plumbing change, not the ~0.5-day one-liner §6 prices. Three costed options recorded (set `cfg.MotokoModel` from models.yml · move the check into `Execute` · widen the interface, 6+ implementors), plus the recommendation to express the condition on the **resolved provider** — `ai.EnvVarForProvider(ai.GuessProvider(model))` returns the needed var *or `""`* for every provider (`config.go:104-119`) — rather than hardcoding one vendor; note `internal/executor/motoko` does not import `internal/ai` today, so the boundary must be checked first. **Free from the same read:** the preflight returns *before* the `motoko --version` query that discovers `e.motokoRepo`, and `MOTOKO_REPO` is what stops the profile silently degrading to `extensions.order=[]` — the defect `motoko.go:344-364` records as **39 of 39** eval sessions with `loaded_extensions=[]` — so a conditional check must not run ahead of repo discovery, or it would drop the `fmt` extension that IS the treatment. **NO SILENT-FALLBACK PATH EXISTS ANYWHERE**: repo-wide non-test Go reads `OPENROUTER_API_KEY` at exactly **4** sites (`ai_handlers.go:276`, `exec.go:475` — both explicit `openrouter` subcommands — `config.go:115,143`, keyed by an ALREADY-resolved provider, and the motoko preflight), none re-routing on absence; control **23** `OPENAI_API_KEY` sites. **WHAT THE TRACE DOES NOT SETTLE, moved to acceptance rather than claimed:** the static arm proves no OpenRouter routing is *reachable*, not that no connection is *made*; the live arm is circular today (the fmt lane cannot run while the preflight refuses it), so it becomes `AC-D1-live` — one fmt run reaching `localhost:11434` with **zero** `openrouter.ai` connections, asserted on the connection (not on the absence of an error, which "the run never started" satisfies equally) and paired with an OpenRouter-lane known-positive control. **DELIVERED**: doc `m-motoko-fmt-remeasurement-instrument.md` un-parked, O4 CLOSED with disposition, new **§12** (the trace, the three options, the ordering note, `AC-D1-live`) and verification rows **V25–V32**. **Phase-0 predicates re-run, not transcribed — all still FALSE, rows 10/11/12 stay parked:** G1 `#154` `state=OPEN`/`mergedAt=-` (control `#161`/`#162` → `MERGED` with non-null `mergedAt`), G2 predicate rc=**128** with the mandatory `README.md` control rc=**0** → FALSE for the right reason, G3 registry `latest=2.2.0`, `versions=1.0.0,2.0.0,2.1.0,2.2.0` → no 5.x, G4 unrunnable, G5 unchanged. **UPSTREAM IS WORKING OUR ISSUE:** `arniwesth/mot-100-fix-output-headroom` moved `ffd6256 → 2f61665` since iteration 12 and now carries **7** commits ahead of `main_dst`, including `da999ac fix(compaction): reserve provider output headroom` and their PR **#166** — i.e. `#165` is being acted on, and iteration 12's re-anchor landed in time to matter. **RULED OUT:** *the `models.yml:1854` comment "`MODEL env > profile config model (index.ts:580)`" is current* — the precedence claim is TRUE (re-derived at `:768-771`) but its **citation is stale**, which is rule 3b(v)(b) caught in the repo's own comments rather than in a controller's prose; the correct behaviour was re-measured, not inherited. *The silent-OpenRouter fallback the reviewer feared is live today* — it is **real but unreachable on the wired path**: tier 3 `anthropic/claude-sonnet-4-6` does resolve to OpenRouter (measured), and it fires only if MODEL is empty AND the profile does not resolve, which the unconditional `MODEL=` at `motoko.go:343` forecloses. Recorded as a residual with its trigger named, not as a live defect. Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, ran from the `#558` driver pin root detached and clean at `c29c48e96` == `origin/dev`, running skill **byte-identical to origin** (`cmp` rc=0); dev **verified green, not merely un-red** — **16** exact-SHA checks, **0** not-green, `runs_total=2` so a run exists; **0** human directives on `#743` since the watermark (of 10 comments) — the ruling arrived by the attended-session channel, not the issue; ledger valid at **3** rows, **0 OPEN**. No died-mid-flight traces: no motoko worktree or PR from iteration 12 outstanding; `#695`/`#613` are V1's, as the last four iterations also measured. Weekly sweep and rotation **both not due** (`#743` created `2026-08-17T05:48:23Z`, after the Monday-07:00 **local** boundary = `05:00Z`; 10 comments < 80). Routing: controller `claude:claude-opus-5` only; **no designer, planner, executor, evaluator or quorum spawned** — a controller-run trace answering a reviewer objection has no doc to design, no plan to write and nothing to judge, and the doc's own quorum is spent (2 rounds, both reviewers present, `absent_reviewers` empty in both artifacts). Rotation pointer untouched at `claude:claude-fable-5`. Metered **$0.00** of $5. No GPU, no `rig.lock`; `make quick-install` deliberately NOT run (shared-write guardrail) — the one build step taken was a throwaway `go test ./internal/ai/` whose test file was deleted and the tree re-asserted clean. Gates on **darwin/arm64 only** (rule 3b(viii)). Gate-5: **NO skill edit** — the iteration's friction (a repo COMMENT carrying a stale `file:line` citation for a behaviour that is itself still true) is instance 1 of *"rule 3b(v)(b) applies to the codebase's own comments, not just to controller prose"*, pre-registered at the ≥2-friction bar rather than written on one datapoint.

## STATUS 2026-08-18 — ITERATION 12 COMPLETE: **THE UPSTREAM ISSUE WE FILED SIX HOURS AGO WAS ALREADY ANCHORED TO A BASE THAT HAD MOVED 59 COMMITS — AND THE MAINTAINER HAD JUST CUT THE BRANCH TO WORK IT.** Pick was **not** the untagged head (item 7): Gate 1's external-predicate re-read — the rule iteration 11 added to this skill — found queue row 5's six-hour-old claim *"`origin/main_dst` is **still exactly `6c06b08`**, **0** commits since our V27–V29 rows"* **FALSE**, measured `git rev-list --count 6c06b08..8110ffc` = **59** (**100** files), with `src/core/session.ail` among them (`+67/-13`). Upstream cut `arniwesth/mot-100-fix-output-headroom` in the same window. **THE FINDING: a LANDED row's deliverable can rot against a base nobody re-reads, and every freshness rule in this file is scoped to a row you are about to PICK.** Iteration 11's rule catches a *blocked* row re-asserting a stale measurement; this is the mirror — the row reads LANDED so no gate revisits it, and its deliverable is not a local file but an artifact **published to an external party** pinned to that party's moving branch. **THE DEFECT IS INTACT — only the offsets moved, and that is measured, not hoped.** The ten-line window `session.ail:2552-2561@6c06b08` is **byte-identical** to `:2606-2615@8110ffc`, sha256 `c53792eb5b778cf32e72001006e485274b05f33d13f14cbe578c836e9e15f1dc`; **negative control** — the same window shifted by one line hashes `01d10306…`, so that is a real match and not a hash of nothing. All three `session.ail` citations in `#165` shift **+54**: `2552→2606` (the `context_limit` binding), `2556→2610` (`ext_context_limit = context_limit - pinned_tokens`), `2561→2615` (`seal_compacted_payload(…, context_limit, …)`). The asymmetry the issue is about — extensions get the reduced limit, the seal gets the raw one, five lines apart — survives verbatim. **The other cited files are byte-unchanged, with a SAME-CALL control:** `git diff --stat 6c06b08 8110ffc -- phase_vocab.ail compaction.ail compaction_structural.ail context_usage.ail session.ail` returns **`session.ail` only**, so the instrument fires (rule 3a(i-d)) and `compaction.ail:25-28`/`:30`, `compaction_structural.ail:97-113`/`:170`/`:191`, `phase_vocab.ail:145-155` all stand as written. **DELIVERED — a correction comment on [#165](https://github.com/arniwesth/motoko_agent/issues/165#issuecomment-5332596902)** carrying the old→new table, the block sha256 with its negative control, the same-call unchanged-files control, and the note that `arniwesth/mot-100-fix-output-headroom` points at `ffd6256`, an **ancestor** of `main_dst@8110ffc` with **zero** commits of its own, so branching from the tip saves a rebase. Posted `--body-file`, **delivery asserted by comment count 0 → 1**. **PHASE-0 PREDICATES RE-RUN, NOT TRANSCRIBED — all still FALSE, so 10/11/12 stay parked.** G1 `#154` `state=OPEN`, `mergedAt=-` (control: `#161`/`#162` return `MERGED` with non-null `mergedAt`) — but `updatedAt=2026-08-18T16:48:58Z`, i.e. actively moving, which is what put `main_dst` 59 ahead; G2 predicate rc=**128** with the mandatory `README.md` control rc=**0** → FALSE for the right reason; G3 registry `latest=2.2.0`, `versions=1.0.0,2.0.0,2.1.0,2.2.0` → no 5.x; G4 unrunnable; G5 unchanged. **RULED OUT:** *upstream already fixed it* — `session.ail:51` now imports `resolve_context_limit` from `src/core/context_usage`, which looks exactly like the fix, but `context_usage.ail` is **byte-unchanged** in the range and resolves the limit rather than reserving output headroom, and `:2615` still gets the raw value (the solved-upstream class, negative on measurement). *The four merge commits moved `session.ail`* — **refuted before posting**, and the draft comment had already asserted it: `git log 6c06b08..8110ffc -- src/core/session.ail` returns exactly **two** commits (`b1ad13b`, `a45f708`, both `#160`), which are the whole `+54`; rule 3b(v)(b) caught on this controller's own prose. *The fix branch has work on it* — it is an ancestor of `main_dst`, `git log main_dst..branch` empty. Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, ran from the `#558` driver pin root detached and clean at `9467ef183`, running skill **byte-identical to origin** (`cmp` rc=0), pin root **4 behind** `origin/dev` so all mission state was read from origin blobs and this record is written in a worktree branched from `origin/dev`; dev's last **settled** commit `9467ef183` green (**16** checks, **0** not-green) while the tip `d22681e27` is **mid-flight** (15 pending) and is recorded as *unverified, not green*; `5f9f4ba4f` `checks=0` is the documented intra-push zero, not an anomaly — and dev is V1's to own regardless. **0** human directives on `#743` since the watermark (of 8 comments), ledger valid at 3 rows with **1 OPEN** (`D-MOTOKO-FMT-1`). No died-mid-flight traces: five motoko worktrees `dirty=0`; `#695`/`#613` are V1's; the stale source clone's 7 paths unchanged since iteration 11 adjudicated them superseded. Weekly sweep and rotation **both not due** (`#743` created `2026-08-17T05:48:23Z`, after the Monday-07:00 **local** boundary; 8 comments < 80). Routing: controller `claude:claude-opus-5` only; **no designer, planner, executor, evaluator or quorum spawned** — a base-freshness re-measurement of a filed artifact has no doc to design, no plan to write and nothing to judge. Rotation pointer untouched at `claude:claude-fable-5`. Metered **$0.00** of $5. No GPU, no `rig.lock`; `make quick-install` deliberately NOT run (shared-write guardrail) — nothing here tests local AILANG behaviour. Gates on **darwin/arm64 only** (rule 3b(viii)). Gate-5: **NO skill edit** — the gap (*every freshness rule here is scoped to a row you are about to pick; none covers an artifact a LANDED row already published to a third party whose base moves*) is **instance 1**, pre-registered at the ≥2-friction bar rather than written on one datapoint; the correction goes to queue row 5 in the process lane instead.

## STATUS 2026-08-18 — ITERATION 11 COMPLETE: **THE QUEUE'S BOUNDED WAIT HAD ALREADY EXPIRED — ARNI ANSWERED #97 FIVE DAYS AGO, AND THE ROW TRACKING HIM WAS RE-ASSERTING A MEASUREMENT TAKEN BEFORE HIS REPLY.** Pick was queue item **5**, not the untagged head 7, because Gate 2's blocker re-verification found item 5's declared blocker **dead**. The row read *"Remaining: the bounded wait only — still zero `arniwesth` events on #97; 2026-08-27 stands"*; measured this iteration, `arniwesth` commented at **2026-08-13T18:45:54Z**, control firing (`commenter:arniwesth` → **34** issues/PRs). **THE FINDING: a blocker row can re-assert a stale measurement as a fresh one, and its own text cannot tell you which it is.** Iteration 3 measured the zero on 2026-08-13 and was right; iteration 4 wrote *"still zero"* on 08-14, **after** the comment, and iterations 5–10 carried it forward. The word *still* reads as a re-check and is a transcription (rule 3b(v)(b)). Sharper: the 08-27 timebox iteration 3 added as the **remedy** for an unbounded wait is what hid the wait ending, because a timebox invites checking the clock instead of the predicate. Five days of a nine-day window, on a row sitting above two ungated items. **WHAT ARNI SAID, and it is better than the row assumed:** *"Agreed on all four points"*; close `#97` as superseded (calibration, elision ladder and system-prefix pinning all covered more strongly by `main_dst`/`#154`, nothing to revive); **the output-headroom concern is still valid**; and *"Please go ahead with the separate issue against `main_dst`"* — with scope specified: the effective input budget for **both** the pre-step chain and the final seal, raw context limit retained for telemetry, regression coverage for 262,144/65,536 **and** the unknown-/small-limit fail-open. So iteration 3's original precondition, declared FALSE and replaced by the timebox, is now **satisfied on its own terms**. **DELIVERED — upstream [arniwesth/motoko_agent#165](https://github.com/arniwesth/motoko_agent/issues/165)**, every load-bearing claim re-derived first-party at `main_dst@6c06b08` rather than inherited (rule 3b(v)); the base was checked, not assumed — `origin/main_dst` is **still exactly `6c06b08`**, **0** commits since our V27–V29 rows. `session.ail:2561` passes the seal the **raw** `context_limit` while `:2556` gives extensions `context_limit - pinned_tokens` — so the seal is the one consumer still handed the unreduced number, and the precedent for the fix is one line above the call site. `phase_vocab.ail:145-155` + `compaction.ail:30` show the refusal is **input-only** at `exhaustion_pct() = 95`. The ladder cannot close the gap: `compact_for_pre_step` (`compaction_structural.ail:170`) targets `result_target_pct() = 70` (`:30`,`:16`) but every tier routes through `elide_walk` (`:97-113`), which rewrites a message **only** at `m.role == "tool"` (`:101`), so a large **user** message is invisible to all four and the hook returns the unconditional `Compacted(floor, …)` (`:191`) — a payload it has already determined does not fit, with no error. And `compaction.ail:25-28` returns **0%** when `limit == 0`, so an unknown model makes the seal compare `0 >= 95` and send unconditionally: the fail-open arm Arni named, and the reason a percentage-shaped reserve cannot inherit its own zero case. **THE R8 PROBE WAS RE-RUN, NOT QUOTED** (`tools/motoko/r8_headroom_band.ail`, fresh `~/dev/mk-r8-main-dst` at `6c06b08`; `ailang lock` rewrote the 19 dev-container absolute paths — `/workspaces` **0**, control `"path":` **19**). Every number reproduced: arm **A** ladder `tier=floor keep_last=1` at **79%**, seal **`Ok` — SENDS**, headroom **54,905 < 65,536**; controls **B** (`PassThrough`/`Ok`, 259,642) and **C** (`Err(SealExhausted)` at 157%) both fire, so that `Ok` is a real permission and not a dead gate. New on the re-run and carried into the issue: the ladder removed **2,061 of 208,980** segment tokens (~1%), and the extension saw `ext_limit = 261,824` while the seal saw the raw **262,144** — the asymmetry as one line of output rather than as an argument. **`#97` CLOSED with the verdict**, comment-then-close, comment count asserted **2 → 3**. **RULED OUT:** *the charter's "zero production callers" for `try_emergency_compaction_with_limit`* — it has exactly one caller (`compact_step_with_limit:137`), so the bare claim is false as written; the substance holds and upstream says so itself (`006_compactor_strategy/PLAN-compactor-strategy.md:75` records it **test-only**, single caller), so the issue says "off-path" citing their line rather than repeating our overstatement. Also ruled out: *an output-headroom issue already exists upstream* (all **20** enumerated, none is one; `#31`/`#49` cited as related, titles verified), and *`main_dst` moved under us* (**0** commits since `6c06b08`). Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, ran from the `#558` driver pin root detached and clean at `1350d308f` == `origin/dev`, running skill **byte-identical to origin** (`cmp` rc=0 with a rc=1 control), dev **verified green not merely un-red** — **16** exact-SHA checks, **0** not-green, `test: success` present, `runs_total=2` — **0** human directives on `#743` since the watermark, ledger valid at 3 rows with **1 OPEN** (`D-MOTOKO-FMT-1`, unanswered), inbox 5 unread carrying no request or bug against motoko, no died-mid-flight traces (four motoko worktrees `dirty=0`; `#695`/`#613` are V1's; the stale `ailang-motoko` checkout's 7 paths re-verified **superseded not orphaned** — 3 of 4 blobs verbatim in `de0e41099`, the 4th the decision-ledger block already in origin's charter). Weekly sweep and rotation **both not due**. Routing: controller `claude:claude-opus-5` only; **no designer, planner, executor, evaluator or quorum spawned** — an upstream issue assembled from measurements the controller took itself has no doc to design, no plan to write and nothing to judge; quorum-at-pick N/A (item 5 has no doc of its own; its case lives in already-reviewed V27–V29 and R8). Rotation pointer untouched at `claude:claude-fable-5`. Metered **$0.00** of $5. No GPU, no `rig.lock`; `make quick-install` deliberately NOT run (shared-write guardrail) — PATH binary `v0.33.1-103-g0002c9b0b-dirty`, stated because the R8 re-run used it. Gates on **darwin/arm64 only** (rule 3b(viii)). Gate-5: **one skill edit** — Gate 2, a blocker row's freshness claim.

## STATUS 2026-08-18 — ITERATION 10 COMPLETE: **ITERATION 9'S ENTIRE RECORD WAS SITTING IN AN OPEN PR, BLOCKED ON A RED ITS BASE HAD ALREADY BEEN FIXED FOR — AUTO-MERGE IS NOT A LANDING MECHANISM.** Gate 2's died-mid-flight check was the pick: PR [#760](https://github.com/sunholo-data/ailang/pull/760) OPEN, `MERGEABLE`/**`BLOCKED`**, worktree `.wt-motoko-iter9-record` corroborating, and the charter's newest stamp reading **iteration 8** — so no reader could tell whether iteration 9 ran, crashed, or never fired. Deliverable per the rule: **verify and land**, not redo; Standing rule 1 then allowed queue head **6b** as the second item, recovery being bookkeeping. **THE FINDING, and every number is first-party:** iteration 9 ended by enabling auto-merge (`enabledAt 2026-08-17T19:36:39Z`, SQUASH) rather than force-merging over a red, predicting *"it goes green once #759 lands"*. The premise came TRUE — `#759` merged `2026-08-17T20:02:27Z`, 26 minutes later, and its merge commit `cf56772bf` reads `test=success`, as does every commit after it to HEAD — and the PR **still did not merge**. #760's `updatedAt` is still `2026-08-17T19:36:34Z`, comments **0**, `test` still `failure` on head `bf66f7655` at `run_attempt=1`, **12h13m** later. Mechanism: GitHub auto-merge merges when *the PR's own* required checks pass; it does **not** update the branch and does **not** re-run checks when the base advances, so a base-inherited red is pinned to the PR's head SHA and outlives its own fix. The loss is **silent** — no timeout, no red to triage, no failed command, because Gate 3b was never left in a state that could fail — the vacuous-pass class aimed at the *landing* step. **Two of motoko's last five iterations have now lost their record**, by two different mechanisms (6→7, 9→10). **SECOND FINDING — iteration 9 named the wrong failing step, and it was the one it had spent the whole iteration fixing.** Its PR body attributes the red to `Check changelog index hygiene` and baselines `make check-changelog` rc=2 both ways; measured on `actions/jobs/95482959213`, that step is **17** and `skipped` — it never ran — while the failure is step **14 `Run stdlib .ail test suites`**, and the base `714f1cecc` (`actions/jobs/95481571091`) fails at step **14** identically. So the *verdict* (inherited, not caused by a docs-only diff) is **right** and the mechanism cited for it is **wrong**: a real measurement, of a different gate. Rule 3d in its purest form — the red arrived in exactly the direction the iteration had been looking all day. NB the step list is visible only in `actions/jobs/<id>`; `check-runs` reports the job, never its steps. **DISPOSITION:** rebased `docs/motoko-iter9-record` onto `origin/dev` (`git diff --stat 714f1cecc origin/dev` over the five mission files is **empty**, so no conflict was possible), re-deriving iteration 9's load-bearing claims rather than adopting them (rule 3b(v)) — `#758` CLOSED `19:27:25Z`, `#759` MERGED `20:02:27Z`, base `test=failure`, `cf56772bf` `test=success`. **One correction to the inherited diff:** it wrote the bare `design_docs/mission-dashboard.md` that V1's iteration-216 rule forbids; that hunk was dropped and the snapshot went to `design_docs/motoko-mission-dashboard.md`. The bare file still holds motoko's own stale iteration-7 snapshot and is **left alone** per the rule, flagged as one line of future cleanup. **QUEUE ROW 6b CLOSED as a measurement — motoko owns 0 of 15.** 3 have closed since the sweep (`#727`, `#708` 08-18; `#696` 08-17); **11 of the remaining 12 carry `ailang-message` + `from:<consumer>` labels** (`ailang-parse` #689 #688 #662 #656 · `email-parse` #679 #676 · `housemove2026` #672 #671 · `cli` #694 #646 #644) and are **already enumerated in V1's charter** at `v1-mission.md:2104-2109`; the 12th, `#687`, is V1's **declared next pick** in both its 220 and 221 reports. So there was nothing to hand over and no comment was posted — a verdict from the non-owning loop is noise on someone else's lane. Table over 8 files, controls firing (`#663`→motoko charter **6**, `#617`→v1 charter **2**, `#770`→v1 dashboard **1**), `${#FILES[@]}`=8 asserted against the 8 columns printed. **RULED OUT:** *the sweep row poisons its own detector* — true that all 15 now grep ≥1 because 6b names them, but that is the sweep **working** (they are tracked, by that row); the narrower hazard is a row closing without action, which is why 6b's close carries an ownership measurement rather than a bare tag. Not a skill edit. Also ruled out: *auto-merge is unsafe* (V1's log carries **76** mentions and it lands PRs routinely — the failure is specific to a base-inherited red), and *`#695`/`#613` are the same shape* (neither has auto-merge enabled). Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, ran from the `#558` driver pin root detached and clean at `c0dde65eb` == `origin/dev`, running skill **byte-identical to origin** (`cmp` rc=0), dev **verified green not merely un-red** — **16** exact-SHA checks, **0** not-green, `test: success` present, non-zero run count — **0** human directives on `#743` since the watermark, decision ledger valid at 3 rows with **1 OPEN** (`D-MOTOKO-FMT-1`, unanswered). Weekly sweep **not due** and no rotation due: `#743` created `2026-08-17T05:48:23Z`, **after** the Monday-07:00 **local** boundary (=`05:00Z`), 4 comments < 80. Routing: controller `claude:claude-opus-5` only; **no designer, planner, executor, evaluator or quorum spawned** — recovery plus a measured queue disposition has no doc to design, no plan to write and nothing to judge; rotation pointer untouched at `claude:claude-fable-5`. Metered **$0.00** of $5. No GPU, no `rig.lock`; `make quick-install` deliberately NOT run (shared-write guardrail). Gates on **darwin/arm64 only** (rule 3b(viii)). Gate-5: **one skill edit** — Gate 3b, auto-merge is not a landing mechanism when the red is base-inherited.

## STATUS 2026-08-17 — ITERATION 9 COMPLETE: **DEV WAS RED IN TWO DIFFERENT WAYS, THE CHANGELOG GATE COULD SEE 1 OF 5 REAL OFFENDERS — AND V1 WAS FIXING THE IDENTICAL THING FOUR MINUTES BEHIND ME, SO I STOOD DOWN.** Pick was **not** the queue head: `origin/dev` HEAD `0002c9b0b` was RED, which under the rulebook in force at pick time outranked the queue. **The red was two reds and only one was actionable.** `test` failed at `steps=41`, failing step `Check changelog index hygiene` — a repo command that ran; `lint` and `Build macos-latest` (×2) all failed at `steps=1, last=Set up job`, i.e. before checkout, inside a declared GitHub incident (`13:40:03Z`, impact **critical**, still `investigating`) whose window covers the run's `15:49:32Z`. Per the outage rule those three were diagnosed, **not** reverted and **not** fixed-forward; the negative control arrived later and is decisive — the same three jobs came back **green** on my PR head at 21:30Z on a byte-different tree, so they failed for the platform. **The actionable half: `4c3ef27c8` appended release notes to root `CHANGELOG.md`, which is an INDEX — release-manager builds notes from `changelogs/v0.18-current.md`, so anything left there is silently dropped. But the gate could only ever have caught that block by luck, and that is the finding.** `scripts/check_changelog.sh` matched Keep-a-Changelog *keyword* headers (`### Added|Fixed|Changed|Removed|Deprecated|Security`) and *bracketed* `## [Unreleased]`; this repo writes neither. Measured at `0002c9b0b`: the index held **168 lines of release notes in 5 blocks** and the gate flagged **1** — the only one whose header began with the word "Added". `### Docs — …`, `### Eval cost accuracy — …` (×2), `### Mission infrastructure …` and the unbracketed `## v0.32.0 (Unreleased)` were invisible **by construction**. Confirmed against the last-green commit `22c74d7c3`: **3** `###` blocks present, **zero** gate hits — so the gate had been printing a green checkmark over misfiled release notes for as long as they had been there. Rule 3j exactly: the defect is the **enumerator**, not the branches, and the tempting one-line fix (rename the header to dodge the regex) was rejected because it satisfies the enumerator while leaving the content release-manager drops. **Delivered as PR [#758](https://github.com/sunholo-data/ailang/pull/758)** (2 commits): all 5 blocks moved (**167 of 168 lines verbatim**, asserted line-by-line with a firing negative control; the one dropped line is the redundant `## v0.32.0 (Unreleased)` wrapper), detector widened to any `###` sub-section plus any version/Unreleased `##` bracketed or not, an **anti-vacuity floor** (a missing/empty `changelogs/` dir now fails loudly — every other check in the script is an absence test, which an empty destination satisfies exactly as well as a clean index), and `scripts/test_check_changelog.sh` / `make check-changelog-selftest` wired into CI, because **the gate had no test at all** (control: `test_pin_root` appears in `make/test.mk`, so the instrument finds tests where they exist). `make check-changelog` **rc=1 → rc=0**, baselined red first on a pristine `origin/dev` worktree so the failure is the repo's and not local. Self-test **11/11**, non-vacuous by drill: against the pre-fix detector **5 arms fail** and the four shapes live in the repo return **`rc=0` with the green checkmark**; mutant restored from a `cp` backup (never `git checkout --`) and sha256-verified. **Gate 3b GREEN on the PR head `ef3b6a32c`: 21 checks, pending=0, 0 not-green, 4/4 required contexts**, and both changelog steps ran green including the new self-test; three bounded poll windows, every reading of `notgreen=0` over a non-zero `pending` recorded as vacuously green and NOT as a verdict. **THEN THE ITERATION'S REAL LESSON LANDED UNDER ME.** V1's iteration 217 had independently preempted onto the same red and opened [#759](https://github.com/sunholo-data/ailang/pull/759) **four minutes after** #758 — the same six files, both adding `scripts/test_check_changelog.sh`, both moving the same 169 lines — and mid-poll V1 landed `c2022c7fa`, which scopes "a red outranks the queue" to the mission that **OWNS** the repo. For `sunholo-data/ailang` that is V1, not motoko. **So I stood down rather than raced**: #758 CLOSED with the verdict (comment-then-close, comment growth asserted 4→5), #759 is the one to land. That is the right call on merit as well as ownership — V1's detector (`^#{2,6}[[:space:]]` minus the archive heading) is **strictly more general** than mine, and their 9-arm self-test is at least as rigorous, which I established by **reading their file rather than grepping for my own idiom** (my first `grep -c` for arm markers returned **0** against a test file that demonstrably exists — rule 3a caught on my own instrument). **Two measurements handed to #759 that it predates, both of which change its diff:** (1) the index has **accreted two MORE stranded sections since the red** — `9504393d0` and `dfb19a551` each appended to it — so `origin/dev` now holds **7** blocks, not 5, and #759 as written would move 5 of 7 and **strand a `### SECURITY` entry** (the prelude `println` capability bypass), the one with the strongest claim on being in the release notes; (2) the outage-vs-real split above with its negative control, so nobody attributes a lingering `lint`/`Build macos-latest` red to the changelog fix or reaches for a revert. Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, **the running skill byte-identical to `origin/dev`** (`cmp` rc=0 — this fire ran from the `#558` driver pin root at `0002c9b0b`, detached and clean, so the stale-checkout hazard is closed by construction rather than by luck), **0** human directives since the watermark on `#743` **and** on the rotation-week predecessor `#663` (rotation was 05:48Z today), decision ledger valid at 3 rows with **1 OPEN** (`D-MOTOKO-FMT-1`, unchanged — no directive answered it), inbox 34 unread all `eval-suite` telemetry with **zero** non-eval-suite senders, no died-mid-flight traces (all three prior motoko worktrees clean; the three open loop-authored PRs `#744`/`#695`/`#613` are V1's). Weekly external-issue sweep **not due** — iteration 8 ran it this morning and `#743` was created after the Monday-07:00 local boundary. **Queue untouched: item 6b remains [NEXT]** — the red was never mine to keep, and a hand-off is not a pick. Routing: controller `claude:claude-opus-5` only; **no designer, planner, executor, evaluator or quorum spawned**, deliberately — the work was a doc move plus a ~10-line shell gate, which the skill routes as deterministic mechanical work, and the codex lane is quota-exhausted until Aug 20 while `pi` is metered. Metered **$0.00** of $5. No GPU, no `rig.lock`; `make quick-install` deliberately NOT run (shared-write guardrail). Gates ran on **darwin/arm64 only** (rule 3b(viii)). Gate-5: **no skill edit** — the one friction worth a rule (two loops preempting one red with no cross-mission mutex) was already written into the shared skill and both charters by V1's `c2022c7fa` while this iteration was in flight, and duplicating it would violate the one-edit rule for zero information.


## STATUS 2026-08-17 — ITERATION 8 COMPLETE: **THE −74% WE WERE ABOUT TO RE-PROVE IS ONE BENCHMARK AND ONE VOID PAIR — ON THE PAIRS WITH PROVEN TREATMENT AND A PASSABLE BENCHMARK IT IS −5.7%. DOC PARKED ON ONE MEASURED QUESTION.** Queue head item **6** (fmt re-measurement instrument). **The pick's premise did not survive Gate 2, and that reframed the whole item before any role was spawned.** The −74% (AC5, `m-fmt-dialect-alignment.md`, 2026-07-31) is real and correctly attributed to the fmt extension — but five things are true of it, all re-derived first-party this session: (a) its own author rates it *"direction, not proof"* (n=1/pair, sign test p≈0.11); (b) **74.7% of the token saving is ONE benchmark** — all six pairs give **−74.2%**, dropping `log_file_analyzer` gives **−47.1%**, and that pair alone contributes **3,125,933 of 4,182,882** saved tokens; (c) that benchmark is **3/30 lifetime and 0/10 over the last five nights** in the `rag_on` opencode rotation lane (scope stated, per rule 3b(ix) — the history file holds **only** `arm=rag_on`, 516 rows, control `config_file_parser` 4/46), with open issue **#649**, so tokens-to-pass is **undefined** there; (d) one of the six pairs was **quarantined by the harness itself** and summed into the headline anyway — `ab2_fmt_on/emit_exact_bytes_varied` banked **zero** fmt-hook events and `validity={valid:False, reason:treatment_unproven}`, while the other 5 ON rows carry exactly one `status=formatted` event each and all 6 OFF rows are clean (control arm uncontaminated); (e) the run was **order-confounded by construction** — sorting all 12 banked rows by their in-file `timestamp` gives a perfect block, ON 16:42:37→17:00:10 then OFF 17:01:28→17:36:49, zero interleaving, and the within-arm benchmark order differs between arms so "pair by trial index" never paired the same position. **Net: on the four pairs with both proven treatment and a currently-passable benchmark the headline is −5.7% (ON cheaper 3/4), not −74.2%.** So "re-prove the −74%" is not a well-posed target, and the designer was directed at the honest question instead. **Deliverable: [m-motoko-fmt-remeasurement-instrument.md](planned/m-motoko-fmt-remeasurement-instrument.md)** (421 lines, `0e1edd80c`) — a paired **censored win-rate** defined even when arms fail (a non-pass is right-censored at the 4M cap; null P(ON wins | non-tied)=0.5), a **counterbalanced** execution schedule with an order-integrity gate that VOIDs a slot banked otherwise, ELO-banded benchmark selection with `log_file_analyzer` **out** and numeric continuity with −74% deliberately abandoned, ~**9.8 rig-hours** priced against a measured **4.91 min/row** anchor, and a **pre-registered** KEEP/RETIRE/inconclusive rule. **A live defect was confirmed on the way:** the Wednesday fmt A/B lane has banked **nothing since AC5** — both the 08-05 and 08-12 fires died at `internal/executor/motoko/healthcheck.go:64`, an **unconditional** `OPENROUTER_API_KEY` refusal reached via `MotokoExecutor.HealthCheck`→`runHealthCheck` with no lane condition, whose own error text (*"motoko routes ALL models via OpenRouter"*) is **false** for these entries: both arms declare `provider: "ollama"`, `env_var: ""` (*"No API key — local inference"*), `agent_model_name: "ollama/qwen3.6:35b-a3b-mxfp8"` (models.yml:1854, :1880). **Two quorum rounds, both reviewers present both times, `absent_reviewers` empty in both artifacts** (so neither verdict is an N−1 degrade) — R1 BLOCKED, one revision, R2 BLOCKED; metered **$0.1424** of $5. **All four objections were classified premise-vs-design and every premise was MEASURED rather than forwarded (rule 3f); three are ANSWERED and carried into the text.** O1 (`gpt5-6-sol`, arm ordering) was **upheld and its "if" is fact** — that is finding (e) above, which the reviewer supposed hypothetically and the banked rows confirm. O2 (`gemini-3-1-pro`) correctly caught an unverified deployment premise: `PlistBuddy -c "Print :ProgramArguments" ~/Library/LaunchAgents/dev.ailang.nightly-eval.plist` → `{/bin/bash, /Users/voightkampff/dev/sunholo-data/ailang/tools/launchd/nightly-eval.sh}`, i.e. the script executes **in place from V1's checkout**, so the doc's `cp`-to-LaunchAgents instruction was useless — removed, and replaced with the real constraint (a merged fix reaches the rig only when V1's clone pulls — open issue **#558**). O3 (`gemini-3-1-pro`) was procedurally right that the load-bearing `#649` claim carried no verification row; measured **TRUE** (`gh issue view 649` → OPEN, created 2026-08-11; control `#721` → MERGED, so the instrument separates states). **O4 is the park and it is one question:** `gpt5-6-sol` is right that models.yml declares a provider while nothing here traces motoko's **resolved runtime** provider, so removing the preflight might delete a real fail-fast or admit a silent OpenRouter fallback. That trace needs the `mk-ast` fork's own resolution path and/or a live motoko run holding `rig.lock` — **not a verbatim text fix**, and choosing between "make the trace a D1 precondition" and "redesign D1" is controller judgment, so the narrow-refinement carve-out does **not** apply and the doc is **PARKED `needs-human-review`** as **D-MOTOKO-FMT-1** rather than force-passed. **No reviewer disputed the instrument's DIRECTION in either round** — not the metric, the set, the power arithmetic, or the decision rule; the park is one prerequisite, and nothing else in the doc waits on it. Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN** (re-checked before the nested `claude`), local HEAD == `origin/dev`, **running skill byte-identical to `origin/dev`** (`cmp` rc=0), **16** checks on HEAD with **0** not-green and a non-zero run count (so dev is verified, not merely un-red), **0** human directives since the watermark, no died-mid-flight traces — both motoko worktrees clean, the two open loop-authored PRs (`#695`, `#613`) are V1's, and the stale main checkout's 7 modified paths were re-verified as 5 byte-equal-to-origin + 2 explained by its 21-commit lag, not orphaned work. **Weekly external-issue sweep DUE and RUN** (first iteration past the 2026-08-17 07:00 **local** Monday boundary; `#663` created 08-12 07:59 CEST, before it): **15 orphans of 75 enumerated open issues**, printed as a per-issue table across 8 charter/log/archive/dashboard files with firing controls, batched into ONE new queue row **6b** and NOT allowed to outrank the pick. **The sweep's first run was WRONG and its own table caught it** — see the log's Ruled-out. Routing: designer `claude:claude-fable-5` via `claude-sub` (probe rc=0), **FLAGGED as a rotation fallback**: the pointer's next entry is `codex:gpt-5.6-sol` and codex is genuinely quota-exhausted (`ERROR: You've hit your usage limit … try again at Aug 20th`, rc=1 on my own re-probe, corroborating the driver's degradation notice), gemini is unwired pending G4, so the rotation wrapped back to claude. Planner/executor/evaluator **never spawned** — the deliverable is a parked design doc, so no plan and nothing to judge. No GPU touched, no `rig.lock`; `make quick-install` deliberately NOT run (shared-write guardrail). Gates ran on **darwin/arm64 only** (rule 3b(viii)). Gate-5: **one skill edit** (Gate-0 sweep, zsh 1-indexed arrays) and the **weekly thread rotation**, which was due.

## STATUS 2026-08-16 — ITERATION 7 COMPLETE: **RECOVERED ITERATION 6'S LANDED WORK AND RESTORED THE MISSION'S MISSING RECORD.** Gate 2 found the exact died-mid-flight shape: PR **#728** (`4f300bfa1`) had already merged queue item **5b**, but the charter and log still ended at iteration 5, while the named mission checkout held uncommitted routing/decision-doc residue on a stale base. Verified rather than adopted: `tools/launchd/test_pin_root.sh` passes **35/35** on the rig; PR #728 records the isolation mutation at **3 passed / 32 failed**; all **21** PR checks are terminal success/skipped, including `launchd drivers (bash 3.2)`; the exact-SHA `origin/dev` set is **16** checks with **0** not-green. The fix clears ambient scheduled-driver state before the hermetic pin-root lab, so CI and the rig now exercise the same synthetic paths. The other residue was already landed by `de0e41099`; byte comparison showed the running skill, driver, planner router, make target, and decision script equal origin. The stale main checkout remains untouched because it is 21 commits behind with user edits. Item **5b → LANDED**; item **6** is next. No heavy role, quorum, evaluator, GPU, or metered lane fired; controller `codex:gpt-5.6-sol`, metered **$0.00**. No human directives and the authoritative decision ledger has **0 OPEN** rows. Gate-5 retro: no skill edit—this was recovery/bookkeeping, and the existing died-mid-flight rule found the work exactly as intended.

## STATUS 2026-08-14 — ITERATION 5 COMPLETE: **THE FLEET'S "NO USABLE MODEL" REFUSAL WAS NEVER ABOUT MODELS, QUOTA, OR CONTENTION — OUR OWN SessionStart HOOK WAS HOLDING ITS STDOUT OPEN. ONE REDIRECT.** Queue head item **5a** (diagnose the empty-output probe hang). **Root cause, and it is entirely ours:** `scripts/hooks/session_start.sh` ended with `ailang docs embed-warmup --quiet --timeout 3m &`, commented *"(non-blocking)"* — true of the SCRIPT, false of every CONSUMER that CAPTURES its stdout. A backgrounded child inherits the stdout descriptor, so a `$(...)`-style capture cannot observe EOF until the **child** exits, however promptly the script itself `exit 0`s. Claude Code captures hook stdout (that is how the banner reaches a session), so the hook was held open for as long as the warmup ran — bounded only by the warmup's own `--timeout 3m` = **180s**, against the driver's **120s** `PROBE_TIMEOUT`. That is the whole of `probe timed out after 120s — captured output: ''`: empty because `claude -p` emits nothing until it completes, and identical across all three models because **it was never a model verdict — it is one hook stall observed six times** (3 models × 2 attempts). **Mechanism proven deterministically, three arms:** capture **8,433ms** / stdout redirected to a file **8ms** / identical script with the background child removed **237ms** — so the negative control fires and the block is the child, not the script. One real hook capture measured **96,377ms** against a warm-cache `embed-warmup` of ~1.2s. **Population separation is the strongest single piece of evidence and it was sitting in plain sight:** refusals per fire over the same 24-day window are v1 **47/186**, motoko **6/11**, world **0/89** — and world is the one mission whose checkout has **no `.claude/settings.json` at all**, hence no hooks (control: world's log carries **90** `probe ok` lines, so the zero is a measurement, not a broken grep). **`quota-limited` has NEVER fired once** in either driver log, so the driver's own `quota-limited, timed out, or errored` summary has only ever meant *timed out*, and every reading of these refusals as a quota problem — including this charter's — was wrong. **Amplification, measured not inferred:** `_mc_bounded` kills the `claude` process on expiry, and the warmup is a GRANDCHILD, so it survives — verified reparented to `ppid=1` — meaning each timed-out probe leaves a GPU tenant behind and the next probe adds another, up to six per fire. **RULED OUT — and this refutes my own leading hypothesis:** GPU contention alone is not sufficient. The filler held `rig.lock` from **07:58:30 to 12:39:58** on 08-14, and motoko's *successful* 09:45 fire is inside that window alongside the three refusals preceding it, so the correlation that fit three data points does not survive the fourth (rule 3d — the red arrived in the predicted direction and needed the negative control). **This also corrects iteration 4's charter claim that the stall is "not motoko-specific" / environmental**: it is common to v1 and motoko because both are `sunholo-data/ailang` checkouts carrying the three SessionStart hooks, and world is immune for a structural reason, not a lucky one. **Scope widened from diagnose-only to the fix**, deliberately: the mechanism is now known, and the remedy is a stdout redirect — emphatically NOT the probe-timeout increase the item forbids. Landed as PR **#721** with `tools/launchd/test_hook_stdout.sh`, wired into `make test-launchd-drivers` (already a CI gate at `ci.yml:472`). **The guard's own first draft was hollow and the mutation drill caught it** — worth more than the fix: `session_start.sh` early-returns on the no-unread-messages path **before** the warmup line, so a stub answering `[]` never reaches the code under test and the timing arm reported **0ms against the mutant and passed**. That is rule 3i exactly — an observable that is not downstream of the mechanism — found only because the mutant was run rather than reasoned about. Repaired with a marker control asserting the warmup line is REACHED; the arm now goes **1s → 11s RED** on the mutant, which was asserted LANDED by sha256 and parsing under bash 3.2, then restored **byte-identical from a `cp` backup** (never `git checkout --`, which in a worktree would have deleted the work). Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, local HEAD == `origin/dev`, running skill byte-identical to `origin/dev` — `cmp` against the **resolved symlink target** in V1's checkout, with a deliberately-different file as a firing control — **16** checks on HEAD with **0** not-green, zero human directives since the watermark, no died-mid-flight traces (both motoko checkouts clean; the three open loop-authored PRs are all V1's). Weekly issue sweep not due (`#663` created 08-12, after the Monday-07:00 local boundary; 13 comments < 80). Gates run on **darwin/arm64 only** — windows and ubuntu legs unrun locally (rule 3b(viii)). No GPU work, so no `rig.lock`; `make quick-install` deliberately NOT run (shared-write guardrail). Metered **$0.00** of $5; no sub-agent spawned — the deliverable was controller measurement plus a one-line fix, so no role needed a pin. **Gate 3b GREEN on the merge commit `d7d2c0fdb`**: **16** checks, **pending=0**, **0** not-green, completeness assertion satisfied — `CI` (incl. **`launchd drivers (bash 3.2)`, the gate now carrying the new guard**), `Build and Release`, SonarCloud, CodeQL and the multivac deploy all present; the PR head `06c5b8946` was separately green at **21** checks with **4/4** required contexts passing. Five bounded poll windows; every window reading `notgreen=0` over a non-zero `pending` was recorded as vacuously green and NOT as a verdict. **Push-time collision again, and it is now the rule rather than the exception**: V1's iteration 202 landed four commits under me mid-poll and the PR went `CONFLICTING/DIRTY` on exactly one file — `changelogs/v0.18-current.md`, where both loops had appended an `[Unreleased]` entry. Rebased, kept BOTH entries, re-ran the guard post-rebase before force-pushing. Second consecutive iteration to hit cross-mission contention on *work* rather than on a git ref. **Operational residual, flagged not fixed:** the probe runs `claude -p` with cwd = `$MISSION_WORKDIR`, so the hook that executes is the one in *that checkout's working tree* — the fix is inert until each mission's clone is updated. motoko's was fast-forwarded to `d7d2c0fdb` this iteration (clean tree, re-verified at the moment of use) and now carries the redirect (control: the unfixed spelling greps to **0**). **V1's checkout is still at `28dcedae2` and still runs the unfixed hook** — its tree is dirty with benchmark JSONs, so it is not mine to pull; it will pick the fix up on its own next pull, and until then V1 keeps paying the refusal. NB V1's own `origin/dev` tracking ref reads level with its HEAD only because that clone has not fetched — a stale-tracking-ref reading, not a green.
## STATUS 2026-08-14 — ITERATION 4 COMPLETE: **R8 SETTLED → PORT, AND THE LADDER TURNS OUT TO HAVE NO LEVER AT ALL. ALSO LANDED THE RECOVERY JOB THAT MADE THIS ITERATION EXIST — IT WAS LIVE BUT UNCOMMITTED.** Two deliverables. **(1) A Gate-2 died-mid-flight trace, found by `git status` in the mission workdir:** an attended session authored AND installed `dev.ailang.mission-recovery-motoko.plist` at 08:44–08:45 and never committed it. Verified before landing rather than adopted: the committed file is behaviourally IDENTICAL to the copy actually running (`plutil -convert json` byte-equal on both, with the v1 plist as a firing negative control — the delta is comments only), and the job works by evidence rather than smoke test, because **this iteration exists because of it** (kickstarts at 08:45/09:05/09:25/09:45; the first three re-probed and refused, the fourth got through). Landed as `ceb2bb055`. **The measurement that justifies it is worse than the plist's own header claimed**, and was re-derived from the driver log rather than transcribed (rule 3b(v)(b)): **6 refusals against 4 starts = 10 fires, 60% refused**, not the "three of six" written at 08:44 — which was true then and superseded by the three refusals that followed it, so the header now says so. At `StartInterval=43200` each refusal costs a half-day. **Only the first refusal (08-12 09:19) carried a diagnostic** (the trust dialog, V22); every one since reads `captured output: ''` on all three models across both attempts — a hang, not a quota message, which the driver's "quota-limited, timed out, or errored" summary flattens away. **Not motoko-specific**, and the control is the whole point: v1 refused with the identical empty-output signature at 05:20/05:36/08:49 on 08-14, overlapping motoko's 08:47/09:07/09:27, in a separate checkout with separate config. Filed as new queue item **5a** (diagnose only — do NOT lengthen the 120s timeout before the mechanism is known), because recovery-by-brute-retry masks the defect from the human channel. **(2) The queue head's remaining unit — R8 — is SETTLED → PORT**, by building and running the instrument iteration 3 sharpened, not by re-reading: `tools/motoko/r8_headroom_band.ail` against `main_dst@6c06b08`, replicating the live `session.ail:2534-2561` wiring rather than calling the two `pure` functions in isolation. Three arms, both controls firing: **A** ladder returns `structural: tier=floor keep_last=1` at **79%**, seal sees 207,239 tok = 79% < 95% and returns **`Ok`** — it SENDS — leaving headroom **54,905 < the 65,536 output cap**; **B** small history → `PassThrough`/`Ok`, headroom 259,642; **C** 158% → **`Err(SealExhausted)`**, so A's `Ok` is a real permission and not a dead gate. **The mechanism is sharper than the verdict and it changes the upstream argument.** The sharpening pictured a transcript grinding the ladder down; what actually happens is the ladder has **no lever**: `elide_walk` only rewrites `role=="tool"` messages, so a large **user** message — the `docx_lambda` shape, a pasted document — is invisible to all four tiers, which remove ~**1%** between them (208,980 → 206,919 tokens). So the ask is not "make the ladder try harder", which is what an extension-side reserve would be; it is "the seal is the only place that can see this" — hence **one argument at `session.ail:2561`**, with upstream's own `:2534` (`context_limit - pinned_tokens`) as precedent, now confirmed by reading the call site first-party rather than inherited from the row. Ledger moves to **14 SUPERSEDED / 17 PORT / 14 DROP / 6 UNRESOLVED = 51** — first of the seven post-review UNRESOLVED rows to close, and it closed on the row's own decision rule. **Two instrument notes worth more than the result.** `main_dst`'s committed `ailang.lock` carries **absolute** paths from the upstream maintainer's dev container (`/workspaces/motoko_agent`, 19/19 entries), so from any local worktree every import fails with "package directory not found" — which reads as a broken checkout rather than a stale lock; `ailang lock` after `scripts/sync-extension-packages.sh` fixes it (control: `/workspaces` grep 19 → **0** against 19 total path entries), and this is now documented in `tools/motoko/README.md` so the next iteration does not re-pay it. And **an `ailang fmt`-shaped PostToolUse hook rewrote my `.ail` file on write** (stripped trailing `;`), which is exactly what the charter guardrail forbids on motoko sources — harmless on a throwaway probe of my own, latent if it ever fires on an upstream source. Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, local HEAD == `origin/dev`, running skill byte-identical to `origin/dev` (`cmp` silent — and checked against the **resolved symlink target** in V1's checkout, not the repo-local copy, which is a different file), **20** checks on HEAD with **0** not-green, zero human directives since the watermark (control: World's `#53` returns **1**, so the instrument fires). **`make quick-install` was deliberately NOT run**: an `ailang eval-suite` (3 models incl. `motoko-local-qwen3`) has been live since 07:58 on the shared `~/go/bin/ailang`, and the charter's shared-write guardrail makes swapping it mid-eval a language-regression-shaped hazard — so the probe ran on the installed `v0.33.1-23-g644cf178a-dirty`, and that narrowing travels with the result (rule 3b(ii)). No GPU touched (`pure` functions only), so no `rig.lock`. Metered **$0.00** of $5; no sub-agent spawned — both deliverables were controller measurement, so no role needed a pin. **Gate 3b GREEN on the pushed SHA `e1420e0b5`**: **16** checks, **pending=0**, **0** not-green, completeness assertion satisfied — both workflows this diff can trigger (`CI`, `Build and Release`) had runs on that SHA, and `Deploy Documentation to GitHub Pages` is correctly **N/A** (its push `paths:` filter lists `docs/`, `prompts/`, `llms.txt`, `CHANGELOG.md`, `internal/`, `cmd/`, `go.mod`, `go.sum` — my diff touches none). Four bounded poll windows were needed; the first three read `notgreen=0` over an incomplete set, which is vacuously green and was NOT recorded as a verdict. **And the green corroborates item 5a first-party**: `launchd drivers (bash 3.2)` is **completed/success in CI on this very commit**, while the same target is `10 passed / 25 failed` on the rig at the same SHA — so the divergence is confirmed on our own push, not just inferred from dev's history. **Push-time collision**: `origin/dev` moved under me — V1's iteration 198 landed the SAME orphaned plist ~15 min after I did (`f5a7ce8be`). Kept theirs, rebased mine on top as a comment-only correction (their header carried the "three of six" figure three later refusals had already falsified). Two loops, one orphan, no cross-mission lock — the Repo Profile's predicted contention, observed on *work* rather than on a git ref.

## STATUS 2026-08-12 — ITERATION 2 COMPLETE: **PHASE 0 IS NOW A BOUNDED FAIL-CLOSED GATE — AND THE PREDICATE AS FIRST DRAFTED COULD NEVER HAVE FLIPPED TRUE. ITEM 4 PARKED `needs-human-review` ON A DOC-vs-CHARTER CONTRADICTION.** Queue item 4 (head): designer pass on [m-motoko-dst-refactor-migration.md](planned/m-motoko-dst-refactor-migration.md) + the one re-quorum the item allows. Gate 0/1 clean — the RUNNING skill is byte-identical to `origin/dev` (`cmp` silent) and local `dev` == `origin/dev`, so no reconcile was owed; 18 checks on HEAD, 17 green, `test` in-flight, no RED to outrank the queue. Designer ran on `claude:claude-fable-5` (rotation start — the namespaced `mission-motoko-designer-rotation` was absent; probe rc=0, `claude-sub` wrapper, tripwire CLEAN), 11,159-byte directive, rc=0, **exactly one file touched, no git writes**. **R1's two objections are answered and were NOT re-raised at R2**: Phase 0 became a conjunction of four predicates each carrying its evaluating command and observed value, a **28-fire (~14d) timebox** at the charter's 12h interval, a structured BLOCKED expiry escalating to Mark, and a declared human residual; and the four "Port — carry forward" claims got rows V21–V24, each with a **same-scope** known-positive control. **The iteration's sharpest find is a defect in the designer's own output, caught only by running the predicate table verbatim instead of reading it (V25):** the draft wrote G2 as `git cat-file -e origin/main:packages/…` with **no repo**, and `origin/main` is ambiguous across this mission's three repos — from this checkout it is `rc=128 invalid object name`, because the anchor repo's default branch is `dev`. The wrong-repo error and the genuine path-absent answer **both return 128**, so an `exits 0` test cannot separate them and **G2 would have read FALSE forever, including after `#154` merges** — a gate that can never open, wearing a correct gate's clothes. Fixed by pinning `-C` and pairing the predicate with a `README.md` control that proves the ref resolves. **R2 BLOCKED on two NEW internal-consistency defects**, both measured rather than forwarded (rule 3f): `gemini-3-1-pro` found (i) the Design Freeze said "check off before *sprint-executor* starts" — a **deadlock**, since Phase 0 is precisely what the executor runs to evaluate G1–G4, silently restoring the unbounded wait R1 rejected (fixed with its verbatim wording) and (ii) the Phase 1 ordering rationale claimed `compaction-ai`'s `on_pre_step` "genuinely needs all ten effects" — **refuted and WORSE than filed** (V26): the row is `! {AI, IO, Trace}`, **three**, and the ABI commentary the sentence cited *as its authority* is one upstream has **retracted** — *"it was taken as given … over-declared by SEVEN"* — with `on_tool_handle`'s genuine ten-effect row in the same record as the firing control. `gpt5-6-sol` found the gate authorises Phase 1 on G1–G4 while the doc's own Dependencies demand more — and the contradiction reaches **outside the doc**, because the **ratified charter guardrail** requires "#154 merged **AND** Arni has declared the ABI stable", which the R1 *Declared residual* had quietly relaxed. Its fix has two branches, one needing the named decision owner, and branch A's G6 would make queue item 11 — itself Phase-0 gated — a Phase-0 predicate, a dependency loop. **Choosing is Mark's call, so item 4 is PARKED `needs-human-review` rather than force-passed**, the one-revision-one-requorum budget being spent. **Nothing is blocked by the park**: Phase 0 measures CLOSED today (G1 `#154` OPEN with a firing MERGED control, G2 `packages/` absent from `origin/main`, G3 the registry exposes only 1.0.0–2.2.0 against a non-empty 4-version control), so no sprint existed to hold. Metered **$0.096** of $5. Both reviewers present both rounds, `absent_reviewers` empty — no N−1 hole.

Rotated out of [motoko-mission.md](motoko-mission.md) by Gate 4, which keeps the newest **3**
stamps in the charter. Append-only; the per-iteration narrative lives in
[motoko-mission-log.md](motoko-mission-log.md). The pre-2026-08-12 charter follows below.

## STATUS 2026-08-12 — ITERATION 1 COMPLETE: **THE FIRST ITERATION THAT ACTUALLY RAN. QUEUE ITEM 3 LANDED — 51 FORK COMMITS DISPOSITIONED — AND THE INDEPENDENT JUDGE BROKE 2 OF 16 `SUPERSEDED` ROWS, BOTH REAL.** Iteration 0 was bootstrap and the 09:11 fire died on V22's trust-dialog defect, so this is the loop's first end-to-end pass. Gate 1 found local `dev` **4 behind** origin with a clean tree; reconciled via `git checkout -B dev origin/dev` after measuring all four obligations with firing controls, and confirmed git wrote a **new inode** (35083371→35155023) so the in-flight driver kept its old fd. **Quorum-at-pick BLOCKED the migration doc** (both reviewers present, `absent_reviewers` empty, **$0.064**) and per rule 3f both objections were MEASURED rather than forwarded: `gemini-3-1-pro`'s "the four Port claims are unverified" is procedurally right and **substantively refuted 4/4** (it needs verification rows, not a redesign); `gpt5-6-sol`'s "Phase 0 is an unbounded wait" is **upheld — and its obvious remedy is vacuous**, because ABI 5.0 declares `[stability] level = "stable"` and so does the 2.2.0 we are pinned to and call unstable, so a gate on that field passes immediately and falsely. That is the sharpest negative result of the iteration and it is now queue item 4's starting point. The pick itself produced [m-motoko-fork-disposition.md](planned/m-motoko-fork-disposition.md) (`752254d3f`): **14 SUPERSEDED / 16 PORT / 14 DROP / 7 UNRESOLVED = 51**. Two instruments were retired before they could mislead: the range holds **52** commits but `ed61097` is a merge whose second-parent diff is empty, so 51 is the ceiling and the doc's own success criterion asked for a row that cannot exist; and **path existence is not a supersession signal here** — 80 of 94 touched paths survive upstream, but `agent_loop_v2.ail` is **4,005 B** there against **95,868 B** here, so the paths survive as facades while the substance moved, and a sweep built on that test would have been ~85% wrong. The `codex:gpt-5.6-sol` executor ran as pinned (probe rc=0, no lane degradation) and its evidence held under three first-party re-runs that reproduced exactly, file:line and denominators included — but the **sonnet** evaluator (generator≠judge: openai executor, anthropic judge) still **FAILED it 65/100** and was right twice, both in `SUPERSEDED`, the one verdict whose error is irreversible: **R16** cited `should_retry_stream_error` by name while its body is `retryable && retry_enabled && budget > 1` and `session.ail:2379` builds `code: "Internal"` with `retryable: false` **in the file the row cited**; **R34** named `phase_vocab.ail` as superseding a resolved-config broadcast, but that module is types and four candidate tokens measure **0** upstream against a firing 29-file control. Both were the failure mode the directive had named as the primary attack surface — a token match read as a behavioural equivalence — and both were reproduced by the controller before the downgrades were applied. The artifact landed as **REVISED-after-FAIL, not as a pass**, and the UNRESOLVED count went **up** under review, which is the direction to trust. Gate 3b: 13 of 14 checks green on the pushed SHA with `test` still running at the poll's bound, recorded as **not-yet-green** rather than waved through.

## STATUS 2026-08-12 — ITERATION 0 COMPLETE: **CHARTER RATIFIED BY MARK; QUORUM BLOCKED TWICE AND ALL FOUR OBJECTIONS WERE TRUE; THE V21 DRIVER DEFECT IS FIXED FOR v1+motoko.** Bootstrapped per [mission-bootstrap.md](../docs/docs/guides/mission-bootstrap.md) (separate checkout, env profile, plist, kill switch ON, dry-run isolation proven). Charter put through `ailang design-quorum` twice, both reviewers present both rounds, **metered $0.115 total**. Round 1 — `gpt5-6-sol`: no Premise Verification Log for operationally decisive claims. `gemini-3-1-pro`: clause 6.2 designs a fallback with no loud signal, **in a charter that cites the World mission losing five iterations to exactly that**. Both fixed (V1-V18 log added; 6.2 now requires a degradation-time notice). Round 2 — `gpt5-6-sol`: the *live* `codex→pi→opus` chain has none of the safeguards clause 6 demands of the future motoko lane. `gemini-3-1-pro`: V19 illegitimately inherited a **per-working-tree** build artifact claim from V1. The V19 fix (running `make quick-install && make build` here) then surfaced **V20 — `quick-install` writes the SHARED `~/go/bin/ailang`** that V1 and the eval rig resolve through, a cross-mission side effect no reviewer could have predicted. V21 measured the round-2 objection and **confirmed it: lane demotion is logged (driver 360/392) and never posted to the bookkeeping issue — a defect affecting v1, world AND motoko.** Re-quorum budget was exhausted (one re-run, per the guardrail), so V21 was escalated rather than ping-ponged. **Mark ratified the bar + queue and routed the V21 fix to this mission**, which then landed it in the driver: a degradation ledger accumulated at both probe loops and emitted ONCE, after every early exit and before the iteration starts, over the same two channels the driver's four existing report sites use. Deliberately NOT fail-closed on the post — aborting would make GitHub availability a hard dependency of every fire — but a failed post is itself loud, which is the one thing the old path never was. Tested at both halves without spending an iteration: five stubbed-channel tests over the real emit block (fires when degraded; **silent when healthy**; `gh` invoked; `gh` failure warns and does NOT abort; unset issue warns), plus a forced codex failure proving accumulation (probe rc=1 → handed to the pi lane). The seam between the two halves is now permanently testable: `MISSION_DRY_RUN=1` reports `lanes=ok` / `lanes=DEGRADED(n)…` — both arms exercised. **World is still owed the fix** and cannot take it by copy: its driver has drifted 233 lines (165 ailang-only / 68 World-only) and its fallback site is shaped differently (`_fb`), so it is handed over via the cross-mission channel rather than overwritten mid-flight.

---

# MISSION: Make Motoko the Best AILANG-Specific Code Harness

**Type:** Long-running mission (advanced in downtime, e.g. while evals run)
**North star:** motoko should be the BEST harness for writing AILANG specifically — exploiting structural
advantages a generic harness on an untyped language cannot (typed-interface reads, AST edits/queries, effect
rows, contracts + Z3 verification, exact best-of-N selection), with robust large-context AILANG synthesis and
CORRECT, consistent delivery of the canonical AILANG teaching prompt into every session.

**MILESTONE ACHIEVED (2026-06-21), NOT THE GOAL:** "match or beat pi" is DONE — motoko = pi at pass@1 (parity)
+ ahead on best-of-N; best-of-N shipped as a first-class rotation metric. Do NOT re-derive "beat pi" as the
goal or declare victory on it again — that bar is behind us.

**CURRENT GOAL = best AILANG-specific harness. The frontier is HARNESS CORRECTNESS + AILANG-native power:**
1. CANONICAL PROMPT DELIVERY — the versioned teaching prompt (prompts/versions.json / `ailang prompt`) must be
   inserted into EVERY motoko session consistently (persistent system prompt, like all other evals). The
   recurring system-prompt failure. NEVER duplicate or approximate it with ad-hoc cheat-sheets — one source.
2. RESPECT CONTEXT LENGTH — compaction must use the model's real token accounting (provider input_tokens /
   count tool_calls, not a content-only estimate) so sessions never overflow the model window.
3. UNCAUGHT HARNESS ERRORS — premature stops / disengagement are HARNESS bugs (swallowed errors), NOT model
   capacity. Every such case this arc was a harness bug. Find the error; do not band-aid with heuristics.
4. AILANG-NATIVE POWER — AST edits/queries, typed-interface reads, contracts+Z3, exact best-of-N: the moat.

**The standard set is now SATURATED** (both harnesses 100% best-of-N ceiling, 0 hard-fails) — it can no
longer discriminate "best vs equal." **Remaining (optional, "best not just equal"):** the large-context
frontier (P0, ailang-parse repo-source reimplement instrument) where pi and motoko actually diverge — a
deliberate focused-session build, not a cron task. See [[motoko-strategic-goal]] +
the [analysis log](motoko-harness-analysis-log.md).

**STATUS 2026-06-22 — P0 LARGE-CONTEXT GRADED (the frontier moved; two desynced sessions merged here).**
First real `docx_reimplement` grade (727-line parser reimplement): motoko **0/17**, but the **timeout fix is
VALIDATED** — 48 steps to completion, no "context deadline exceeded" (prior runs died at 14/27 on the 300s
cap). So 0/17 is a genuine capability result, and the failure is exactly the discriminating signal P0 exists
for: **on a 727-line generation qwen's AILANG syntax fidelity DRIFTS** (emits Haskell `\x.` lambda instead of
`\x ->`; bad `match` arm) → the package won't COMPILE. Compounded by **under-testing** (only 3 `ailang check`
calls in 48 steps despite 40+ spare → it never caught its own parse errors). The short standard benchmarks
hide this; the large-context instrument surfaces it.

**Re-prioritized "beat pi" levers (now DATA-BACKED, not speculative) — the path to a passing large-context grade:**
1. **best-of-N + EXACT selector (P1, BUILT):** N attempts, reject non-compiling → a syntactically-clean
   candidate likely appears across samples. Directly rescues the syntax-drift class. **TOP lever — try first.**
2. **compile-gate-before-done (DP7):** force `ailang check --package` green before "done" (motoko had 40+
   spare steps). DP7 either isn't in this rig build or didn't fire on this task — re-verify/enforce.
3. **EditDecl / ast-edit (P3, [`planned/m-motoko-editdecl-astedit.md`](planned/m-motoko-editdecl-astedit.md)):**
   smaller per-edit surface → less drift than a 727-line monolith write.
4. **R1 syntax-fidelity diagnostics + R1-glue contract verification:** surface `\x.`→`\x ->` as an
   actionable error; verify candidates against provided contracts. The `\x.` drift is qwen-on-AILANG friction
   (hits ANY harness) = an AILANG eval-gap, NOT motoko-specific.

**Infra cleared (both sessions): /v1 timeout fix deployed (runtime-process.ts:357) + upstream DRAFT PR #65;
parser `s[0]` panic FIXED on dev (dbc8bf391); CI green (gofmt + approvaltoken). R1-glue MVP done
(astedit.InjectContract + `select-best --contract-spec`); next gate = multi-function body-less `contract_spec`
extraction + a HARDER contract benchmark (current contract_* are saturated, 0 runs-but-wrong → no lift to show).**

## Roadmap: BEYOND parity (2026-06-21) — exploit what pi structurally can't do

The standard set is saturated (motoko = pi). Beating pi requires AILANG-native structural advantages a
generic harness has no access to. Priorities (mine):

- **R1 (TOP — build now) — Contract-aware best-of-N selector.** Extend the best-of-N selector (select-best
  / the rotation rollup) to reject runs-but-WRONG candidates via AILANG **contracts** (`ailang run
  --verify-contracts`, or Z3 `ailang verify`). pi has no typed verifier → it keeps selector-misses (the
  head-to-head `pipeline` case: pi picked runs-but-wrong; motoko 0 misses). The `contract_*` stretch
  benchmarks are the proving ground. **Lever class: AILANG-native MOAT.** Small build (Go/eval-harness).

- **R2 — Real-codebase "evolving codebase" eval (design-doc → sprint → motoko-execute → compare-reference).**
  The realistic instrument that replaces saturated synthetic benchmarks (the user's idea). **TARGET = real
  codebases WRITTEN IN AILANG, NOT the ailang-core repo's design_docs/ (those are the Go COMPILER — wrong
  substrate).** Use: `ailang-parse` (docx/office parsing), `ailang-demos` (ecommerce/BigQuery/budgets),
  `docparse`, `motoko_agent` (`src/core/*.ail`). Pipeline: take a feature/design-doc from one of THESE
  AILANG projects (reference AILANG implementation exists) → sprint-plan → motoko executes on the pre-feature
  copy → grade vs the reference (its AILANG tests + diff: "did motoko match or BEAT the human solution?").
  docx-reimplement (ailang-parse) is the first instance — already AILANG-source. Tests ALL differentiators
  on real evolving-AILANG-codebase work.

- **R3 — Cross-model + cross-language generality study.** Once motoko is optimal on-device: motoko vs pi/
  opencode on BIG openrouter models (gpt5, opus, gemini-3) + across langs (ailang/python/js/go). Q: (a) do
  motoko's gains hold with strong models (model-independent harness win)? (b) AILANG-specific or general?
  **Generality split:** best-of-N (check+run) is LANGUAGE-GENERAL (any compiler+runtime) = portable edge;
  contracts (R1) are AILANG-SPECIFIC = the moat. This positions motoko: portable advantage vs substrate moat.

Sequence: R1 (lever, now) → R2 (instrument, measures R1 + context_mode on real work) → R3 (positioning).
docx-reimplement = R2's first instance + the context_mode/large-context proving ground.

**Large-context infra prerequisites (surfaced by docx-reimplement, 2026-06-22) — clear these for a clean P0/R2 grade:**
1. ailang parser PANIC on `s[0]` index access → **FIXED on dev** (dbc8bf391; nil-guard + regression test).
2. ollama `/v1` TOTAL timeout aborts legitimately-long requests ("context deadline exceeded", killed docx at steps 14 & 27). Band-aid: raise total to 1800s (motoko_agent#65). **Robust fix designed:** [`planned/m-ollama-v1-streaming-idle-timeout.md`](planned/m-ollama-v1-streaming-idle-timeout.md) — stream `/v1` + idle/inter-chunk deadline (tolerates long generation, still catches hangs). P1, v0.26.0.
3. Root lever for prefill size = **P2 context-compression** (a bigger timeout can't shrink the prompt). Sequence: #65 (unblock now) → streaming-idle-timeout (robust) → P2 (root).
4. Write-side efficiency = **EditDecl/ast-edit** ([`planned/m-motoko-editdecl-astedit.md`](planned/m-motoko-editdecl-astedit.md)) — replace one decl by parsed span instead of re-emitting the whole file (docx had motoko write 526 lines). Shrinks WRITES (per-turn prompt growth); P2 shrinks READS. AILANG-native; pi's line-edit tools can't. P3, trigger now met.

## AILANG AST/type advantages — examined + prioritized (2026-06-22)

The "beat pi" thesis rests on structural advantages a generic harness on an UNTYPED language cannot access.
AILANG hands the harness a real typed AST (HM types, effect rows, `requires/ensures` contracts, span-based
stable IDs) plus two verifiers (`ailang check`, `ailang verify`/Z3). Audit of what we exploit vs the gap:

**EXPLOITED (shipped):**
- Type + effect checking (`ailang check`) as a best-of-N tier — HM inference + effect-row checking.
- Runtime contract checking (`ailang run --verify-contracts`) — rejects runs-but-WRONG candidates (R1).
- **Z3 SMT static verification (`ailang verify`) as the TOP selector tier (2026-06-22, `select-best
  --verify-z3`)** — PROVES a contract for ALL inputs (or returns a counterexample), vs one runtime input.
  (Had been dormant — z3 wasn't installed on the rig.)
- Provided-contract injection (`astedit.InjectContract` → `select-best --contract-spec`) — verify against the
  benchmark's contract the model omits (44/48 drop it).
- AST-anchored decl LOCATION for edits (`ailang ast-edit replace` parses to find the exact decl span).

**UNEXPLOITED — the opportunity (ranked; HIGH/MED/LOW):**
1. **[HIGH — in flight] Z3-verify everywhere it pays.** Wire the `--verify-z3` tier into the eval-rotation
   best-of-N, and auto-couple `--contract-spec` + `--verify-z3` (inject the provided spec, then Z3-PROVE it —
   not just runtime-check). Strongest reference-free signal; pi has no equivalent. Tier built; integration +
   a harder contract benchmark that elicits prove-vs-counterexample divergence remain.
2. **[HIGH — data-backed] EditDecl (AST-span decl-replace) in motoko's edit loop** (= existing P3, elevated).
   motoko whole-file-writes (727-line monolith → AILANG syntax drift `\x.`); editing ONE decl at a time via
   `ailang ast-edit replace` shrinks the surface → less drift. Design done (`m-motoko-editdecl-astedit.md`);
   CLI exists; fork wiring pending. The docx 0/17 directly justifies it.
3. **[MED] Type/effect-aware edit validation.** astedit holds the full typed AST (ReturnType, Effects) but
   uses only the span; validate that an edit preserves the signature + effect row (reject silent pure->effectful
   drift) — catches wrong edits at edit-time, not post-hoc.
4. **[MED] Type-directed context** (= existing P2 context_mode, reframed). Feed the typed AST / a projection
   (signatures, effect rows, contracts, type-directed slices) instead of raw file text → smaller, more legible
   context. The STRUCTURAL answer to the context-bloat that timed out docx (vs the blunt timeout bump).
5. **[LOW] SID-anchored edits.** FuncDecl carries a span-based SID (stable structural id); anchor edits by SID
   (rename/duplicate-name robust) vs the current name-match. Marginal until rename/collision thrash appears.
6. **[LOW] Typed AST-diff surfacing between turns** (compaction-resistant re-grounding) — blocked on the
   faithful AST<->source formatter (backlog), so lowest until that lands.

**Rationale / sequence:** #1 and #2 are the two HIGH levers, both data-backed by the docx 0/17 (under-testing
-> stronger verification/gates = #1; syntax-drift -> smaller AST-scoped edits = #2). Sequence:
**#1 (extend the shipped Z3 tier) -> #2 (EditDecl, kills the monolith-write drift) -> #4 (type-directed
context) -> #3 -> #5/#6.** The honest framing from the 2026-06-22 review: the EDITOR is still text-splice
(only decl LOCATION is AST-aware); the deep AILANG advantage lives in the VERIFIER (now incl Z3) and in
type-directed context — that is where the moat compounds.

## How the mission runs (each cycle)

A cycle picks up **one** item and runs the full record-keeping flow:

1. **Observe** — read the latest rotation/eval numbers (`os/latest.json`,
   `eval_results/rotation/os-rolling`) + the [analysis log](motoko-harness-analysis-log.md).
   Append a new analysis-log entry if there's new failure data.
2. **Pick** — take the top open item from the Backlog below (or a newly-found one).
3. **design-doc → sprint-plan → execute** — same flow as M-EMBED-TASK-PREFIX /
   M-EVAL-OS-LONGITUDINAL, so every change has a design doc + sprint record.
4. **Land** — per the routing rule below. Verify locally before landing.
5. **Record** — update the analysis log (prior-action status), tick the Backlog,
   re-measure on the rig next cycle.

## Routing rule (where changes land)

| change is in… | lands as |
|---|---|
| **AILANG** (`internal/…`, `cmd/…`, eval rig, `tools/…`) | **commit to `dev`** (this repo) |
| **motoko_agent** (`.ail` core, profiles, prompts, TS) | **PR** to `arniwesth/motoko_agent` (via our `sunholo-voight-kampff` fork) — verified working locally first |

## Reference harness

**pi** = `@mariozechner/pi-coding-agent` → `@mariozechner/pi-ai`. It's motoko's
inspiration and the 96% bar. Key learnings (mine the source under
`/opt/homebrew/lib/node_modules/@mariozechner/pi-*` and `internal/executor/pi/`):
- Drives ollama via **OpenAI-compat `/v1/chat/completions`** (no native ollama
  provider) — the reason its tool-calling is reliable on qwen.

## Backlog (prioritized — top = next)

**[NEXT — 2026-06-20] CONVERGENCE EFFICIENCY (context hygiene).** Truncation + disengagement are
fixed; motoko is at pass-rate parity with pi (88.9% vs 90.4%) but **3–10× less efficient** (pi median
5 turns vs motoko 15–50). Source+transcript root cause: motoko's **verbose tool results + 70%
auto-compaction erase the model's own writes**, forcing re-reads/rewrites (vicious cycle). Plan in
[`planned/m-ailang-semantic-context.md`](planned/m-ailang-semantic-context.md): near-term = match pi
(truncate tool results, raise compaction floor, echo writes; fork PR, A/B by turns-to-success), then
AILANG-native semantic-context routes (distilled diagnostics, type/effect-directed surfacing, AST
diffs, trace distillation, typed projection layer). Pending cheap-confirm: `wire_diag` elision capture.

0. **[LANDED 2026-06-19 — TRUNCATION fixed.]** max_tokens floor 16384 (fac848054): disengaging
   benchmarks **21%→79%**, finish=length 11→0. Per-model precision plumbed (006a679a6 + motoko PR #48:
   registry max_output_tokens→motoko via AILANG_OLLAMA_MAX_TOKENS). NEXT (blocked on fresh rotation
   data): re-run Gate 1 to measure the full gap drop. Original finding below:
   **[ROOT CAUSE: TRUNCATION.]** Wire-proven: motoko's
   disengagement = qwen3.6 thinks ~4k+ tokens (median 13.9k chars reasoning) and **`finish_reason=length`
   truncates at `max_tokens=4096` BEFORE the tool call** (11/14 disengaged turns = length). pi sends
   `max_completion_tokens=16384`. **Fix: raise the agent/ollama max_tokens to ≥16384** (pi-faithful;
   AILANG default is `internal/ai/handler.go:95`). Also: motoko's `enable_thinking:false` is DROPPED
   before /v1 (never forwarded) — optional second fix (forward a real thinking-disable param). Reasoning-
   parsing/hermes hypothesis REFUTED (0 tool calls found in reasoning); reasoning-CAPTURE done anyway
   (79714e3d5, c1f87275e) — it's the observability that found this. Next: raw-replay confirm
   (max_tokens=16384 → qwen finishes?), then raise + A/B by disengage-rate. Routing: AILANG default vs
   motoko per-request max_tokens (motoko has no max_tokens config knob today).
0b. **[eval rig — user priority] Add `qwen-code` (QwenLM/qwen-code) as an eval-suite harness arm.**
   qwen's own coding agent (OpenAI-compat/ollama, CLI like opencode/pi → fits the executor contract).
   A qwen-tuned reference on the SAME benchmarks — directly measures the motoko↔well-tuned-harness delta.

1. **[THE gap, precisely located 2026-06-18 — DISENGAGEMENT, needs GPU to fix].** Rotation-scale
   failure-mode segmentation (`tools/eval_failure_modes.py`): the motoko↔pi gap (+26pp) is
   **entirely disengagement** — motoko fails with ≤2 tool calls (prose / one inspect call) **29%**
   of runs vs pi's **3%**; grind-wrong (engaged-but-incorrect) is ~1% for both, NOT the gap.
   **7 always-disengage benchmarks** (3/3 fail, 0 tool calls): csv_to_json_converter,
   log_file_analyzer, graph_bfs, polymorphic_ord_defaulting, run_length_encode, symbolic_diff,
   config_file_parser. **Next (GPU):** request-dump the FIRST turn of those 7 to see WHY qwen emits
   0 tool calls (tool schema not seen? task framing? result format?), then a targeted fix A/B'd by
   the **disengage-rate delta** (not just pass rate). Prior fixes touched the symptom not the 29%:
   - *Ruled out — M-MOTOKO-PERSIST-NUDGE* (PR arniwesth/motoko_agent#47): forces continuation AFTER
     disengagement; +3/18 on a biased subset; pi has no such mechanism. Kept default-off; divergent.
   - *Ruled out — M-MOTOKO-AGENT-SYSTEM-PROMPT*: lean agentic system prompt, proper core-tier A/B =
     **+1/52 (null)**; the 6-flaky smoke (+14pp) did NOT generalize. Knob kept, not productionized.
   - *Ruled out — system-role delivery* (teaching → system slot): A/B −2/18.
2. **[RULED OUT 2026-06-19 — SAMPLING is already optimal].** Wire-verified motoko sends only
   `{model, max_tokens}` (no temp/top_p/top_k); ollama then applies qwen3.6's OWN modelfile vector
   (`temperature 1, top_p 0.95, top_k 20, presence_penalty 1.5`). So motoko already runs the model's
   recommended sampling — there is NO sampling lever (the "forcing greedy temp 0" premise was false;
   `resolveOllamaTemperature`→0 is omitempty → unsent). The `AILANG_OLLAMA_TEMPERATURE` knob stays as
   an opt-in override only. See analysis log 2026-06-19 SAMPLING-RULED-OUT.
3. **[THE residual, data-elevated 2026-06-19 — CONVERGENCE/correctness, partly SHARED with pi].**
   Post-truncation-fix the gap is no longer disengagement. Partial h2h (n=18 ea): motoko 88% vs pi
   94%; motoko's only net loss = `explicit_dataflow_ssa` (grind, 32 tool calls fighting AILANG
   `expected float arguments` numeric-type friction), and `csv_to_json_converter` fails for BOTH
   harnesses (shared qwen-on-AILANG limit). "Step budget too low" ruled out (motoko runs ~50 steps
   via `rpc.ail` clamp). **Next (disciplined, NO pre-committed lever): complete the full head-to-head
   as a clean MEASUREMENT, read EVERY residual transcript, then classify each as (i) motoko-specific
   & fixable harness lever, or (ii) shared qwen-on-AILANG capability friction → AILANG eval-gap item,
   not a motoko harness lever.** See analysis log 2026-06-19 SAMPLING-RULED-OUT / residual entry.

## Resolved / ruled out (this investigation arc)
- **System-role delivery** (M-MOTOKO-SYSTEM-ROLE, `7a0caf7a`): A/B off-vs-on (6 flaky ×3, 2026-06-18)
  = 10/18 → 8/18 (net −2, within noise) → **KEEP GATED, not default-on.** Retained as a lever: it
  lifts the FLOOR (config_file_parser/graph_bfs 0→1) and raises iteration depth (turns up across the
  board) — re-test combined with iteration-persistence (#1), not standalone. Clean injection
  mechanism shipped: motoko `--system-prompt` flag PR (arniwesth/motoko_agent#46, GPU-verified).
- **3-way capture** (motoko/pi/opencode, `f92df86b`): threading IDENTICAL across all three (chat
  history ruled out). Differentiator = TOOL SURFACE — opencode 33 tools (23 ailang-docs MCP) vs
  motoko 6 / pi 4 → keep motoko lean. Backs the "why motoko beats opencode" decision.
- **Prompt** is sub-dominant: agent-mode output-delivery override landed (+11pp, `2cbaf85a`,
  motoko 76%→83%); copying pi's full system prompt only added +1/18 (A/B) → NOT the gap.
- **Temperature** ruled out as the pi differentiator (pi runs qwen at the same 1.0 default).
- **Loop guards** (compel-write / definition-of-done) built + reverted (fired 0/18 or regressed).
- **Tolerant Hermes/XML parsing** — moot post-/v1; dropped.

## Done / superseded
- motoko ollama integration enabled + PATH/key rotation fix (this repo, landed).
- First failure analysis → root cause = AILANG-INTEGRATION (tool-calling), not language.
- **#2 ollama tool-calling over `/v1` (M-OLLAMA-V1-TOOLCALLING) — LANDED on `dev`
  (41c52ffe, 2026-06-17).** Root cause of the 26% closed: native `/api/chat` → 0 tool
  calls; now delegates to OpenAI-compat `/v1`. Aggregate confirmed: **AILANG 26%→79%**,
  motoko now BEATS opencode (72%) on AILANG, approaching pi (88%). Zero 0-tool-call runs.
- **`agent_tool_calls` surfaced in result JSON (M-MOTOKO-OBS-TOOLCALLS) — LANDED (e92bc4ba).**
- **/v1 HTTP timeout (63fc63e0) + native rig-lock in eval-suite (8a18e4e7) — LANDED.**
  Fixed a ~2h rig hang: /v1 (non-streaming, no timeout) stalled on a GPU-contention model
  reload; lock prevents concurrent rig jobs (fail-fast). The user's hypothesis — /v1 vs
  native endpoints caused the historical hangs — was correct.
- **Tool-call transcript retained (M-MOTOKO-OBS-TRANSCRIPT) — LANDED.** Parser kept counting
  tool calls but discarding content; now retains tool name + write path/content into
  `agent_transcript`. Unblocks root-causing the stub failures (backlog #1).

## Skill
When the analysis log has ~3+ entries and the cycle is repeatable, codify a
`motoko-analyzer` skill (the log template + lever taxonomy is the spec).
## STATUS 2026-08-13 — ITERATION 3 COMPLETE: **THE QUEUE HEAD'S PRECONDITION WAS AN UNBOUNDED WAIT ON A HUMAN WHO NEVER REPLIED — AND THE CASE IT WAS WAITING TO FILE WAS WRONG IN BOTH DIRECTIONS. ITEM 5's EVIDENCE HALF IS DONE; THE UPSTREAM ASK IS NOW ONE ARGUMENT.** Queue item 5 (head): "file the output-headroom case against `main_dst` **if Arni's #97 reply invites it**". Gate 0/1 clean — kill switch armed, `gh` on `sunholo-voight-kampff`, billing tripwire **CLEAN**, driver pin **pinned** (`7ea12af93`, source clone 38 behind — exactly what the pin exists to survive), the RUNNING skill byte-identical to `origin/dev` (`cmp` silent on BOTH the repo-local copy and V1's symlinked one), 16 checks on HEAD with **0** not-green, no died-mid-flight traces (both motoko checkouts clean, the one open loop-authored PR `#613` is V1's), zero human directives since the watermark (control: the allowlist script returns **2** Mark directives on World's `#53`, so the instrument fires). **Gate 2 refuted the pick's precondition**: **zero** `arniwesth` events on PR #97 across all four surfaces — issue comments, review comments, reviews, timeline — since our 2026-08-11T19:39Z comment, with a firing control (`commenter:arniwesth` → **34** issues/PRs in that repo). That is an unbounded wait on a third party, i.e. **the same defect `gpt5-6-sol` blocked Phase 0 for at iteration 1, sitting in the queue head one iteration after we fixed the identical shape next door** — the fix generalised to the gate but not to the queue. Replaced with a bounded rule (no reply by **2026-08-27** ⇒ file standalone + carry locally; a reply may still redirect to a PR — what expires is the waiting, not the offer). **The unblocked half — the evidence — is the iteration's real deliverable, and it says the case as written would have filed something incorrect.** Re-measured against `main_dst@6c06b08` (advanced from the `303d869` the doc's rows were taken at) as migration-doc rows **V27–V29**: **(a)** the live ladder targets **70%**, not the 95% we asserted — `register.ail:24` binds `compact_for_pre_step`, whose every branch gates on `candidate_fits` → `< result_target_pct()` = `elide_tier_pct()` = **70**, while `emergency_pct() = 95` occurs at exactly 3 sites, all inside the off-path `compact_step_with_limit` (control: the `result_target_pct()` grep returns 2 hits in the same file, so both patterns fire). On a 262144 window that is ≥**78,644** of headroom — **more than the 65,536 output cap and more than the 75k reserve `96542f8` adds**, so upstream already beats our patch *when the ladder reaches its target*. **(b)** The mitigation the doc credited, `try_emergency_compaction_with_limit`, has **ZERO production callers** — every importer of `compact_step_with_limit` is a smoke or integration test (control: `compact_for_pre_step` is imported by the live `register.ail:17`, so the instrument distinguishes live from test). **(c)** The live refusal nevertheless EXISTS and is loud: the phase core's `seal_compacted_payload` (`phase_vocab.ail:145`) at `exhaustion_pct() = 95` (`compaction.ail:30`), called at `session.ail:2561` with the **raw** `context_limit`, terminating with `CompactionExhausted` / `code: "ContextExhausted"` / `retryable: false`. So **"hard-stop, not silent corruption" is CONFIRMED — at a different function than we named**. The residual is therefore neither "no reserve" nor "no refusal" but **the band between the ladder's 70% aspiration and the seal's 95% permission**, whose predicate counts messages only. **This shrinks the upstream ask from "adopt an output reserve in a compaction extension" to one argument at `session.ail:2561`** — and the strongest precedent is one line up at `:2534`, where upstream **already** hands the extension `context_limit - pinned_tokens`, subtracting a reserve before delegating. **The sharpest instrument lesson, and it is V9's**: a negative-existence grep for the vocabulary of OUR fix (`headroom|reserve|output_budget`) establishes only that upstream does not spell it our way, never that it lacks the PROPERTY — upstream's protection is spelled `result_target_pct`, a word no such grep contains. V9's own row was true as measured; the behavioural clause the compaction table built on it was not, which is rule 3b(vi) inside a doc two quorum rounds had read. **Self-correction recorded rather than buried**: this controller's first reading was "the live path has no refusal at all" (from the unconditional `floor` branch at `compaction_structural.ail:191`) — **FALSE**, caught only by chasing the second `compaction_exhausted` site instead of trusting upstream's own ADR-002, which calls the off-path one "the only" and is itself stale. R8 re-sharpened from a rig A/B to one `pure`-function assertion. Metered **$0.00** of $5; no sub-agent spawned — the deliverable was controller measurement, so no role needed a pin. **Gate 3b GREEN on the pushed SHA `0ab437ec4`**: 16 checks, **pending=0**, **0** not-green, with the completeness assertion satisfied — both workflows a `design_docs/`-only push can trigger (`CI`, `Build and Release`) had runs on that SHA, and `Deploy Documentation to GitHub Pages` is correctly **N/A** (its push `paths:` filter does not list `design_docs/**`), recorded as N/A rather than pending. The long `test` job was `in_progress` across two poll windows and only the third reached `pending=0` — the two earlier windows read `notgreen=0` over an incomplete set, which is vacuously green and was NOT recorded as a verdict.

## STATUS 2026-08-23 — ITERATION 19 COMPLETE: **ROW 6c LANDED, AND THE ROW UNDERSTATED ITSELF TWICE — THE SELF-TEST WAS NOT A WEAK GATE, IT WAS NOT A GATE; AND TWO OF THE PROBE'S REFUSALS COULD NOT REFUSE.** Pick was the queue head, row **6c** (iteration 18's evaluator finding B2). PR [#831](https://github.com/sunholo-data/ailang/pull/831) → [`c1950750c`](https://github.com/sunholo-data/ailang/commit/c1950750c), evaluator **PASS 93/100, ZERO blocking**, all three non-blocking findings reproduced and closed in the same iteration. **THE ROW'S OWN PREMISE WAS TOO KIND, MEASURED WITH A SAME-SCOPE FIRING CONTROL:** `grep -rl test_motoko_connection_probe make/ .github/workflows/` returns **0** files, the sibling control `test_fmt_ab_schedule` returns **1** (`make/test.mk:43`), and a repo-wide sweep over `*.mk`/`*.yml`/`Makefile` also returns **0** — so the self-test row 6c calls under-covering ran **nowhere at all**. Wired into `test-launchd-drivers` (the target `ci.yml:507` runs) under an explicit `/bin/bash`, the rig's being **3.2.57**; proven by making the self-test exit 1 and watching the target go rc=2, then restoring byte-identically. **THE LOAD-BEARING FIND IS A SOUNDNESS DEFECT IN THE INSTRUMENT, NOT A COVERAGE GAP, AND IT WAS FOUND WHILE VERIFYING THE ROW RATHER THAN INHERITING IT.** Two arms each, exit codes captured without a pipe and printed beside each other: **(a)** `instrument_failure` does `exit 1`, and called from inside a command substitution it exits only the **subshell** — repro rc=**0** against a control (same call outside `$( )`) rc=**1**; `descendant_pids`'s process-tree deadline was exactly that shape, so `pids` became `""` and the probe carried on. **(b)** `lsof -nP -iTCP -sTCP:ESTABLISHED -a -p ""` returns **75** lines, rc=0, **empty stderr** — byte-for-byte the count of the fully unscoped query (**75**) — against a control with a real pid holding no established TCP at **0** lines, rc=1. **An empty pid list does not narrow the scope, it REMOVES it.** Chained, an instrument whose entire job is to certify *no OpenRouter connections* would sample every established connection on the machine and could then pass on another process's `127.0.0.1:11434` or fail on another process's OpenRouter peer — silently, behind `2>/dev/null || true`. **False certification in BOTH directions**, on the probe that gates a mission KPI. Fixed by making `descendant_pids` report through a status the caller checks and asserting every lsof scope is a non-empty comma-separated pid list before use; audit of the whole script for the same shape re-derived at **0** remaining, and the evaluator attacked that claim twice — including re-introducing the pre-fix shape verbatim — and could not break it. **ROW 6c's NAMED NEXT STEP DONE:** the `trap … EXIT` no longer discards both lanes' driver logs and lsof captures; retention runs FROM the trap so it fires on the **refusing** path, which is the case the log exists for, and a failed copy is itself an instrument failure. The evaluator confirmed under a real `SIGTERM` that retention fires and the 143 is not masked. **COVERAGE RE-DERIVED, NOT TRANSCRIBED:** 17 `instrument_failure` call sites + 6 `usage` refusals = **23** branches, of which the suite reached **4** (the `assert_*` front doors); the evaluator's B2 demonstration reproduced first-party — neutering the darwin/arm64 gate with `if false && …` (mutant LANDED by sha256, VALID by `bash -n`) leaves stdout **byte-identical at `PASS: 8`**, rc=0 both arms. Now **34** arms, live path driven hermetically through a stub `AILANG_BIN` and a pruned `PATH` — no eval run, GPU, ollama or network call. **THE EXECUTOR SELF-REPORTED FOUR ARMS AS NOT HONESTLY PROVEN** (its mutation batch had been contaminated by a concurrent process mutating the shared probe) — better evidence than a silent run (rule 3h(d)) — and the controller re-proved them **sequentially**: `dig`/`lsof`/`jq` each rc **0 → 1** dying on its **own named message**, plus a `pgrep` control that also reds. Note what the tightening bought: those three arms die with *"lacked expected message"* rather than *"unexpectedly succeeded"*, i.e. a later gate refuses too and the **coarse assertion would have passed** — rule 3i's "what else writes this value", met in the executor's own output. **A DEFECT OF MY OWN, IN THE HARNESS WRITTEN TO CLOSE THIS ROW.** Isolating the fourth flagged arm with a mutant that keeps retention on the success path and drops it on the refusing one (the exact "reachable only on the success path" defect the directive named) killed the suite rc **0 → 1** on `refusal lost treatment.driver.log` — but the suite printed **`ok 29 - refusing live path still retains both lanes diagnostics` FIRST**. The `expect_failure` arm observes only that the probe refuses with the control-void message; the retention assertion is a loop **outside any arm**. The gate had teeth and the **label** did not. Both arms renamed to what they observe and each retention loop given its own `pass_arm`; re-run under the same mutant the named arm no longer prints ok (grep count **0**). **EVALUATOR: PASS 93/100, ZERO BLOCKING** — it attacked the two soundness targets and the controller's own repair and broke none of them. All **three** non-blocking findings reproduced by command before acting (a NON-BLOCKING label is the judge's opinion of severity, not a measurement) and all three closed: **(1)** the **real** wall-clock deadline inside `descendant_pids` was never exercised — arm 25 reached that refusal only via the `PROBE_TEST_DESCENDANT_FAILURE` short-circuit, and the `PROBE_TEST_PGREP_LOOP` stub written to drive the in-loop check (it makes the process tree self-referential) was **defined and never used**, grep returning the definition and nothing else; now arm 33, proven by `return 1` → `return 0` (rc 0 → 1, arm dies by name, falls through to `assert_pid_scope` — defence in depth demonstrated rather than assumed). **(2)** No gate against refusal-branch drift: reproduced the evaluator's mutation, a **24th** `instrument_failure` leaves the suite byte-identical at `PASS: 32`, rc=0, shellcheck rc=0. Every other arm proves a branch that EXISTS reds when neutered — **a removal proves the check FIRES; only an addition proves it LOOKS** (rule 3a(i-e)). Arm 34 counts the branches (18 + 5 = 23) and refuses when the number moves, with an anti-vacuity floor so a counter matching nothing reports INSTRUMENT FAILURE rather than a clean result; proven by re-running the 24th-branch mutant → rc 0 → 1, `refusal-branch drift: probe has 24 refusal branches`. **(3)** `PROBE_TEST_FORCE_TREATMENT` was inert residue (1 hit in the suite, **0** in the probe; control `PROBE_TIMEOUT_SECS` = 3) — removed. **V38 IS NOT ISOLATED AND THIS SAYS SO.** The mechanism by which the probe as shipped breaks the runs it observes still lies in the deadline-carrying `descendant_pids` or the two-lanes-in-one-process sequencing, and isolating it needs the rig. What changed is that the next rig run will **have the driver logs**, which was this row's own named next step; the two soundness defects fixed here are live candidates but were **not** demonstrated to be the V38 mechanism, and are not claimed as it. **GATE LIST BASELINED BEFORE THE DIRECTIVE WAS WRITTEN** (recipe false-green (4)): all seven rc=0 on the unmodified tree, `go build ./...` deliberately excluded because it is red at base (`cmd/wasm` has no native `main`). Final sweep, all **rc=0**, run by the controller OUTSIDE the executor sandbox: `bash -n` ×2 · `/bin/bash` self-test (**PASS: 34**) · `shellcheck -S warning` · `make test-launchd-drivers` · `check-file-sizes` · `check-changelog` · `check-skills` · `test-check-changelog`. **RECONSTRUCTION PROVEN FAITHFUL, NOT ASSUMED:** the executor's final tree was sha256-manifested before commit-building and `shasum -c` returned **OK on all four files** after replaying `.snap/M1`→`M4`; M2 and M3 are **byte-identical snapshots** (the executor declared them combined and said so unprompted), so they land as one commit with the reason stated rather than as a false bisection. M1 was verified independently green at its own boundary (`PASS: 8` through the newly wired target). **LANDED GREEN, OBSERVED NOT PREDICTED**: head `7de24f03c` — **21** checks, **0** pending, **0** not-green, **4/4** required contexts (`build`/`docs-gate`/`lint`/`test`) pass, `mergeStateStatus=CLEAN` — then squash-merged. `mergeable` was read FIRST per the iteration-198 rule and came back `MERGEABLE` throughout, so no dropped-event lever was reached for. Autoclose scan run on all five commit messages and the PR title/body: **0** hits, known-bad control string matching **1**. **A DORMANT EXPOSURE FOUND AND RECORDED, NOT ACTED ON:** the Repo Profile's declared `MISSION_WORKDIR` — `~/dev/sunholo-data/ailang-motoko` — is **160 commits behind origin/dev** with **7** uncommitted files dated 2026-08-15, among them a **modified `.claude/skills/mission-control/SKILL.md`** (38 lines changed) that the charter says "takes precedence for sessions run here". It does not execute today (this loop runs from the `#558` pin root, and the pin's copy, `~/.claude`'s resolved target and origin are all byte-identical, `cmp` rc=0 three ways), and the residue is **superseded, not unfinished** — both untracked files now exist on origin/dev, `scripts/mission_decisions.sh` byte-identically. So it is not a died-mid-flight trace to adopt; it is a rulebook 160 commits stale sitting at the exact path the Repo Profile tells a future session to use. Gate 0/1 clean: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, ran from the pin root detached and clean at `30176187f`; **running-skill check done on the RESOLVED symlink target** (V1 iteration 241's rule) — `~/.claude/skills/mission-control` resolves to **V1's** main checkout (inode `47681680`) and is byte-identical to origin, as is this pin's own copy (inode `47772680`), and the two are identical to each other. dev at pick time: HEAD was V1's iteration-255 docs record mid-flight (13 checks, none red), **parent control 16 checks with 0 not-green** — and motoko does not own this repo's CI anyway (charter guardrail), so nothing displaced the pick. **0** human directives on `#743` since the watermark `2026-08-17T05:48:45Z` (of **17** comments); ledger valid at **4** rows, **0 OPEN**; inbox **0** unread; **0** open `[nightly-eval]` alarms. External-predicate re-read of the blocked rows run as commands with controls: G1 `#154` `state=OPEN`/`mergedAt=-` (control `#161` **MERGED** with non-null `mergedAt`) — Phase 0 stays CLOSED on G1 alone, so rows 10/11/12 stay parked and **no G3 verdict is claimed** (the registry probe returned empty and an empty probe is a claim, not a fact). Row 5's upstream `#165` **OPEN**, `#166` **OPEN** against `main_dst` — nothing local waits on either. No died-mid-flight traces: `#695` is a stale coordinator PR with no branch in this clone's `git worktree list` (mine: 5, all motoko) and therefore **not attributable to this mission** — left alone per the fleet-filter rule. Weekly sweep and rotation **both not due** (`#743` created `2026-08-17T05:48:23Z` = 07:48 local, AFTER the Monday-07:00 local boundary; 17 comments < 80). Routing: controller `claude:claude-opus-5`; executor `codex:gpt-5.6-sol` (probe rc=0, replied `ok`, 13.6k tokens); evaluator **sonnet** in **its own worktree** (V1 iteration 199's rule — a good judge mutates source, and it did: it ran mutations, including adding a synthetic 24th branch, in a tree that was not mine) — distinct provider from the executor, so generator≠judge holds; **FLAGGED**: my own arm-label repair is Anthropic-authored and judged by an Anthropic evaluator, same provider, different model, and it was named to the judge as a target for exactly that reason. **No planner** — row 6c specifies its own scope and the sprint plan does not cover it, so the directive is controller-authored, which is precisely why its gate list was baselined first. **No designer, no quorum** — the parent doc's quorum is spent and this is a bounded remediation of the artifact its M2 shipped. Rotation pointer untouched at `claude:claude-fable-5`. Metered **$0.00** of $5 — codex and sonnet are both quota buckets. **No GPU, no `rig.lock`** — every arm added here is hermetic by construction. Gates on **darwin/arm64 only**; the windows and ubuntu legs are unrun locally and were read from Gate 3b's matrix (rule 3b(viii)). Gate-5: **NO skill edit** — this iteration's frictions (a refusal that cannot refuse inside `$( )`; an arm whose label outran its observable) are instances of rules the skill ALREADY has (3j's "a guard is not a gate until something reds when you remove it", 3i's "which write does this read"), so they go to the log's Ruled-out rather than into the rulebook; the one genuinely new shape — **an empty scope argument silently widening a query instead of narrowing it** — is recorded as instance 1 and pre-registered, not written in on a single datapoint.

## STATUS 2026-09-01 — ITERATION 31 COMPLETE: **THE QUORUM BLOCKED TWICE ON ONE SURFACE, MY REFUTATION OF IT WAS MEASURED WITH THE WRONG BINARY, AND THE JUDGE CAUGHT THAT.** Pick was the **queue head, row 6n** — the discovery arm that cannot fail for the reason it names, reported at #975 by V1 iteration 308's evaluator. **ROW 6n's OWN STATED BLOCKER IS DISCHARGED.** The row said the runner half (finding B3) *"is unreproduced here and must be measured on the runner, not locally, before any fix is designed"*. It was measured, and it was sitting unread in a sibling's CI: the `launchd drivers (bash 3.2)` job `99402730557` on **V1's open PR [#971](https://github.com/sunholo-data/ailang/pull/971)** head `8a384e81b` fails with exactly `not ok - descendant discovery refuses on the real wall-clock deadline lacked expected message: process-tree discovery failed`, and the surrounding log shows `lane=treatment driver_rc=0 peers: []`, `lane=control driver_rc=0 peers: []`, `INSTRUMENT FAILURE: empty peer set`. So `#975`'s *"refuses with NEITHER message"* understates it: under that configuration discovery **did not refuse at all** — both lanes completed and the run died downstream on the empty-peer-set guard. Control that the log was really parsed: **32** passing `ok` lines in the same fetch. **SCOPE, stated because it is load-bearing: that certifies #971's tree, NOT motoko's HEAD**, whose own `launchd drivers` leg was `success` at `4bd58bef6`. **THE DEFECT, REPRODUCED FIRST-PARTY AND THEN SHARPENED.** Every mutant LANDED (sha256), PARSES (`bash -n` rc=0), effect-asserted against the system's own view, restored from a `cp` backup byte-identical with `git status --porcelain` **0**. Baseline `/bin/bash tools/eval/test_motoko_connection_probe.sh` **rc=0, 41 ok, 0 not ok**. **E2** neuter the in-loop wall clock alone → **rc=0, 41/41, arm 33 still `ok`** (the row's claim, re-derived). **E7** neuter the node ceiling alone → **rc=1, ONLY arm 40 reds, arm 33 still `ok`**. Together those are the precise statement the row did not have: **each branch independently suffices for arm 33**, so the arm cannot discriminate them by construction and no measurement of "which fires first" is needed. **E3 IS A CORRECTION TO MY OWN INFERENCE, AND IT IS THE ROOT OF EVERYTHING THAT WENT WRONG LATER.** Neutering BOTH bounds hangs the suite past a 600s bound; I read that as *"the node ceiling is what terminates the arm at HEAD"*. It does not follow — the sound reading is only *"with the wall clock dead, the ceiling stops the walk"* — and that bad inference is what seeded the design the quorum then rejected. **E8** refuted it directly: the minimal fix alone (three distinct messages, arm 33 asserting the wall-clock one), **no ceiling change**, default `MAX_TREE_NODES=4096` → **rc=0, 41/41, 50s**, so the wall clock is what fires here. **T1**, the load-bearing acceptance, measured rather than predicted: that same fix **plus** the wall-clock mutant, ceiling untouched → **rc=1 in 44s** with arm 33 the failing arm on the exact message. Clean **rc=0/50s** vs mutant **rc=1/44s**, printed side by side — outcomes DIFFER and the mutant is *faster*, not a hang. **THE DOC IS PARKED, NOT LANDED, AND THAT IS THE RIGHT CALL.** Quorum **BLOCKED twice**, **3/3 external reviewers rejecting in BOTH rounds with no absentees** (`.synthesis.absent_reviewers` read at the correct nested path; $0.0806 + $0.0781). Per V1 iteration 257's rule the objection SURFACE was tracked rather than the round count: both rounds localise on **one** surface — the wall-clock-versus-ceiling race — and **no reviewer flipped to pass**, so the disposition is *immature*, not SPLIT. Round 1 rejected raising `MAX_TREE_NODES`; round 2 rejected **not** raising it. The revision and the one re-quorum are spent, and `gpt5-6-sol`'s surviving objection carries **no** concrete reviewer-authored fix, so the narrow-refinement carve-out does not apply. Standing rule 2 forecloses force-passing over three unanimous rejections however good my argument felt. **JUDGMENT park, not `PARKED-ON-LANE`** — nothing here unblocks on a clock. **THE EVALUATOR'S BLOCKING FINDING IS AGAINST ME, AND IT IS CORRECT.** To answer the reviewers' shared premise (*a CI runner might do 4096 iterations inside the 1s window*) I benchmarked **system `pgrep`** at ~**79** iter/s and reported a **~52×** margin. Arm 33 sets `PATH="$live_bin"` and this suite installs its **own** `pgrep` stub at `test_motoko_connection_probe.sh:254-262`, so the walk never calls system `pgrep` at all. Re-derived first-party against the actual stub: **474.9 / 652.7 / 648.6** iter/s → 4096 iterations ≈ **6.3–8.6s**, a **~6–9× margin, not ~52×**; the wrong instrument re-run for the side-by-side reads **92.1** iter/s / 44.5s; negative control confirms the stub resolves first on that PATH. The judge's independent instrument measured ~455 iter/s / ~9s and agrees. **The conclusion survives and never rested on that number** — E8 and T1 measured the behaviour directly — but the figure offered to Mark as grounds for reconsidering three unanimous rejections was inflated ~6× and is corrected in the doc before he reads it. This is rule 3a(i-d)'s scope trap aimed at a *binary* rather than a directory: my control ran, and it ran against the wrong executable. **THE JUDGE ALSO FOUND THE SYNTHESIS BOTH QUORUM ROUNDS CIRCLED AND NEITHER REACHED (D4):** scope `PROBE_MAX_TREE_NODES` to arm 33's own `env` line rather than raising it globally or deleting it — measured free on the happy path (41/41, 47.1s) — which makes the ceiling structurally unreachable inside the window while leaving every other arm at the default; a middle value keeps a bounded fail-fast backstop. Recorded in the doc, **not applied**: applying it would be a controller-invented resolution to a blocked quorum. Evaluator **PASS 78/100, 1 blocking, 4 non-blocking** (sonnet, own worktree at `b76b0823a`, distinct provider from the pi/deepseek designer and from me → generator≠judge holds); it reproduced **every** controller claim before ruling, confirmed C0–C4 including the 43.76s timing, and ran the addition-shaped mutant on the branch-count gate, confirming the current gate at 24 is **silently blind** to an added echo-shaped refusal. Findings 2, 3 and 4 are applied to the doc verbatim. **A LIVE COLLISION, ATTRIBUTED AND LEFT ALONE.** The fleet-account open-PR filter returned **3** — `#997`, `#971`, `#945` — none with a branch in this clone's **16**-entry worktree list. `#971` touches **exactly** row 6n's two files and is `MERGEABLE`/`UNSTABLE`, but branch `mission/iter306-*` is V1's numbering (V1 is at 312, motoko at 31), so it is **not attributable to this mission** and was read, never touched; the hand-over is a cross-mission message. It is **not** superseded by this work and does not supersede it: `PROBE_TREE_DISCOVERY_SECS` is **0** occurrences at motoko HEAD in both files (controls `PROBE_MAX_TREE_NODES` **2**/**3**, negative control **0**). **DEV CI: NOT RED, UNFINISHED.** `checks=18` on `a223e7274` with the control firing (parent **16**); the only non-green is `test`, `pending` — V1's iteration-312 merge still running, and `sunholo-data/ailang` is V1's to own. Gate 0/1: kill switch armed, `gh` on `sunholo-voight-kampff`, tripwire **CLEAN**, invalid-store control REFUSED rc=1 so the inbox read is real (`store: gcp (Firestore, project ailang-multivac)`). **41** unread on the canonical store — and note the truncation: a bare `--json` returns **20**, `--limit 200` returns **41**, so the default limit silently halves the queue. None a motoko directive, none outranking (11 mission-v1, 9 eval-suite, 7 mission-world, 7 package-feedback, 3 mark-attended-steering fleet broadcasts, 1 motoko's own). **0** human directives on `#987` since the watermark, and the **rotation-week catch fired**: `#987` was created 2026-08-31T10:45:34Z with `-prev` = `#850`, so `#850` was read too — **0** since the watermark, and a widened read returns exactly **1**, the `MOTOKO-WORKDIR-2: Yes` at the watermark instant, which is the known-positive control proving the instrument sees him. Ledger valid at **5** rows, **0 OPEN**. Blocked-row predicates re-run **as commands**: upstream `#154` still **OPEN** (control `#175` **MERGED** 2026-08-23, negative control 404s), so rows 10/11/12 stay Phase-0 parked. Weekly sweep **not due** — iteration 30 ran it at the rotation. **RUNNING-SKILL CHECK ON THE RESOLVED SYMLINK:** `~/.claude/skills/mission-control` → V1's checkout (inode `60291442`), **byte-identical to `origin/dev`** (`cmp` rc=0), and this fire's pin worktree copy (inode `61376834`) byte-identical too — second consecutive fire where all three agree. Routing: controller `claude:claude-opus-5`; designer **`pi:ollama/deepseek-v4-flash:0731-cloud`** — the rotation's next entry after the pointer's `claude:claude-fable-5`, probed rc=0, verdict `ok` both runs (366s / 91s, 1 changed file each), pointer advanced; **no planner and no executor ran**, because the doc parked before a plan; evaluator **sonnet in its own worktree**. **Fable unspent.** Metered **$0.1587** of $5 (two quorum rounds; the pi designer lane is flat-rate $0.00). No GPU, no `rig.lock`. Gates on **darwin/arm64**; windows and ubuntu legs unrun locally. Bookkeeping second pick: row 6i's design doc and sprint plan moved `planned/` → `implemented/v0_35_0/` with the status header corrected from `Planned` to the merge it actually landed as. Gate-5: **NO skill edit** — the rule that mattered most (reproduce a judge's finding before believing it) worked, and my own failure was a rule I already had and mis-applied, not a gap. Next: row **6o**.

## STATUS 2026-09-01 — ITERATION 32 COMPLETE: **THE ATTENDED RULING LANDED, AND THE MEASUREMENT I RAN TO ANSWER THE REVIEWERS PARTLY REFUTED THE DOC I WAS DEFENDING.** Pick was **not the queue head**: `D-MOTOKO-6N-1` was answered attended by Mark on 2026-09-01 (commit `878e0a5a0`, author an attended identity, not the fleet bot — provenance checked first-party, an attended identity the fleet bot does not hold), and an attended ruling outranks the queue and unparks its item. Ruling: **(B) HOLD for D4** — scope `PROBE_MAX_TREE_NODES` to arm :449's own `env` line and remove the race structurally. **LANDED as [#1008](https://github.com/sunholo-data/ailang/pull/1008), squashed to `64ca81852`.** Rows **6n** and **6q** close; **6p** closes on its design half with a named residual. **Gate 3b GREEN on the PR head (21/21 checks, `mergeStateStatus=CLEAN`), including `launchd drivers (bash 3.2): success`** — the leg decision (d) named as the ONLY place the runner's behaviour is observable, and the same leg that failed on V1's `#971` with this exact discovery error. **Evaluator PASS 93/100, ZERO blocking** (sonnet, its own worktree at `23e153a80`, distinct provider from the codex executor and the opus controller, so generator≠judge holds). **The iteration's real finding is about my own evidence, not about the code.** Rounds 1–3 of the quorum blocked 3/3 on one surface; `gemini-3-1-pro` flipped to **pass** at round 3, and round 3's two remaining objections were *premise* objections, so I measured them instead of buying a revision. `gpt5-6-sol`'s is **refuted**: a 50,000 ceiling needs 25,000 iter/s to win the race, and the walk forks one process per iteration by construction while bare `/usr/bin/true` spawns at **205–250/s** here — ~100x, a hardware bound rather than a rate extrapolation. `oc-glm-5-2`'s named mechanism is **refuted** (the test file is byte-identical between `a223e7274` and `48817dcdd`, stub region sha256 `abffefd3…` both sides, so the stub cannot have changed) — **but its underlying ask was satisfied and it partly UPHELD me wrong**: re-measured at this base the stub runs **181–200 iter/s** against iteration 31's **474–653**, a **3.3–3.6x spread from ambient load alone** (load average **20.59 on 16 CPUs**, and a `perf_counter` clock agrees with `date +%s`, so granularity is refuted as the cause). That leaves the race leg *stronger* and the backstop leg *weaker* than the doc claimed — headroom **2.2x–5.7x**, not 5.7x — and **makes the doc's own feasible interval EMPTY at its stated margins**. The doc now states that as an explicit trade (satisfy the leg whose violation is a WRONG VERDICT, accept degradation in the leg whose violation is only a WRONG EXPLANATION) rather than as a satisfied constraint. Round 4 then blocked on two consistency defects **I** had introduced, both reviewer-fixed verbatim under the narrow-refinement carve-out — and note the surface finally MOVED off the design after three rounds stuck on it. **Two instrument failures caught by the prescribed controls, not by luck.** (1) My commit-reconstruction script read the executor's snapshots at nested paths when they are flat; the recipe's mandatory sha256 manifest check reported `FAILED` and no work was lost. (2) The **executor's own T1 line is excluded from evidence**: it reported `rc=1 ok=27 wall=51s` with none of the three messages, and the judge could not reproduce it in any dimension — arm 28 is `hermetic live success path completes`, which has no code path to the wall-clock branch. The judge's own T1 reds the **correct** arm 33 at 641s in the cap-shaped form the plan pre-authorised. A number I relayed from a sub-agent was wrong and the judge caught it, which is Gate 2 rule (d) working. **The judge also ran T3, which the executor never reached** (its 50-min cap killed it before it could report): adding a dead-but-real refusal branch moves the count 27→28 and reds the gate, so the extended drift gate **LOOKS**, not merely fires — the exact property row 6q existed to buy. **Second, bookkeeping pick (authorised, not invented): the source clone was 55 commits behind**; reconciled under the standing authorisation `D-MOTOKO-WORKDIR-2` with all three safety predicates re-measured at the moment of use (0 ahead / 0 dirty / 55 behind), `git checkout -B dev origin/dev` rc=0, post-verified **0 behind, 0 dirty, all 17 worktrees intact**. **Metered $0.2179** of the $5 ceiling (quorum rounds 3 and 4). Routing: designer `claude:claude-fable-5` (rotation, Agent pin), planner `opus` (`derive-planner-lane.sh` → `fail-closed:planner-lane-field-missing`, used verbatim), executor `codex:gpt-5.6-sol`, evaluator `sonnet`. **Nothing is parked on Mark.**
