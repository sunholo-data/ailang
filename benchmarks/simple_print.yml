id: simple_print
description: "Simple print statement (entry-module prelude test)"
languages: ["python", "ailang"]
entrypoint: "main"
caps: ["IO"]
difficulty: "trivial"
expected_gain: "very_high"
task_prompt: |
  Write a program that prints the string "Hello, World!" to stdout (without a trailing newline).

  Output only the code, no explanations.
expected_stdout: |
  Hello, World!
