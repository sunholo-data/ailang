id: print_missing_effect
description: "Print without IO effect (should fail with helpful error)"
languages: ["ailang"]
entrypoint: "main"
caps: []  # Intentionally no caps
difficulty: "trivial"
expected_gain: "medium"
expect_failure: true
task_prompt: |
  Write a program that prints "Hello!" (without a trailing newline).

  Output only the code, no explanations.
expected_stderr_contains: "! {IO}"
